# Ascent Schools Application — Project Guide

## Overview
Converting a legacy VB6 school management application (SAS) into a modern **SaaS** web + mobile platform.
Multi-tenant architecture: one DB per school group/society, shared master DB for SaaS control.

---

## Tech Stack

| Layer | Technology | Notes |
|---|---|---|
| Frontend — School App | React JS + Vite | Ant Design UI, Zustand state |
| Frontend — Control App | React JS + Vite | Ant Design UI, Zustand state |
| Backend API | .NET Framework 4.8 Web API | Single API serving both apps |
| ORM / Data Access | Dapper | Lightweight, SQL-friendly, easy multi-tenant connection switching |
| Database | SQL Server | Master DB + one tenant DB per school group |
| Auth | JWT (access + refresh token) | See Auth section below |
| UI Component Library | Ant Design (AntD) | MIT license, fully free, enterprise tables/forms built-in |
| State Management | Zustand | Lightweight, simple, sufficient for this scale |
| HTTP Client | Native `fetch` API | No axios — avoids third-party dependency vulnerabilities |
| Mobile | Android — Kotlin + Jetpack Compose | MVVM, Retrofit, EncryptedSharedPreferences |

---

## Two Applications

### 1. Ascent Control App (`admin.edu-care.in`)
- **Who uses it:** Ascent internal team only (SaaS admin)
- **Purpose:** Onboard school groups, manage subscriptions, toggle modules, manage branding
- **Auth:** Against `ascent_master.control_users`
- **RBAC:** Simple — 3 roles as a column: `super_admin` / `support` / `billing`

### 2. Ascent School App (`{subdomain}.edu-care.in`)
- **Who uses it:** School staff (Principal, Admin Clerk, Fee Clerk, Teachers, etc.)
- **Purpose:** Full school management — students, fees, attendance, marks, library, transport
- **Auth:** Against tenant DB `users` table (after resolving subdomain → tenant DB)
- **RBAC:** Full role/permission system managed per school group

---

## SaaS Architecture: Two-Database Model

### Why two databases
- Schools are legally sensitive about student data → group-level isolation
- One group's load must not affect other groups
- Clean per-group backup / restore / offboarding
- Cross-branch reports within a group remain simple (same DB)

### Database 1 — `ascent_master` (Central / Control Plane)

| # | Table | Description |
|---|---|---|
| 1 | `school_groups` | One row per paying client. Has `subdomain` for tenant routing. `db_username`/`db_password` store per-DB SQL credentials (fastwebhost assigns separate creds per DB). |
| 2 | `schools` | Individual branches under a group |
| 3 | `subscriptions` | SaaS plan, billing cycle, expiry, max_schools, max_students |
| 4 | `master_audit_logs` | SaaS-level events: logins, subscription changes, school creation |
| 5 | `control_users` | Ascent internal staff only |
| 6 | `refresh_tokens` | Hashed refresh tokens for control_users |
| 7 | `school_settings` | App configuration per school branch (1 row per branch) |
| 8 | `modules` | Master list of all system features, seeded with 7 modules |
| 9 | `group_modules` | Module on/off per school group (SaaS admin controls) |
| 10 | `school_modules` | Module on/off per branch (group admin controls, within group limits) |
| 11 | `group_branding` | Default branding for all branches in a group |
| 12 | `school_branding` | Branch-level branding override |

### Database 2 — `ascent_group_{group_id}` (Tenant DB)
One DB per school group. DB name is always `ascent_group_{group_id}` — derived from PK, never stored.
Run `tenant_tables.sql` on onboarding (called automatically by `POST /control/school-groups`).

| # | Table | Description | Source (VB6) |
|---|---|---|---|
| 1 | `permissions` | Granular permission codes (seeded) | *(new)* |
| 2 | `roles` | Staff roles per group/branch (seeded with defaults) | *(new)* |
| 3 | `role_permissions` | Role ↔ permission mapping | *(new)* |
| 4 | `users` | School staff login users | *(new)* |
| 5 | `refresh_tokens` | Hashed refresh tokens for school staff | *(new)* |
| 6 | `user_roles` | User ↔ role ↔ branch assignment | *(new)* |
| 7 | `audit_logs` | Row-level change tracking (FK to users in same DB) | SAS_TransLogData |
| 8 | `academic_years` | Academic year config per school | SAS_AcdYear |
| 9 | `class_groups` | Pre-Primary / Primary / High-School / General | SAS_ClassGrps |
| 10 | `fee_categories` | General / Staff Child / VTPS etc. | SAS_FeeCategory |
| 11 | `classes` | Individual classes/grades | SAS_ClassNames |
| 12 | `sections` | Sections per class (e.g. A, B, C) — school-defined | *(new)* |
| 13 | `fee_types` | Fee types per academic year | SAS_FeeTypes |
| 14 | `terms` | Terms/months per academic year | SAS_TermMonthData |
| 15 | `subjects` | Academic subjects | SAS_Subjects |
| 16 | `payment_modes` | Cash / Cheque / Online | SAS_PaymentMods |
| 17 | `fee_periods` | Monthly fee periods per school/academic year (month_no, year_no, period_label, sequence_no); used when fee_structures.payment_type = 'Monthly' | *(new)* |
| 18 | `fee_structures` | Fee amount per class+category+type+term/period; `payment_type` (Term/Monthly), `fee_period_id` FK → fee_periods, `admission_type` (New/Old/NULL) | SAS_FeeMaster |
| 19 | `buses` | Bus vehicle details | SAS_BussData |
| 20 | `bus_routes` | Bus route definitions | SAS_BusRoutes |
| 21 | `bus_fee_structures` | Bus fee per route+term | SAS_BusMaster |
| 22 | `students` | Student master data; `student_unique_id` (stable cross-year INT, same value across promotions); `section_id` FK → sections; `blocked_reason`, `is_detained`, `detained_reason`, `join_type` columns | SAS_StudentMaster |
| 23 | `fee_receipts` | Payment receipt header (receipt_no, total_amount, status Active/Cancelled, student_unique_id for cross-year reporting) | *(new)* |
| 24 | `fee_receipt_items` | Receipt line items (fee_type_id, term_id, fee_period_id, amount, concession_amount, net_amount) | *(new)* |
| 25 | `gateway_configs` | Payment gateway API keys per school (key_secret never returned in API) | *(new)* |
| 26 | `payment_gateway_orders` | Online payment lifecycle tracking (Pending/Paid/Failed) | *(new)* |
| 27 | `student_mobile_accounts` | Mobile app student accounts (PIN hash) | *(new)* |
| 28 | `student_refresh_tokens` | Mobile student sliding-expiry refresh tokens | *(new)* |
| 29 | `student_attendance` | Daily attendance per student | *(new)* |
| 30 | `exam_types` | Exam type definitions per academic year | *(new)* |
| 31 | `student_marks` | Per-student per-subject marks per exam | *(new)* |
| 32 | `homework` | Homework assignments per class+section (`section_id` nullable FK — NULL means class-wide) | *(new)* |
| 33 | `homework_attachments` | File attachments for homework entries | *(new)* |
| 34 | `announcements` | School/class announcements with optional PDF attachment_url | *(new)* |
| 35 | `school_events` | Events gallery metadata + external media URLs (YouTube/Cloudinary) | *(new)* |
| 36 | `staff` | Employee master per school branch | *(new)* |
| 37 | `staff_attendance` | Daily staff attendance (Present/Absent/Late/HalfDay/OnLeave) | *(new)* |
| 38 | `staff_advances` | Advance/loan records per staff member | *(new)* |
| 39 | `staff_advance_repayments` | Repayments against staff advances | *(new)* |
| 40 | `staff_salary_components` | Salary structure template per staff (Earning/Deduction) | *(new)* |
| 41 | `staff_salaries` | Monthly salary header per staff (Draft/Paid/Cancelled) | *(new)* |
| 42 | `staff_salary_items` | Snapshot of components at salary processing time | *(new)* |
| 43 | `sms_logs` | Audit trail of every SMS sent from the school app | *(new)* |
| 44 | `fee_concessions` | Concession amounts per student per fee type per term/period (school fees) OR per bus route per term (transport); `fee_type_id` nullable — NULL for transport rows; `bus_route_id` nullable FK → bus_routes; three filtered unique indexes: term+fee_type, period+fee_type, transport (bus_route+term); receipt_no format CFR{yr8}{D5} | *(new)* |
| 49 | `class_teacher_assignments` | Class (+ optional section) → teacher(s) per academic year; `section_id` NULL = whole class; drives parent→teacher message routing; two filtered unique indexes | *(new)* |
| 50 | `message_threads` | One parent↔teacher conversation per child; keyed on `student_unique_id` (stable across promotions); `parent_id` → master DB (no FK); status Active/Blocked | *(new)* |
| 51 | `messages` | Messages in a thread; `sender_type` parent/teacher; `sender_name` display snapshot; status Active/Removed; `read_at` = when the other side read it | *(new)* |
| 52 | `message_reports` | Reported messages awaiting admin review (Play UGC requirement); status Open/Reviewed/Removed | *(new)* |

> **Migration:** Run `Database/sections_migration.sql` on existing tenant DBs to add the `sections` table and `section_id` column on `students`. Run `Database/fee_tables_migration.sql` for fee receipt tables. Run `Database/fee_periods_migration.sql` for fee_periods table + payment_type/fee_period_id columns. Run `Database/student_unique_id_migration.sql` to add and backfill student_unique_id on students. Run `Database/fee_concessions_migration.sql` for fee_concessions table. New groups get all 52 tables automatically via the updated `tenant_tables.sql`.

---

## Authentication & Tenant Routing

### Subdomain-based tenant identification
Each school group gets a unique subdomain:
```
srividya.edu-care.in      → resolves to group_id → ascent_group_1
ascent-public.edu-care.in → different group      → ascent_group_2
admin.edu-care.in         → control app (master DB only)
```

### JWT Strategy — Two token types

#### Control App tokens (`tokenType=control`)
- Claims: `tokenType=control`, `userId`, `fullName`, `role`
- Generated by `JwtHelper.GenerateControlAccessToken(ControlTokenClaims)`
- Validated by `ControlAuthAttribute` on all control controllers
- Refresh token stored in `ascent_master.refresh_tokens` (cookie name: `controlRefreshToken`)

#### School App tokens (`tokenType=school`)
- Claims: `tokenType=school`, `userId`, `groupId`, `schoolId`, `dbName`, `perm[]`
- Generated by `JwtHelper.GenerateAccessToken(TokenClaims)`
- Validated by global `JwtAuthenticationFilter`, populates `TenantContext.Current`
- `JwtAuthenticationFilter` skips TenantContext for `tokenType=control` tokens
- Refresh token stored in tenant DB `refresh_tokens` (cookie name: `schoolRefreshToken`)

**Access token:** web/control 480 min / 8 h (`Jwt:AccessTokenMins` in Web.config; was 30); **mobile (parent/student/teacher) 180 days** (`Jwt:MobileAccessTokenMins` = `259200`, code-default 259200 — Phase 96/97; the mobile app does NO cold-start refresh, so this lifetime alone keeps a parent signed in between opens) | **Refresh token:** web apps 7 days (`Jwt:RefreshTokenDays`); **mobile (parent/student/teacher) 365 days** (`Jwt:MobileRefreshTokenDays`, defaults to 365 in code — Phase 80, cuts SMS re-OTP cost); both sliding expiry, HttpOnly cookie, SHA-256 hash in DB

> **Refresh cookie Path = `/`** (Phase 56). All refresh cookies (`schoolRefreshToken`, `controlRefreshToken`, `parentRefreshToken`, `studentRefreshToken`, `teacherRefreshToken`) MUST use `Path=/`. They previously used the route-specific path (e.g. `/school/auth`), which silently broke silent-refresh in production: the API is served under `/api` (`https://edu-care.in/api`), so the real refresh URL is `/api/school/auth/refresh` and a cookie scoped to `/school/auth` is never sent (path mismatch) → 401 → logout the moment the access token expired. It only worked in local dev because there's no `/api` prefix there.

### Password hashing
SHA-256 (same as `JwtHelper.HashRefreshToken`) — used for both `control_users.password_hash` and `users.password_hash`.

### School App login flow
```
1. React app loads → GET /branding (public, no auth) → applies school colors/logo
2. Silent refresh attempted on mount → restores session if cookie is valid
3. User submits login form → POST /school/auth/login with X-Subdomain header
4. API resolves subdomain → group_id → ascent_group_{group_id}
5. Validates username + password_hash against tenant.users
6. Determines schoolId (from users.school_id or first from user_roles)
7. Loads permissions for that school
8. Returns: access token (JWT body) + sets HttpOnly schoolRefreshToken cookie
9. React stores schoolId in localStorage (for refresh calls across page reloads)
10. React stores access token in Zustand memory only (XSS protection)
11. All API calls send Authorization: Bearer {access_token} + X-Subdomain header
```

### School App refresh flow
```
POST /school/auth/refresh?schoolId={id}
Headers: X-Subdomain: {subdomain}
Cookie: schoolRefreshToken (HttpOnly)
→ Rotates refresh token, returns new access token
```

---

## RBAC Design (Tenant DB)

### Tables
- `permissions` — codes like `STUDENT_FEE.COLLECT`, format: `MODULE_CODE.ACTION`
- `roles` — named roles, can be group-wide (`school_id NULL`) or branch-specific
- `role_permissions` — many-to-many
- `users` — school staff; `school_id NULL` = access to all branches
- `user_roles` — user ↔ role ↔ branch (different roles in different branches supported)

### Default permissions seeded (24 codes)
| Module | Permission Codes |
|---|---|
| STUDENT_PROFILE | VIEW, CREATE, EDIT, DELETE |
| STUDENT_FEE | VIEW, COLLECT, EDIT, CANCEL_RECEIPT, CONCESSION |
| ATTENDANCE | VIEW, MARK, EDIT |
| MARKS | VIEW, ENTER, EDIT, PUBLISH |
| LIBRARY | VIEW, ISSUE, RETURN, MANAGE |
| TRANSPORT | VIEW, MANAGE |
| HOSTEL | VIEW, MANAGE |

### Default roles seeded
`Principal`, `Admin Clerk`, `Fee Clerk`, `Class Teacher`, `Librarian`

### API permission check (every protected school endpoint)
```
[RequireModule(ModuleCodes.StudentFee)]           ← checks group_modules + school_modules
[RequirePermission(PermissionCodes.StudentFee.Collect)]  ← checks permissions[] in JWT
public HttpResponseMessage CollectFee(...) { }
     ↓
SchoolAuthAttribute       → TenantContext.Current == null → 401
RequireModuleAttribute    → module disabled for group/branch → 403
RequirePermissionAttribute → permission missing in JWT claims → 403
     ↓
Controller executes
```

### Filter execution order
Declare `[RequireModule]` before `[RequirePermission]` — Web API runs attribute filters in declaration
order within the same scope. `SchoolAuthAttribute` is always first (applied at class level on
BaseSchoolController). Module check runs second, permission check last.

### ModuleRepository.IsModuleEnabled(groupId, schoolId, moduleCode)
Single SQL query covering the full three-level hierarchy:
- group row disabled (is_enabled=0) → CAST(0 AS BIT) — hard block
- school row exists                 → use school is_enabled value
- no school row                     → ISNULL(group is_enabled, 1) — inherit, default on
- module code not in modules table  → returns null → treated as false (disabled)

---

## Module Access Control

### Three-level hierarchy
```
modules (system-wide, Ascent manages)
  └── group_modules (per society, SaaS admin controls)
        └── school_modules (per branch, group admin controls)
```

### Rules
- Group disabled → branch **cannot** override (hard block)
- Group enabled → branch can disable for their branch
- No row in `school_modules` → inherits group setting
- `modules.plan_tier` — Basic / Standard / Premium (enforced against subscription)

### Known modules (seeded in master DB)
| Code | Name | Min Plan |
|---|---|---|
| STUDENT_PROFILE | Student Profile | Basic |
| STUDENT_FEE | Student Fee | Basic |
| ATTENDANCE | Student Attendance | Basic |
| MARKS | Student Marks | Standard |
| LIBRARY | Library | Standard |
| TRANSPORT | Transport | Basic |
| HOSTEL | Hostel | Premium |

---

## Branding

### What is configurable per school/group
- Logo (header, login page, receipts/reports)
- Favicon
- Primary color, secondary color, header background, nav text color
- Display name (override school name shown in UI)
- Tagline (shown on login page)
- Receipt/report footer text
- Login page background image

### Hierarchy
```
group_branding   ← default for all branches
  └── school_branding  ← overrides group when set for a specific branch
```

### How React applies it (School App)
1. Public endpoint `GET /branding` called before login (no auth needed)
2. API resolves subdomain → returns most specific branding (branch → group fallback)
3. React applies via Ant Design `ConfigProvider` theme tokens:
   ```jsx
   <ConfigProvider theme={{ token: { colorPrimary: branding.primaryColor } }}>
   ```
4. CSS variables set on `document.documentElement` for non-AntD elements.

---

## Tenant DB Provisioning (on school group onboarding)
Tenant DB is provisioned **manually** — the control app UI only creates the master DB record.

**Control app flow:**
1. `POST /control/school-groups` with `{ groupName, subdomain, description }`
2. API inserts into `school_groups` → gets `group_id`, seeds `group_modules` (all enabled)
3. UI shows success message with the DB name to provision (`ascent_group_{group_id}`)

**Manual DB setup (run in SSMS on SQL Server):**
1. Connect to SQL Server
2. Run `Database/tenant_tables.sql` while connected to `master` — creates `ascent_group_{N}` DB with all 52 tables + seeds permissions and roles
3. Connect to `ascent_group_{N}`
4. Run `Database/seed_tenant_data.sql` — seeds role-permission mappings, payment modes (Cash/Cheque/Online), and default admin login
5. Set `@SchoolId` in `seed_tenant_data.sql` to the `school_id` from `ascent_master.schools` (add a school branch first via the control app)
6. In control app → school group → **Edit Settings** → fill in **DB Name**, **DB Username**, **DB Password** → Save
7. School group is ready to use

---

## Build Order — Status

```
Phase 0  ✅  Project scaffolding + conventions (API solution + React apps)
Phase 1  ✅  Control App (full: onboarding, subscriptions, modules, branding, user management)
Phase 2a ✅  School App — login + JWT + branding load + silent refresh
Phase 2b ✅  School App — RBAC screens (roles/permissions, user management)
Phase 2c ✅  API middleware (module check + permission filter — both fully wired)
Phase 3  ✅  Master data screens (academic years, class groups, classes, sections, fee categories, fee types, terms, subjects, payment modes)
Phase 4a ✅  Student Profile module
Phase 4b ✅  Student Fee module (fee structure setup, fee collection, receipts + cancel)
Phase 4b+ ✅ Payment Gateway integration (Razorpay; pluggable for Cashfree/PayU; webhook support)
Phase 4c ✅  Attendance module (mark attendance + monthly summary; student_attendance table from 6A)
Phase 4d ✅  Marks module (exam types + marks entry grid built in Phase 6C)
Phase 4e ⏸   Library module (deferred — schema not yet designed)
Phase 4f ✅  Transport module (buses, routes, bus fee structure, student assignment)
Phase 5  ✅  Dashboard + cross-module summary (students, attendance, fee trend, recent receipts, upcoming homework)
Phase 6A ✅  Mobile app — DB schema (all new tables)
Phase 6B ✅  Mobile app — Backend APIs (auth + data endpoints)
Phase 6C ✅  Mobile app — Web UI additions (marks entry, homework, announcements)
Phase 6D ✅  Mobile app — Android app (Kotlin + Jetpack Compose)
Phase 7  ✅  Bulk CSV import — students (POST /school/students/bulk) + fee structure (POST /school/fees/structure/bulk); papaparse client-side parsing, preview table, example data, error report download
Phase 8  ✅  Student promotion — multi-year model (one row per student per academic year); PromoteStudentsPage: from year+class+section → preview → to year+class+section (required) → confirm; mobile auth re-points to latest student_id on login
Phase 9  ✅  Android app — parent-only login (student login removed); product flavors for white-label multi-school builds (one APK per school, unique applicationId + SCHOOL_CODE baked in per flavor)
Phase 10 ✅  Android app — SMS OTP auth (replaces PIN login); device binding (one device per parent); teacher staff login via bottom sheet; SmsHelper for smslogin.mobi gateway
Phase 11 ✅  School events gallery — video + image gallery tab in mobile app; media stored externally (YouTube for videos, Cloudinary for images); SQL Server stores metadata + URLs only; school web app has Events Gallery management page
Phase 12 ✅  SMS Center — send Absent / Fee Due / Custom SMS to parents; client-side batching (25/50 per API call) + server-side Task.WhenAll parallel dispatch; smslogin.mobi gateway; sms_logs audit trail; SMS History tab
Phase 13 ✅  Student lifecycle flags — Block (status=Blocked, excluded from attendance+SMS) and Detain (is_detained flag, excluded from promotion, remains Active for daily ops); warn on promote preview with detained count
Phase 14 ✅  Blood Group Search — dedicated page under Students; filters GET /school/students by bloodGroup param; colour-coded blood group tags; PDF + CSV export
Phase 15 ✅  Reports additions — Regular Absentees (threshold-based, date range, class/section filter) + Detained Students report; both with PDF/CSV export
Phase 16 ✅  Per-DB SQL credentials — `db_username`/`db_password` added to `school_groups`; `TenantConnectionFactory` looks them up with 15-min cache, falls back to template; control app Edit Settings modal exposes all three DB fields; cache invalidated on save
Phase 17 ✅  Parent OTP auto-link — `request-otp` gates on student existing in school's DB by `father_mobile` (no pre-existing parent account required); `verify-otp` auto-creates `parent_accounts` + auto-links all matching students from that school; no manual SQL or link-child flow needed
Phase 18 ✅  School-scoped children + child link upsert — `GET /mobile/auth/parent/children` filters by `group_id` (current school's group from X-School-Code) so each white-label app only shows its own school's children; `POST /mobile/auth/parent/select-child` validates link belongs to current school (defense-in-depth); `verify-otp` auto-link loop changed to upsert by `admission_no` — updates `class_name`, `student_name`, `student_id` on existing links rather than skipping them (fixes NULL class_name on old links and handles post-promotion student_id changes)
Phase 19 ✅  Homework section field — `section_id` (nullable FK) added to `homework` table; web UI HomeworkPage shows Section dropdown (loaded by class); mobile homework feed filters by `section_id IS NULL OR section_id = @sectionId` so class-wide entries always show but section-specific entries only reach matching section
Phase 20 ✅  Bulk student import extended — 7 new columns added: `MotherMobile`, `AadharNo`, `Caste`, `CasteCode`, `Religion`, `JoiningClass`, `MotherTongue`; `StudentsImportPage` column reference table + example rows updated; `StudentBulkRow` DTO + `BulkCreate` INSERT extended
Phase 21 ✅  Android login screen logo + app icon fix — school logo (`R.mipmap.ic_launcher_round`) displayed above school name in `SmsAuthScreen`; `AndroidManifest.xml` fixed from `@drawable/ic_launcher` → `@mipmap/ic_launcher` (was loading old vector XML, bypassing Image Asset mipmap files); flavor background XML changed to plain white; `versionCode` bumped to 2
Phase 22 ✅  Android UI polish — custom navy/gold palette in `Theme.kt`; login screen: diagonal gradient bg + frosted glass card + `AsyncImage` logo (Coil); shimmer skeletons on all data screens; attendance: monthly calendar grid + summary card + stat pills; marks: animated bar charts per subject + score badge + staggered entrance; `HomeScreen`: `AnimatedContent` tab transitions + navy `TopAppBar` + navy-tinted `NavigationBar`; `NavigationBar` crash fixed (removed `tonalElevation = Dp.Unspecified`); version string shown on login screen (`BuildConfig.VERSION_NAME`)
Phase 23 ✅  Android release build fix — R8/ProGuard stripping Google Tink (used internally by `EncryptedSharedPreferences`) caused silent crash on splash screen in Play Store release builds; fixed by adding full Tink keep rules to `proguard-rules.pro`; `TokenStore` wrapped in try-catch with plain `SharedPreferences` fallback so any future Tink failure degrades gracefully instead of crashing; `versionCode` bumped to 5, `versionName` = "1.0.5"
Phase 24 ✅  Fee collection redesign — 5 separate collection screens (Admission/School/Transport/Hostel/Other); `student_unique_id` added to `fee_receipts`; new receipt_no format `{prefix}{yr6}{00001}` (T=Transport, H=Hostel, O=Other, classGroupPrefix for Admission/School); `GET /school/fees/student-unique/{uniqueId}?feeTypeCategory=` returns cross-year outstanding summary across all academic years; fee type category filter uses `description` LIKE patterns; Admission screen filters students by `join_type='New'`; checkboxes replace editable Pay Now; concession locked at 0 (configurable in future); `joinType` filter added to `GET /school/students`; `student_unique_id` added to StudentListDto; old `FeeCollectionPage` removed from nav/routes
Phase 25 ✅  Monthly fee periods + fee structure enhancements — `fee_periods` table (month_no, year_no, period_label, sequence_no) added as tenant table 17; `payment_type` (Term/Monthly) and `fee_period_id` FK added to `fee_structures`; `fee_period_id` added to `fee_receipt_items`; `student_unique_id` stable INT identifier added to `students` (auto MAX+1 on insert, same value retained on promotion, backfilled via migration); `FeeStructurePage` supports Term/Monthly toggle (Monthly uses fee_periods as columns, Term uses terms) and Admission Type filter (New/Old/All); `FeePeriodsTab` added to Master Data (GET/POST/PUT `/school/master/fee-periods`); `MasterDataPage` refactored — each tab loads its own data independently; `FeePeriodDto`/`SaveFeePeriodRequest` in `FeePeriodDtos.cs`
Phase 26 ✅  Fee Concession — `fee_concessions` table (school_id, academic_year_id, student_id, student_unique_id, fee_type_id, term_id nullable FK → terms, fee_period_id nullable FK → fee_periods, concession_type, amount, remarks, receipt_no); two filtered unique indexes (one per term_id IS NOT NULL, one per fee_period_id IS NOT NULL) for per-term/per-period concession isolation; receipt_no format `CFR{yr8}{D5}`; `FeeConcessionRepository` upserts by student+fee_type+term/period match; `DELETE /school/fees/concessions/{id}` soft-cancels (status=Cancelled) with Popconfirm in UI; outstanding subquery matches by `ISNULL(fc.term_id,0)=ISNULL(fs.term_id,0)` so each term/period line deducts only its own concession; `ConcessionAmount` bug fixed in `FeeCollectionBase.jsx` (was hardcoded 0.00, now reads `li.concessionAmount`); `FeeConcessionPage` has 3 tabs with Term/Period dropdowns in Individual + Bulk tabs; Concession List has delete column (Popconfirm) + Term/Period column; "Fee Concession" added to Fees nav sub-menu
Phase 27 ✅  Transport enhancements — Student Assignment tab: Year/Class/Section/Route filter bar (sections load per class); no auto-load on mount; `GET /school/transport/students` extended with academicYearId, classId, sectionId params; uses `IN ('Active','Y')` for legacy data; `StudentTransportDto` now includes `StudentUniqueId` (INT) and `AcademicYearId`; transport update route changed from `{studentId:long}` to `{studentUniqueId:int}`; UPDATE WHERE uses `student_unique_id + academic_year_id` (stable cross-year) instead of `student_id` (year-specific IDENTITY)
Phase 28 ✅  Transport fee collection — route-based outstanding (not class-based); `bus_route_id INT NULL` added to `fee_receipt_items` (migration: `transport_receipt_migration.sql`); `GetCrossYearFeeSummary` branches on `feeTypeCategory=Transport`: reads amounts from `bus_fee_structures` (route+term+year), skips years with no `bus_route_id` assigned, paid subquery matches `fri.bus_route_id + fri.term_id + student_id`; `fee_structures` transport rows ignored entirely; `FeeLineItemDto` + `CollectFeeItem` get `BusRouteId?`; `FeeReceiptItemDto` gets `RouteName`; `CollectFee` stores `bus_route_id` in receipt items; `GetReceiptById` JOINs `bus_routes` for receipt display; `FeeCollectionBase.jsx` `buildItems` passes `BusRouteId`; route name shown as FeeTypeName in collection UI
Phase 29 ✅  Transport concession — `bus_route_id INT NULL` added to `fee_concessions` (migration: `transport_concession_migration.sql`); `fee_type_id` made nullable (NULL for transport rows, bus_route_id set instead); existing unique indexes recreated with `fee_type_id IS NOT NULL` filter; new `UQ_fee_concessions_transport` index on `(student_id, bus_route_id, school_id, term_id)`; `FeeConcessionDto` + save requests extended with `BusRouteId?`; controller validation changed from `FeeTypeId <= 0` to `FeeTypeId == null && BusRouteId == null`; `FeeConcessionRepository` all 4 methods handle `bus_route_id`; `GetTransportLineItems` gains concession subquery matching by `bus_route_id`; `StudentRepository.GetAll` + `StudentsController.GetStudents` gain `busRouteId` optional filter; `FeeConcessionPage` gains global Radio.Group "School Fee"|"Transport" toggle — transport mode shows route+term dropdowns, bulk mode loads students by `?busRouteId=`
Phase 30 ✅  Hostel module — `hostels` table (hostel_id, hostel_name, description, capacity, contact_no, address, no_of_rooms, school_id, created_by); `hostel_fee_structures` table (hostel_id + academic_year_id + term_id/fee_period_id + payment_type Term/Monthly + amount); `hostel_id INT NULL FK` added to `students`, `fee_receipt_items`, `fee_concessions`; two filtered unique indexes on `fee_concessions` for hostel concessions (`hostel_id IS NOT NULL AND term_id IS NOT NULL`, `hostel_id IS NOT NULL AND fee_period_id IS NOT NULL`); `students.hostel_name VARCHAR` replaced by `hostel_id INT FK`; `HostelRepository` (CRUD + GetFeeStructure/SaveFeeStructure + BulkSaveFeeStructure + GetStudentHostels/UpdateStudentHostel); `HostelController` at `[RoutePrefix("school/hostel")]` (GET/POST hostels, GET/POST fee-structure, POST fee-structure/bulk, GET/PUT students); `GetCrossYearFeeSummary` branches on `feeTypeCategory=Hostel` → reads from `hostel_fee_structures` via `GetHostelLineItems`; `CollectFee` stores `hostel_id` in `fee_receipt_items`; `GetReceiptById` JOINs `hostels` for HostelName; `FeeConcessionRepository` all 4 methods handle `hostel_id`; `FeeConcessionPage` gains "Hostel" Radio.Button (3rd mode); `HostelPage.jsx` — 4 tabs: Hostels CRUD, Fee Structure (Term/Monthly toggle), Student Assignment (year/class/section/hostel filter bar), Bulk Import CSV; `StudentFormPage.jsx` Other Info tab: hostelName Input → hostelId Select (loaded from `/school/hostel`); `AppLayout.jsx` gains Hostel nav item (HomeOutlined icon) after Transport; migration: `Database/hostel_migration.sql`
Phase 31 ✅  Parent web portal (`ascent-parent-app/`) — gateway-only fee payment for parents; reuses all existing `/mobile/auth/parent/*` endpoints; React/Vite app on port 5174; Ant Design 5 + Zustand; routes: /login (SMS OTP — mobile → OTP → 30s resend), /select-child (list children scoped to school group), /fees (FeeHomePage: 3 tabs School/Transport/Hostel → cross-year outstanding with checkboxes → Razorpay checkout → /fees/success), /fees/success (PaymentSuccessPage: receipt details + print); `MobileFeesController` at `[RoutePrefix("mobile/fees")]` — `GET /mobile/fees/outstanding?feeTypeCategory=` (wraps GetCrossYearFeeSummary), `GET /mobile/fees/gateway-config`, `POST /mobile/fees/payment-orders` (creates Razorpay order, looks up student_unique_id + year-specific student_id + online payment mode id server-side), `POST /mobile/fees/payment-orders/{id}/verify` (verifies HMAC, creates receipt with "Parent Portal" as createdBy); `GatewayRepository.GetOnlinePaymentModeId` — looks up `payment_modes WHERE is_online=1`; `StudentRepository.GetStudentUniqueId` and `GetStudentIdForYear` helper methods; parent app sends `X-School-Code` + `X-Subdomain` headers; `MobileCreateOrderRequest` DTO (AcademicYearId, FeeTypeCategory, Items); items sent with `Amount=li.outstanding, ConcessionAmount=0`; `MobileFeesController.cs` + `MobileCreateOrderRequest` registered in `AscentSchools.API.csproj`; `.env.demo` and `.env.srividya` gitignored
Phase 32 ✅  Legacy receipt bulk import — `POST /school/fees/receipts/bulk` (max 1000 rows); `BulkReceiptRow` + `BulkReceiptImportRequest` DTOs in `BulkImportDtos.cs`; `BulkImportReceipts` in `FeeRepository`: name-based lookups (fee_types, terms, fee_periods, bus_routes, hostels, payment_modes, academic_years), groups rows by LegacyReceiptNo (if provided) else by AdmissionNo+AcademicYear+PaymentDate+PaymentMode, validates per group (student exists, payment mode exists, date parseable), inserts one receipt per group with multiple line items in a transaction (receipt_no atomicity), LegacyReceiptNo stored in remarks as "Legacy: {no}"; `FeeReceiptsImportPage.jsx` — CSV template download, column reference, example rows, papaparse client-side upload, preview + result with error table + download errors CSV; nav item "Legacy Receipt Import" under Fees sub-menu; `GET /school/fees/receipts` gains `createdAfter` DateTime? filter (on `fee_receipts.created_at`) for end-of-day sync; `ReceiptsPage.jsx` gains "Created after" DatePicker (with time) + "Export CSV" button (client-side papaparse CSV from loaded receipts)
Phase 33 ✅  Android fee collection redesign — fee screen updated to match parent web portal (3 category tabs: School/Transport/Hostel; cross-year outstanding grouped by academic year; concession amounts shown per item; selection scoped to one year at a time — switching year clears prior selection); `ApiService.kt` old endpoints (`/mobile/student/fees`, `/mobile/student/fees/order`, `/mobile/student/fees/verify`) replaced with new parent portal endpoints (`GET /mobile/fees/outstanding?feeTypeCategory=`, `POST /mobile/fees/payment-orders`, `POST /mobile/fees/payment-orders/{gatewayOrderId}/verify`); `ApiModels.kt` DTOs updated: `MobileFeeLineItemDto` gains `feePeriodId`, `busRouteId`, `hostelId`, `concessionAmount`; `MobileCreateOrderRequest` removes `paymentModeId` (server resolves online mode), adds `feeTypeCategory`; `MobileFeeOrderItem` gains `feePeriodId`, `busRouteId`, `hostelId`; `MobileOrderResponse` gains `gatewayName`; `FeeRepository.kt` updated: `getOutstanding(feeTypeCategory)` returns `List<MobileFeeSummaryDto>`, `verifyPayment` takes `gatewayOrderId` as explicit param; `FeeViewModel.kt` rewrites state to `FeeUiState.Success(years: List<>)`, adds `selectedCategory` StateFlow, `selectCategory()` clears and reloads; `FeeScreen.kt` rewrites with `TabRow`, year-grouped `LazyColumn`, per-year Select All, sticky pay bar; `HomeScreen.kt` + `MainActivity.kt` callback signature updated: `paymentModeId: Int` → `feeTypeCategory: String`; `GatewayDtos.cs` `MobileCreateOrderRequest` renamed to `MobileParentOrderRequest` to resolve compile-time ambiguity with `Mobile.Data.MobileCreateOrderRequest`; `MobileFeesController` updated to `MobileParentOrderRequest`; `Global.asax.cs` `Access-Control-Allow-Headers` extended with `X-School-Code` (was blocked by CORS preflight for parent web app); parent app `LoginPage.jsx` fixed: `MobileNo` → `Mobile` to match `ParentOtpRequest` DTO field name
Phase 34 ✅  Android persistent session — fix for "token expired, must re-login with OTP after closing app for a day"; root cause: OkHttp's default cookie jar is in-memory so the 7-day `parentRefreshToken` HttpOnly cookie was lost on app kill; `PersistentCookieJar` class added to `RetrofitClient.kt` — serializes all OkHttp cookies to a plain `SharedPreferences` file (`ascent_cookies`) so they survive process death; `RetrofitClient.init()` signature changed from `(TokenStore)` to `(TokenStore, Context)` to create the cookie prefs — `AscentApp.kt` updated accordingly; `AuthRepository.kt` gains `silentRefresh()` (calls `POST /mobile/auth/parent/refresh`, saves new access token) and `silentRefreshTeacher()` (calls `POST /mobile/auth/teacher/refresh`); `MainActivity.kt` adds `isCheckingSession` state (true when stored token exists on cold start) + `LaunchedEffect(Unit)` that calls the appropriate silent refresh, shows `CircularProgressIndicator` while waiting, then clears session and shows login screen if refresh fails
Phase 35 ✅  VB6 legacy integration — static API key replaces JWT auth for the legacy VB6 (SAS) application; `integration_api_key VARCHAR(50) NULL` added to `ascent_master.school_settings` with a filtered unique index (migration: `Database/vb_integration_migration.sql`); key generated per school via `NEWID()` in SSMS; `JwtAuthenticationFilter` upgraded to `async Task` and extended with an `X-Api-Key` header check before Bearer JWT — on match, does a master-DB lookup (`school_settings → schools → school_groups`) to resolve tenant context and grants all 24 permission codes so every existing school endpoint works transparently; `LookupApiKeyAsync` uses `TenantConnectionFactory.GetMasterConnection()` + Dapper; `VbIntegration/AscentAPI.bas` — complete VB6 module (9 sections): HTTP core (`ApiCall`/`ApiGet`/`ApiPost`/`ApiPut` via `WinHttp.WinHttpRequest.5.1` late binding), JSON helpers (`JsonStr`, `JsonNum`, `JsonBool`, `JsonSuccess`, `JsonMessage`, `JsonData`, `JsonSplit`, `BuildObj`), master data, students (GetStudents/GetStudent/CreateStudent/UpdateStudent), fees (GetStudentFees/CollectFee/BuildFeeItem/GetReceipts/GetReceipt), attendance, bulk student import, legacy receipt import; `VbIntegration/AscentAPI_Examples.bas` — 13 practical examples: startup/connection test, master data debug printer, listbox student loader, local-DB sync, fee outstanding display, cash/cheque payment collection, bulk student import (batched 100/call), legacy receipt import (batched 200/call), end-of-day receipt pull to local Access table, attendance grid display, sections combobox filler, Immediate Window test sequence
Phase 36 ✅  School Settings — per-branch app configuration stored in `ascent_master.school_settings`; `GET /school/settings` returns current settings (or empty defaults if no row); `PUT /school/settings` upserts the row; `SchoolSettingsController` at `[RoutePrefix("school/settings")]` extends `BaseSchoolController`; `SchoolSettingsRepository` reads/writes master DB via `GetMasterConnection()`; `SchoolSettingsDto` + `UpdateSchoolSettingsRequest` in `Core/DTOs/School/Settings/SchoolSettingsDtos.cs` cover 3 groups: Admission (admissionNoType, categoryWiseAdmissions, newStudentEntryMode, 4 prefix fields), Fee & Billing (feeReceiptLock, feeReceiptPrint, receiptPrintCopies, transportFeeIncluded, fineEnabled, receiptFeeTypeSeparator, billNoSeriesType, studentConcessionEnabled, feeMessageToTeacher, billingStatus), Reports & Institution (progressReportType, institutionHeadName, institutionHeadSignature, otherSubjectsType); `SchoolSettingsPage.jsx` — 3 AntD Cards with grid layout; loads on mount, saves via PUT; navigation restructured: Roles & Permissions, User Management, and Payment Gateway moved under a "Settings" collapsible sub-menu (SettingOutlined icon) in `AppLayout.jsx`; new sub-menu item "School Settings" at `/settings/school`; routes updated: `/settings/school`, `/settings/roles`, `/settings/users`, `/settings/payment-gateway`; `Database/school_settings_seed_migration.sql` inserts missing `school_settings` rows for all existing school branches
Phase 68 ✅  Android child-context restore after refresh (Android-only) — fixes "Please select a child first" on every screen after closing & reopening the app (or after a mid-session token refresh). Cause: the parent **refresh** endpoint returns a parent token WITHOUT child context (by design — the refresh cookie is parent-level), and nothing re-selected the child; data endpoints (`[MobileAuth(requireChildContext:true)]`) then reject with `MobileAuthAttribute`'s "Please select a child first." The Phase 65 session-keepalive surfaced it — previously token expiry forced a full re-login (which re-selected the child); now the session persists via refresh, exposing the missing child context. Fix: persist the selected child's `parent_children.link_id` as `TokenStore.childLinkId` (set in `AuthRepository.selectChild`; cleared on logout); then re-establish child context after every refresh — (a) **cold start** (`MainActivity` `silentRefresh().onSuccess { if childLinkId>0 selectChild(childLinkId) }`), and (b) **mid-session** (`RetrofitClient.refreshAccessToken` now, for a parent, calls `/mobile/auth/parent/select-child` with the refreshed token + stored linkId and returns that child-context token). linkId is stable across promotions (link upsert by admission_no, Phase 18), so re-selecting is safe. Android-only, compile-verified; ships in the next build. Separately confirmed (user issue): a phone stuck on an old versionCode despite a bumped Gradle is a Play **review/rollout/track** matter, not a build bug — the built AAB's versionName/versionCode are correct (verify with `aapt2 dump xmltree --file base/manifest/AndroidManifest.xml <aab>` / sideload `assemble<Flavor>Release` APK).
Phase 71 ✅  Mobile update gate made opt-in + manual "Update App" button — Phase 57's launch-time force/soft update prompt fired for every published app the moment `min`/`latest` versionCodes were configured, with no way to disable the auto-prompt without un-arming the row. Added an `auto_update_enabled BIT NOT NULL DEFAULT 0` column to `ascent_master.app_config` (migration `Database/app_config_auto_update_migration.sql`, idempotent; **defaults 0 = OFF for all existing rows so auto-prompts stop immediately on apply**). `AppConfigDto` gains `AutoUpdateEnabled`; `AppConfigRepository.GetConfig` selects it; `MobileAppConfigController.GetConfig` now ANDs both computed booleans with `cfg.AutoUpdateEnabled` — when the flag is 0 the server always returns `updateRequired=updateAvailable=false` regardless of the reported versionCode (still **fails open** when no config row exists). Turn auto-prompts back on per app with `UPDATE app_config SET auto_update_enabled=1 WHERE application_id='...' AND platform='android'`. **Android:** `AppConfigRepository.kt` (`checkVersion()` → `UpdateStatus.Ok/Soft/Forced`, fail-open on any error) reads `BuildConfig.APPLICATION_ID`/`VERSION_CODE`; `SmsAuthScreen` login footer now shows `Version {VERSION_NAME} (build {VERSION_CODE})` and a manual **"Update App"** `TextButton` → `openPlayStore(context, null)` (always available regardless of the flag, so users can self-update even when auto-prompts are off). `app/build.gradle.kts` `versionCode` 10→18, `versionName` "1.1"→"1.7" (release signing config from Phase 47 still applies). Auto-prompt UI (`ForceUpdateScreen`/`UpdateAvailableDialog`, Phase 57) unchanged — just gated by the new flag.
Phase 87 ✅  Duplicate controller-name routing fix (control branding logo upload + R2 endpoints were 404) — the control app's branding logo upload failed with `Unexpected token '<', "<!DOCTYPE "... is not valid JSON` — the raw `fetch` to `POST /control/uploads/image` got an **HTML 404 page** and the frontend called `res.json()` before checking `res.ok`, masking the real status. Root cause: **two controller classes were both named `UploadsController`** — `Controllers/Control/UploadsController.cs` (`[RoutePrefix("control/uploads")]`, the pre-existing branding uploader) and `Controllers/School/UploadsController.cs` (`[RoutePrefix("school")]`, added in Phase 84 for R2). ASP.NET **Web API resolves controllers by their simple class name**, so two controllers sharing a name collide and **neither's attribute routes get registered** → every route on both 404s (confirmed live: `control/uploads/image`, `school/uploads/presign`, `school/media` all returned `HTTP 404 text/html`). This silently meant the **entire R2 feature (Phase 84/85) was dead** on the server, not just branding. Fix: renamed the classes to unique names — `ControlUploadsController` and `SchoolUploadsController` (+ its constructor) — **route templates unchanged** (they're defined by `[RoutePrefix]`/`[Route]`, not the class name), so no frontend URL changes. After redeploy both routes return `HTTP 401 application/json` (reachable; 401 only = no token). **Rule: controller class names must be globally unique across the whole API project, even in different namespaces/folders** — when adding a controller, don't reuse a bare name like `UploadsController`/`SettingsController`; prefix it (`SchoolUploadsController`). Backend-only fix; API redeployed. (No control-app rebuild — there's no `deploy-control.ps1`; control app is deployed manually to `admin.edu-care.in`, but this fix didn't need it.)
Phase 88 ✅  Mobile explicit refresh token — fix "logged out after ~1 day" (Android session persistence) — the 365-day mobile session (Phase 80) never worked in the field: users were back at the OTP mobile-number screen after ~1 day. Root cause: staying logged in depends on the **8-hour access token** (`Jwt:AccessTokenMins`) + a **successful silent refresh**, and the refresh endpoint read the refresh token **only from the HttpOnly `parentRefreshToken` cookie** (`GetRefreshCookie`). On Android that cookie only survives app-kill via the `PersistentCookieJar` (serializing an HttpOnly cookie to SharedPreferences, Phase 34/70) — a fragile round-trip (host-only/domain semantics lost on cookie rebuild, prefs can clear). When it fails, the overnight cold-start `silentRefresh()` 401s → `tokenStore.clear()` → OTP screen. The 365-day cookie/DB expiry were correct server-side but never got a chance to matter. **Fix — the app holds its own refresh token and sends it explicitly** (standard native pattern; stops depending on the cookie): **Backend:** `MobileAuthResponse`/`TeacherAuthResponse` gain `RefreshToken` (returned in the JSON body from `BuildParentAuthResponse`/`BuildStudentAuthResponse`/`BuildTeacherAuthResponse`); the parent/student/teacher refresh endpoints now read the token via a new `ResolveRefreshToken(cookieName)` = **`X-Refresh-Token` header FIRST, then cookie** (header wins because the app-held copy is the reliable one — a stale cookie rotated out from under the app would otherwise false-401; web clients send no header so they fall back to the cookie unchanged). Cookie is still set/rotated for web/back-compat. **Android:** `TokenStore.refreshToken` in EncryptedSharedPreferences (cleared on logout via `clear()`); saved from every login/verify-OTP/refresh response (**server ROTATES on each refresh — must re-save the new value or the next refresh reuses a revoked token**); `ApiService.refreshParentToken`/`refreshTeacherToken` gained an `@Header("X-Refresh-Token")` param; `AuthRepository.silentRefresh`/`silentRefreshTeacher` pass the stored token + persist the rotated one; `RetrofitClient.refreshAccessToken` (the OkHttp `Authenticator` 401 path) sends the header + saves the rotated `refreshToken` from the body. `PersistentCookieJar` kept as belt-and-suspenders but no longer load-bearing. `versionCode` 21→**22**, `versionName` "2.0"→**"2.1"**. **One-time transition:** first launch after update sends an empty header → server falls back to the still-valid cookie (if present) and the app captures the body token seamlessly; if the cookie is already dead, one OTP re-login then sets it for 365 days. No DB/CORS change (browser web app never sends the header). API compiles (MSBuild Release). Android edits compile-ready (DTOs under `data.api.**` already ProGuard-kept). Deploy API first, then ship APK.
Phase 89 ✅  Mobile update gate → Play In-App Updates (replaces the app_config versionCode gate) — the Phase 57/71 custom gate compared a server-held `app_config.latest_version_code` against `BuildConfig.VERSION_CODE`; when the DB number was set ahead of the versionCode Play actually serves a device (e.g. a build not yet propagated / in staged rollout / never uploaded), it produced a false "update available" every launch, and tapping Update went to Play which showed **"Open"** (nothing newer) → an unwinnable loop. Root lesson: a hand-maintained server version number inevitably drifts from what Play serves per-device (propagation is gradual — ~95% within 24h, not instant, even at full rollout). **Fix: switch to Google's official Play In-App Updates API (`AppUpdateManager`)**, which asks Play **directly** whether a newer build is available to *this* device — structurally impossible to false-prompt. **New:** `ui/update/InAppUpdater.kt` (wraps `AppUpdateManagerFactory`; `checkForUpdate()` → if `UpdateAvailability.UPDATE_AVAILABLE`, picks **IMMEDIATE** when `updatePriority()>=4` OR `clientVersionStalenessDays()>=7`, else **FLEXIBLE**; registers an `ActivityResultContracts.StartIntentSenderForResult` launcher + an `InstallStateUpdatedListener`; exposes `flexibleDownloaded: State<Boolean>` for a Compose "Restart to install" prompt → `completeUpdate()`; `onResume()` resumes an interrupted IMMEDIATE + surfaces a downloaded FLEXIBLE). **`MainActivity`:** constructs `InAppUpdater(this)` in `onCreate` (launcher must register before STARTED) + calls `checkForUpdate()`; overrides `onResume`/`onDestroy`; removed the old `AppConfigRepository.checkVersion()`/`updateStatus`/`ForceUpdateScreen`/`UpdateAvailableDialog` gate (loading now gates only on `isCheckingSession`); shows the flexible-downloaded `AlertDialog`. **Removed:** `data/repository/AppConfigRepository.kt` (+ `UpdateStatus`) — now dead; `UpdateScreens.kt` trimmed to just `openPlayStore()` (still used by the login-screen manual "Update App" button + `InAppUpdater.openInPlayStore()`; `ForceUpdateScreen`/`UpdateAvailableDialog` deleted). **build.gradle.kts:** `com.google.android.play:app-update:2.1.0` + `app-update-ktx:2.1.0`; `proguard-rules.pro` gains a defensive `-keep class com.google.android.play.core.**`. **Server side untouched & now unused for the gate:** `app_config` table, `MobileAppConfigController`, `AppConfigRepository`, `ApiService.getAppConfig` all remain (harmless; can be retired later). **Testing caveat:** in-app updates ONLY work when installed from Google Play or **Internal App Sharing** — a sideloaded/`adb install`/debug build always reports "no update", so this can't be verified from Android Studio run; use Internal App Sharing. Compile-verified (`gradle :app:compileStannsasfReleaseKotlin` BUILD SUCCESSFUL). `versionCode` stays **22** (Phase 88 bump, not yet released). Android-only; no API/DB deploy needed. Ships in the next APK.
Phase 90 ✅  Teacher class/section announcements (mobile Feature 1) — teachers can post announcements to their class from the app, optionally targeting a single section; parents only see section-targeted ones for their child's section (class-wide reach everyone). **DB:** nullable `section_id INT FK → sections` added to `announcements` (`tenant_tables.sql` + idempotent `Database/announcements_section_migration.sql`; NULL = whole class). **Backend:** School `AnnouncementDto`/`SaveAnnouncementRequest` gain `SectionId` (+ `SectionName`); `AnnouncementsRepository` Create/Update/Get carry it (Get LEFT JOINs `sections`). Parent read: `MobileDataRepository.GetAnnouncements` gained a `sectionId` param filtering `section_id IS NULL OR = @sectionId`; `MobileStudentController.GetAnnouncements` resolves the child's section via the existing `GetStudentSectionId` (read-time, NOT via token — no login-flow change). New teacher endpoints on `MobileTeacherController`: `GET /mobile/teacher/announcements?classId=` + `POST /mobile/teacher/announcements` (scope forced 'Class', section optional) + `TeacherCreateAnnouncementRequest` DTO. **Web:** `AnnouncementsPage.jsx` gains a Section dropdown (loads per class; "All Sections (whole class)"/`allowClear`; shown when scope=Class); scope column shows section. **Android:** `TeacherAnnouncementDto`/`TeacherCreateAnnouncementRequest`, `ApiService`+`TeacherRepository`+`TeacherViewModel` announcement methods, new `TeacherAnnouncementScreen` (section picker → title/body → Post; list of existing), "Announcements" card on `TeacherHomeScreen` (Campaign icon), `MainActivity` `TeacherScreen.Announcement` route. `versionCode` 25→26, `versionName` "2.5"→"2.6". API (MSBuild Release) + school app (vite) + Android (`:app:compileStannsasfReleaseKotlin`) all compile-verified. **Not yet deployed** — run `announcements_section_migration.sql` per tenant DB → deploy API → deploy school app → ship APK.
Phase 91 ✅  Push notifications — FCM (mobile Feature 3) — parents get a push when new content is posted; sent server-side from EVERY creation path so web + mobile both notify. Uses **FCM HTTP v1** (legacy server-key API retired). **DB (MASTER):** `device_push_tokens` (one row per device install; parent OR teacher owns each token; `fcm_token` UNIQUE upsert target) — `master_tables.sql` (table 17) + idempotent `Database/device_push_tokens_migration.sql` (**run ONCE on ascent_master, NOT per tenant**). **Backend:** `DevicePushTokenRepository` (master; upsert parent/teacher token, delete, resolve parent tokens by student-set or whole-school via `parent_children` join); `FcmPushService` (Helpers) — **hand-rolls the v1 OAuth, no SDK** (house style, cf. R2 SigV4): RS256-signed JWT from the service account → Google token endpoint → `POST /v1/projects/{projectId}/messages:send` per token; manual PKCS#8 key parse + `RSACng` SHA256 signing; access token cached ~55m; reads the JSON path from Web.config `Fcm:ServiceAccountPath`; **unconfigured/missing → silent no-op (never breaks a create)**. `PushNotifier` (Helpers) — resolves target parent tokens (class/section → current-year students → parents; or school-wide) and fans out **fire-and-forget on `Task.Run`, capturing dbName/groupId/schoolId on the request thread** (never touches `TenantContext` inside the task — Phase 61 rule). Triggers wired into `AnnouncementsController` (web create), `HomeworkController` (create + `/batch`), `SchoolEventsController` (create), and `MobileTeacherController` teacher-announcement create. Register endpoints: `MobileNotificationsController` (`POST /mobile/notifications/register-token` | `unregister-token`, no auth attribute — reads `MobileContext`/`TeacherContext` populated by the global filter, accepts parent OR teacher). **Secret handling:** service-account JSON moved to `API/AscentSchools.API/App_Data/firebase-service-account.json` (**IIS denies HTTP access to App_Data** so it's never served); added as csproj Content (`PreserveNewest`) so it deploys; **gitignored** (`**/firebase-service-account.json`); `google-services.json` also gitignored. **Android:** `com.google.gms.google-services` plugin (root + app) + `firebase-bom:33.5.1` + `firebase-messaging`; `push/AscentFirebaseMessagingService` (onNewToken re-register if logged-in; onMessageReceived → local notification); channel `ascent_default` (`AscentApp.onCreate` + manifest default-channel meta-data); `POST_NOTIFICATIONS` permission + runtime prompt on Android 13+ (`MainActivity` launcher); `data/repository/PushRepository` (fetch FCM token via `suspendCancellableCoroutine`, no play-services-await dep) called on login success + cold-start refresh, unregister on logout/change-school; `ApiService`+`ApiModels` register/unregister; `versionCode` 26→27, `versionName` "2.6"→"2.7"; defensive proguard keep for `com.google.firebase.**` + `push.**`. See **`FIREBASE_SETUP.md`** (repo root) for the console setup + how to add a new white-label app (new `google-services.json`, same service account). API (MSBuild Release) + Android (compile) verified. **Not yet deployed** — run `device_push_tokens_migration.sql` on master → deploy API (ensure the App_Data JSON uploads) → ship APK. **Testing:** FCM works on any install (unlike Play In-App Updates), so push is testable from Android Studio via a device's FCM token / console *Send test message*. **Play Data safety:** declare FCM token = "Device or other IDs".
Phase 92 ✅  Class-teacher assignment + parent↔teacher messaging (mobile Feature 2) — parents chat with their child's class teacher(s); teachers reply from the app. **Prerequisite — class→teacher mapping (nothing mapped a class to a teacher before; the RBAC "Class Teacher" role binds nothing):** new tenant table `class_teacher_assignments` (academic_year_id, class_id, `section_id NULL = whole class`, user_id, school_id) — `tenant_tables.sql` table 49 + idempotent `Database/class_teacher_assignments_migration.sql`. **TWO filtered unique indexes** (`UQ_cta_section` WHERE section_id IS NOT NULL; `UQ_cta_class` WHERE section_id IS NULL) — a single index would silently allow the same teacher twice class-wide because SQL Server treats NULLs as distinct (decision #58 trap). `ClassTeacherRepository` (+ routing helpers `GetTeachersForClass`, `GetAssignmentsForTeacher`); `ClassTeachersController` `[RoutePrefix("school/class-teachers")]`; web `ClassTeachersTab.jsx` in Master Data (after Sections). Unassign = hard DELETE. **Decisions (user):** multiple teachers per class+section allowed (shared thread, any can reply); no teacher assigned → parent is blocked with a clear message (no school-inbox fallback); the teacher class dropdown for attendance/homework/announcements stays UNRESTRICTED (assignment drives messaging only — deploying can't lock live teachers out). **Messaging DB:** tenant tables 50-52 `message_threads`, `messages`, `message_reports` + idempotent `Database/messaging_migration.sql` (run AFTER the class_teacher migration). **Threads key on `student_unique_id`, NOT `student_id`** — student_id is a fresh IDENTITY each year (decision #54), so keying on it would fork every conversation at promotion; routing is computed LIVE from the child's current-year class+section, so a promoted child's thread follows them to the new class teacher with zero migration. One thread per (child, parent) — a single continuous chat, no subjects; the parent side is scoped to the selected child so it needs no thread list. `parent_id`/`sender_id` point at `ascent_master.parent_accounts` — cross-DB, so **no FK**; `sender_name` is a display snapshot so history survives a staff member leaving. `read_at` = when the OTHER side first read it (any assigned teacher reading marks it read). **Backend:** `MessagingRepository`; `MobileMessagesController` (`mobile/messages`, `[MobileAuth(requireChildContext:true)]` — GET one-call chat view [canMessage/reason/teachers/messages], POST send, read, report, block, unblock); `MobileTeacherMessagesController` (`mobile/teacher/messages`, `[MobileTeacherAuth]` — thread list, GET/POST reply/read/report/block/unblock per `{threadId}`, every op guarded by `TeacherCanAccessThread`); `MessageReportsController` (`school/message-reports`, admin review) gated on the **existing SETTINGS.MANAGE — deliberately no new permission code**, which would need a perms migration + forced re-login (the Phase 50/53 trap). **Push:** `DevicePushTokenRepository.GetTokensForParent`/`GetTokensForTeachers` + `PushNotifier.NotifyTeachers`/`NotifyParent` (fire-and-forget, primitives captured on the request thread — Phase 61). **Play UGC (required for release):** report (message-level → `message_reports`), block (thread-level; only the side that blocked may unblock — a teacher can lift a school block but not a parent's), and admin removal (`messages.status='Removed'` → shown as "This message was removed." both sides) via the new **Settings → Reported Messages** page (`pages/settings/MessageReportsPage.jsx`, `/settings/message-reports`). **Android:** parent `ui/messages/MessagesViewModel`+`MessagesScreen` (8th "Messages" tab on `HomeScreen`; recipient header, bubbles, long-press → report, block/unblock, composer disabled with the server's reason when no teacher is assigned); teacher `TeacherMessagesScreen` (inbox w/ unread count) + `TeacherChatScreen` (reply, overflow block/unblock, long-press report), `TeacherScreen.Messages`/`.Chat(threadId)` routes, "Messages" `ActionCard` on `TeacherHomeScreen` (outside the class picker — the inbox spans all assigned classes). `versionCode` 27→**28**, `versionName` "2.7"→**"2.8"**. **Privacy pages updated** (chakin + stannsasf + holyspiritjm + depaul): new "Messages" + "Push notification token" collect bullets and a "3. Messaging and Reported Content" section — note the **FCM token was never disclosed by Phase 91**, fixed here. API (MSBuild Release) + school app (vite) + Android (`:app:compileAscentReleaseKotlin`, `--rerun-tasks`) all compile-verified. **Not yet deployed** — run `class_teacher_assignments_migration.sql` then `messaging_migration.sql` per tenant DB → deploy API → deploy school app → assign class teachers in Master Data (messaging stays off for any class with none) → ship APK. **Play Data safety: declare "Messages / other in-app messages" (linked to user).** **Not built:** push deep-link — a message notification opens the app, not the thread (`type`/`entityId` ride in the FCM data block but nothing reads them; `AscentFirebaseMessagingService` has no extras plumbing). **Keyboard-overlap fix (during testing):** both chat screens (`MessagesScreen`, `TeacherChatScreen`) got `Modifier.imePadding()` on their root — the app is edge-to-edge (`enableEdgeToEdge()`), where `windowSoftInputMode="adjustResize"` no longer resizes content on its own, so without imePadding the soft keyboard covered the message composer. Material3 `Scaffold` already consumes the nav-bar inset, so imePadding lifts the composer to sit exactly above the keyboard.
Phase 93 ✅  Homework "due date" retired (all layers) — schools didn't use it. Column kept but made NULLABLE and no longer collected or shown. **DB:** `homework.due_date` NOT NULL → NULL (`tenant_tables.sql` + idempotent `Database/homework_due_date_nullable_migration.sql`; the `IX_homework_class_due` index is left as-is, harmless with NULLs). **API — DueDate made `DateTime?` everywhere it's mapped** (this is the load-bearing part: `due_date` now returns NULL and Dapper THROWS mapping NULL → non-nullable `DateTime`): school `HomeworkDto`+`SaveHomeworkRequest.DueDate`, mobile `HomeworkDto.DueDate`, and the private `MobileDataRepository.HomeworkRow.DueDate` (would have crashed the parent homework tab otherwise). `HomeworkController.CreateHomework` drops the "Due date is required" 400; single-create inserts NULL (client omits it); `CreateBatchHomework` now inserts `(DateTime?)null` (was `= assignedDate`). Both homework list reads re-ordered `ORDER BY due_date DESC` → `assigned_date DESC, homework_id DESC` (school `HomeworkRepository.GetHomework` + `MobileDataRepository.GetHomework` — NULLs would otherwise sink new homework to the bottom). **Dashboard:** the due-date-based "Upcoming Homework" card is **repurposed to "Recent Homework"** (user decision) — `DashboardRepository` query now `TOP 7 ... ORDER BY assigned_date DESC` (no `due_date >= today` filter); DTO renamed `UpcomingHomeworkDto`→`RecentHomeworkDto` (`DueDate`→`AssignedDate`), property `UpcomingHomework`→`RecentHomework` (JSON key `recentHomework`); `DashboardPage.jsx` retitled + uses `assignedDate`. **Reports:** both Homework reports hit `GET /school/reports/homework-statement`, which **filtered AND grouped by due_date** — re-based on `assigned_date` (`ReportsRepository.GetHomeworkStatement` WHERE/ORDER by assigned_date; `HomeworkStatementRowDto.DueDate` removed); `HomeworkStatementReport.jsx` groups by assignedDate + "Due:"→"Date:" labels + "Due From/To"→"From/To" pickers + Due Date dropped from CSV; `SubjectHomeworkReport.jsx` Due Date column removed from table+PDF(+columnStyles reindexed)+CSV, picker relabeled. **Teacher mobile app (user decision — remove too):** `MobileTeacherController.CreateHomework` drops the dueDate validation + sends `DueDate=null`; `TeacherCreateHomeworkRequest` loses `DueDate` (both C# DTO + Kotlin `ApiModels`); Android `TeacherHomeworkScreen` create sheet loses the date-picker field + dialog (and the now-dead `Modifier.clickable` helper / date imports), `HomeworkCard` no longer shows "Due:"; `TeacherViewModel.createHomework(classId,title,desc)` drops the `dueDate` param+validation; `TeacherHomeworkDto.dueDate` → `String?`. **Parent mobile app:** `HomeworkScreen` drops the "Due: …" line (`HomeworkDto.dueDate` already `String?`). **Web add-homework form (`HomeworkPage.jsx`):** Due Date DatePicker removed (Assigned Date now full-width), openCreate/openEdit/handleSave no longer set/send `dueDate`, Due Date table column removed, `Tag` import dropped. `versionCode` stays **29** (unreleased — rides with Phase 92 messaging + the IME fix). API (MSBuild Release) + school app (vite) + Android (`:app:compileDemoDebugKotlin`) all compile-verified. **Deploy:** run `homework_due_date_nullable_migration.sql` per tenant DB → deploy API → deploy school app → ship APK. Existing homework rows keep their historical due_date; only new rows are NULL.
Phase 94 ✅  Homework web list — server-side pagination + assigned_date indexes (unbounded-growth fix) — the admin `GET /school/homework` returned EVERY non-cancelled homework row for the school (only the AntD table paged client-side), so the payload grew without limit as years of homework accumulated. (Mobile was already fine — `MobileDataRepository.GetHomework` is `SELECT TOP (@count)` with count=20.) **Backend:** new `HomeworkRepository.GetHomeworkPaged(dbName, schoolId, classId, sectionId, assignedDate, page, pageSize)` → `(IEnumerable<HomeworkDto> Items, int Total)`; two Dapper calls — `ExecuteScalar<int>` COUNT + a `Query<HomeworkDto>` with `OFFSET @skip ROWS FETCH NEXT @take ROWS ONLY` (page clamped ≥1, pageSize clamped 1..200). **Deliberately NOT `QueryMultiple`** — Dapper's `GridReader` implements `IAsyncDisposable`, which on .NET Framework 4.8 makes a `using` block fail to compile (`CS0012 Microsoft.Bcl.AsyncInterfaces` not referenced); two separate queries avoid the GridReader entirely (COUNT is cheap with the new index). `HomeworkController.GetHomework` now takes `[FromUri] classId, sectionId, assignedDate, page=1, pageSize=20` and returns `{ items, total, page, pageSize }` (shape change — was a bare list). The old `HomeworkRepository.GetHomework(dbName, schoolId, classId)` is KEPT unchanged for `MobileTeacherController` (different endpoint, not paged). **Optional sectionId/assignedDate filters** were added so `DailyHomeworkPage` (the other consumer of `/school/homework`) can fetch one class+section+date precisely instead of pulling all history and filtering client-side — it now calls `?classId=&sectionId=&assignedDate=&pageSize=200` and reads `data.items`. **Frontend `HomeworkPage.jsx`:** added `page`/`pageSize`/`total` state; `loadHomework(classId, p, ps)` reads `data.items`+`data.total`; Table pagination is now controlled (`current/pageSize/total/showSizeChanger/showTotal/onChange`); class-filter change → page 1; create → jump to page 1 (newest); edit/delete → stay on current page. **DB indexes (Phase 94):** dropped obsolete `IX_homework_class_due` (indexed the retired due_date) and added `IX_homework_school_assigned (school_id, assigned_date DESC, homework_id DESC)` for the paged web list + `IX_homework_class_assigned (class_id, school_id, status, assigned_date DESC)` for the mobile feed — both match the `ORDER BY assigned_date DESC` so the top-N is a seek, not a scan+sort. `tenant_tables.sql` updated + idempotent `Database/homework_index_migration.sql`. API (MSBuild Release) + school app (vite) compile-verified; no mobile change. **Deploy:** run `homework_index_migration.sql` per tenant DB → deploy API → deploy school app. (Ships alongside Phase 93.)
Phase 96 ✅  Mobile "re-asks OTP every day" — REAL fix (server-only) + stable refresh token + 15-day mobile access token — the daily OTP re-login survived Phase 80 (365-day refresh) AND Phase 88 (app-held `X-Refresh-Token` header) because the true cause was elsewhere. **Root cause:** `JwtAuthenticationFilter` is a **global `IAuthenticationFilter`** that runs on EVERY request incl. `[AllowAnonymous]` ones; when a Bearer token was present but **expired**, it set `context.ErrorResult = UnauthorizedResult(...)` → **401 short-circuited the request before the action ran**. On cold start the Android app's main OkHttp interceptor **always** attaches `Authorization: Bearer <access token>`, and by morning that 8h access token is expired — so `silentRefresh()` → `parent/refresh` carried the expired token → the global filter 401'd it → `ParentRefresh` never read the valid `X-Refresh-Token` → app `tokenStore.clear()` → OTP screen, **every day**. Why it hid: **login** works (no token yet after clear → no header), **mid-session refresh** works (uses the separate `refreshClient` with NO interceptor → no stale token); only the cold-start refresh carried the expired token. **Fix (JwtAuthenticationFilter.cs):** an invalid/expired token is now treated like "no token" — just `return;` WITHOUT setting `ErrorResult` (both the `principal == null` and `claims == null` branches). Rejection is the auth attribute's job — `[SchoolAuth]`/`[MobileAuth]`/etc. still 401 when their context is unset, so protected endpoints are unchanged; `[AllowAnonymous]` endpoints (refresh, branding, login, webhook, `/version`) now run even if a stale Bearer is attached. **Rule: an authentication filter must NEVER set ErrorResult for a malformed/expired token — it runs on AllowAnonymous too.** **Retroactive:** server-only, NO new APK — every build-29 phone stays logged in on its next open (its stored 365-day refresh token is still valid; the refresh is simply no longer blocked → no re-OTP even once). **Hardening shipped in the same deploy (also server-only):** (1) **Stable mobile refresh token** — `MobileAuthController.ParentRefresh` + `MobileTeacherAuthController.Refresh` no longer ROTATE (revoke+recreate) the refresh token on each use; they call the new `ExtendRefreshToken(tokenId, expiry)` (added to `MobileParentAuthRepository` + `SchoolAuthRepository`) to slide `expires_at` forward (sliding 365-day window) and return the SAME token — removes the "device failed to persist the rotated token → logout" failure class entirely. The web school/control refresh endpoints STILL rotate (different endpoints, unchanged). (2) **Mobile access token 8h → 15 days** — new `Jwt:MobileAccessTokenMins` (Web.config `21600`; `JwtHelper.MobileAccessMins` **code-default 21600** so it works even if Web.config isn't re-uploaded) used ONLY by the 3 mobile token generators (`GenerateMobileParentToken`/`GenerateMobileStudentToken`/`GenerateMobileTeacherToken` → `BuildToken(claims, MobileAccessMins)`); web/control tokens still use `Jwt:AccessTokenMins` (480/8h) so staff/admin web sessions stay short + revocable. **Tradeoff (accepted):** an access token is a self-contained JWT and CANNOT be revoked before expiry, so a lost phone stays valid up to 15 days (the refresh token IS revocable via device-binding on next login). API built (MSBuild Release, exit 0) + deployed twice via full `deploy-api.ps1`; verified `GET /version` fresh each time. Files touched: `Filters/JwtAuthenticationFilter.cs`, `Helpers/JwtHelper.cs`, `Web.config`, `Controllers/Mobile/MobileAuthController.cs` + `MobileTeacherAuthController.cs`, `Repositories/Mobile/MobileParentAuthRepository.cs` + `Repositories/School/SchoolAuthRepository.cs`. No Android/APK change for any of it.
Phase 98 ✅  Fee payment date showed the PREVIOUS day on mobile — server-timezone fix (IST) + created_at skew — parents reported that a payment made from the Android app was stamped with yesterday's date. **Root cause:** the production server runs in **US Eastern (UTC-4/-5)** (see Phase 83) while schools are in India (UTC+5:30, ~9.5–10.5 h ahead), and the mobile order path stamped the date with **`DateTime.Today` = server-local Eastern** (`MobileFeesController.CreatePaymentOrder`, was line 118). So any payment made in India between midnight and ~9:30–10:30 AM IST landed on the *previous* calendar day (evening IST payments were fine — the server had rolled over by then), which is exactly the "sometimes previous date" symptom. The school web app was NEVER affected: its manual/online collection sends the browser's IST date via `FeeCollectionBase.jsx:213` (`paymentDate` = `dayjs()`), so only the **server-stamped** paths were wrong. **Fix (all server-side; the mobile app just displays what the API returns — no APK/web rebuild):** new **`Helpers/TimeHelper.cs`** (`IstNow()`/`IstToday()` via `TimeZoneInfo.FindSystemTimeZoneById("India Standard Time")` — the server is Windows/IIS so that ID resolves; registered in `AscentSchools.API.csproj` per the old-style-csproj rule [[api-csproj-manual-registration]]). Replaced the four **server-stamped** date sources: (1) `MobileFeesController` order create `DateTime.Today` → `TimeHelper.IstToday()` *(the actual cause)*; (2) `MobileFeesController` verify fallback `?? DateTime.UtcNow` → `?? IstToday()`; (3) `FeeController.ReconcileOrderToReceipt` fallback `?? DateTime.UtcNow` → `?? IstToday()` (covers webhook/reconcile of mobile-originated orders); (4) `FeeRepository.CollectFee` INSERT — the SQL `payment_date` fallback `CAST(GETDATE() AS DATE)` → `CAST(SYSDATETIMEOFFSET() AT TIME ZONE 'India Standard Time' AS DATE)`. **created_at skew (also fixed, same root cause):** `fee_receipts.created_at` relied on the table's server-local `GETDATE()` default (skews the dashboard "Recent Receipts" tiebreak, the EOD-sync `?createdAfter`, and the "Created after" receipt filter by ~10 h) — the `CollectFee` INSERT now sets `created_at` **explicitly** to `CAST(SYSDATETIMEOFFSET() AT TIME ZONE 'India Standard Time' AS DATETIME)` so it's correct on deploy for every mobile/online/manual receipt. **`SYSDATETIMEOFFSET()` carries the server's real offset**, so `AT TIME ZONE` is correct regardless of the box's timezone; needs SQL Server 2016+ (already required by temporal `students`, Phase 79). **SQL (user runs — [[user-handles-all-sql-execution]]):** `tenant_tables.sql` `fee_receipts` `payment_date` + `created_at` DEFAULTs changed to the IST expression (new tenant DBs); new idempotent **`Database/fee_receipts_ist_default_migration.sql`** drops the two auto-named default constraints and re-adds IST ones on existing tenant DBs (safety net for the DB-default fallback + legacy `BulkImportReceipts`, which still relies on the default — the main fee path is already fixed in code, so the migration is non-blocking). **API built (MSBuild Release, exit 0) + deployed** (full `deploy-api.ps1`, 35 files); verified `GET /version` → fresh `buildDate 2026-07-25 05:04 UTC`. **Rule: any server-STAMPED business date (payment_date, created_at, etc.) must be computed in IST via `TimeHelper.IstToday()`/`IstNow()` (C#) or `SYSDATETIMEOFFSET() AT TIME ZONE 'India Standard Time'` (SQL) — never `DateTime.Today`/`DateTime.Now`/`GETDATE()`, because the server runs US Eastern. Client-sent dates (browser `dayjs()`) are already IST and need no conversion.** **NOT done (forward-looking fix only):** existing receipts keep their wrong date; no back-correction script was run (some rows are legitimately back-dated/legacy-imported, so a blanket shift is unsafe — would need a targeted rule like `source='webapp'` online receipts where `payment_date < IST(created_at)`).
Phase 97 ✅  Mobile daily OTP re-login — the ACTUAL client-side cause (cold-start refresh wiped the session) + plain-prefs session storage — Phases 80 (365-day refresh), 88 (app-held `X-Refresh-Token`) and 96 (auth-filter fix + 15-day access token) all made the *token* last longer, but the **client threw the token away before the server ever got a say**, so users kept landing on the OTP mobile-number screen after a single night. **Root cause (`MainActivity.kt`):** the cold-start `LaunchedEffect(Unit)` awaited `silentRefresh()` behind a blocking `CircularProgressIndicator` and called `tokenStore.clear()` inside a single `.onFailure { }`. Because `silentRefresh()` is wrapped in `runCatching`, **every** failure collapsed into that one branch — no network yet (the common case: the phone has just woken from Doze and Wi-Fi hasn't reassociated when `LaunchedEffect` fires on process start), DNS failure, socket timeout, a 502 while IIS recycles the app pool, an unparseable body — all indistinguishable from "your refresh token is genuinely revoked". A perfectly valid 365-day session was deleted by a transient hiccup, **every morning**. `MainActivity.kt:146` was the ONLY involuntary logout in the app (all 7 other `tokenStore.clear()` call sites are deliberate user actions). Trivially reproducible: **airplane mode → open app → OTP screen**. Compounding: the blocking spinner sat behind a 75s `callTimeout`, so on a weak signal the user waited >1 min and *then* got logged out. **Fix — "local-first session" (chosen over making the refresh smarter):** the cold-start refresh is **deleted**, not repaired. Opening the app now does **zero blocking auth work** — no spinner, no network call, straight to home. Safe because nothing there was load-bearing: the stored access token **already carries child context** (`selectChild` → `saveSession` writes the child-context token), it is long-lived, and mid-session expiry is already handled by the OkHttp `Authenticator` (Phase 65), which retries and never clears state. Replaced by two narrow behaviours: (1) an **opportunistic** background refresh after the UI is up — new `AuthRepository.refreshSessionQuietly()`, fire-and-forget, purely to slide the server's 365-day window; it **rolls back to the previous token** if the post-refresh `selectChild` fails (the refresh endpoint returns a parent token WITHOUT child context, and committing a child-less token would break every data screen with "Please select a child first"); (2) **the one and only automatic logout** — `RetrofitClient.sessionExpired: MutableStateFlow<Boolean>`, raised ONLY when a live API call 401s **and** the follow-up refresh is explicitly rejected **401/403** (two independent rejections, so a flaky network can't trip it); `MainActivity` collects it and clears the session. Without (2) a genuinely dead session would strand the user in a working shell with every screen failing and no route back to login. **Server:** `Jwt:MobileAccessTokenMins` 15 days → **180 days** (`259200`; `JwtHelper.MobileAccessMins` **code-default also raised to 259200** so it holds even if Web.config isn't re-uploaded). With no cold-start refresh, this lifetime alone is what keeps a parent signed in between opens. Web/control still `Jwt:AccessTokenMins` (480/8h). **Tradeoff (accepted, same shape as Phase 96):** a lost phone stays valid up to 180 days; the refresh token remains revocable and device binding invalidates the old device on the next login. **Storage (`TokenStore.kt`) — secondary cause:** session data moved from `EncryptedSharedPreferences` to **app-private plain `SharedPreferences`** (`ascent_session_prefs`). `androidx.security-crypto` is **deprecated by Google**, and its Keystore-backed keyset is known to fail on some OEM devices and after a backup / device-to-device transfer; when it failed, the old code fell back to a **DIFFERENT, EMPTY prefs file** (`ascent_fallback_prefs`) which reads as "never logged in" — a silent logout with no error surfaced. App-private storage is unreadable by other apps or without root, and is where **Firebase Auth keeps its own tokens**. **Two one-time migrations** run in `init` so this build doesn't itself log everyone out: from the encrypted store, then (only if no access token was recovered) from the old fallback file — which catches exactly the users the bug hit hardest. Guarded by a `migrated_from_encrypted` flag that **`clear()` deliberately re-writes** — otherwise a logout would be undone by a re-import of the stale session on the next launch. `security-crypto` dependency is KEPT (needed for the migration read). **`AndroidManifest.xml`: `allowBackup` true → `false`** — a restored session belongs to a different device, which device binding rejects anyway, and restoring prefs whose Keystore key doesn't exist on the new device is a known cause of silent logouts (cost: a parent moving phones logs in once with OTP). `versionCode` 31→**32**, `versionName` "3.1"→**"3.2"**. API (MSBuild Release, exit 0) + Android (`:app:compileDemoDebugKotlin`, `:app:compileStannsasfReleaseKotlin`, `:app:compileAscentReleaseKotlin`, all exit 0) compile-verified. No DB migration. **Deploy order matters: API FIRST** (full `deploy-api.ps1` — `JwtHelper.cs` changed, not just Web.config), **then the APK**, so build 32's first opportunistic refresh mints a 180-day token. **Expect one final OTP login** on phones whose installed build predates versionCode 22 (Phase 88 never shipped, so they hold no refresh token) — after that the session is durable. **NOT built (deliberately out of the chosen scope, still open):** remembering the last mobile number across `clear()` (a re-login currently means retyping 10 digits) and recording the logout reason on the login screen for diagnosis. **Rule: a mobile app must never delete its own credentials because a network call failed — only an explicit server rejection (401/403) may end a session.**
Phase 99 ✅  AscentSyncTool — academic-year filter on legacy exports + version stamp — the two legacy-source tabs (Export Students, Export Receipts) now filter by academic year in addition to the date range; the Download Receipts tab (new-app API by `created_at`) is untouched. **`Data/LegacyDb.cs`:** new `GetAcademicYears()` → `SELECT DISTINCT AcademicYear FROM SAS_AcdYear WHERE AcademicYear IS NOT NULL AND LTRIM(RTRIM(AcademicYear)) <> '' ORDER BY AcademicYear DESC` (string DESC gives latest for the "YYYY-YY" format, matching how the app resolves "latest" elsewhere); `GetReceipts`/`GetStudents` gained an optional `string academicYear = null` param → `AND (@acdYear IS NULL OR AcdYear = @acdYear)` (receipts, col **`AcdYear`**) / `AND (@acdYear IS NULL OR StuAcdYear = @acdYear)` (students, col **`StuAcdYear`**); the `@acdYear IS NULL` guard is defensive (empty/no year → date-range only). **`SyncTab.cs`:** optional `Academic Year:` `ComboBox` (`DropDownStyle=DropDownList`) gated by a new `ShowAcademicYearFilter` bool (hidden by default); `SelectedAcademicYear` property (null when hidden/empty) + `SetAcademicYears(IEnumerable<string>)` which selects the **first (latest)** item; combo disabled while busy. **`MainForm.cs`:** `public const string Version = "1.1"` is the single source of truth (no `AssemblyInfo.cs` is compiled in this project) — shown in the title bar AND as a large bold navy header `Label` (`Segoe UI 18pt Bold`, docked Top above the `TabControl`); enables `ShowAcademicYearFilter` on `_tabStudents` + `_tabReceipts`; a `Load` handler calls `GetAcademicYears()` once and populates both legacy tabs (latest pre-selected), wrapped in try/catch so a legacy-DB failure shows a status message instead of crashing startup; both legacy `OnShow` callbacks pass `_tab.SelectedAcademicYear` into the query (no `OnShow` signature change). Needed `using System.Drawing;` in `MainForm.cs` for `Font`/`Color`/`FontStyle`/`ContentAlignment`. Built clean (MSBuild Debug, VS18). No API/DB change; redistribute the rebuilt `AscentSyncTool.exe`.
Phase 102 ✅  CHAK IN launcher label fix — generic app showed "Ascent Schools" text under the CHAK IN icon — the on-device launcher label is `android:label="@string/app_name"` (AndroidManifest, on `<application>`), and each flavor sets `app_name` via `resValue("string","app_name",…)` in `app/build.gradle.kts` — which **overrides** the per-flavor `src/{flavor}/res/values/strings.xml` (those "Ascent Schools" entries are dead/leftover) and `src/main`. The generic **`ascent`** flavor (the published CHAK IN app, `applicationId=in.educare.app`) had its `resValue` app_name still set to **"Ascent Schools"**; the CHAK IN **icon** was updated (Phase 48) but this **label string** never was. **The Play Store *listing* name ("CHAK IN", Phase 49) does NOT change the on-device launcher label — only `app_name` does.** Fix: `ascent` flavor `resValue("string","app_name",…)` "Ascent Schools" → **"CHAK IN"** (one line). Baked per-school flavors unaffected (each has its own resValue). In-app screens unaffected (the generic app shows the *selected* school's API branding name; app_name is just the launcher label + pre-selection fallback). Takes effect on the next build/install of the `ascent` flavor. `versionCode` 34→**35**, `versionName` "3.4"→**"3.5"** (rides with the tile-dashboard build). Android-only, no API/DB change. **Rule: the launcher label is `resValue app_name` per flavor in build.gradle.kts — the Play listing name and the flavor `strings.xml` do NOT control it.**
Phase 101 ✅  Mobile tile dashboard — switchable Tiles/Tabs navigation (Android; reference-styled) — clients wanted a launcher-style tile home instead of the bottom tabs; added a **switchable** view (default stays Tabs) modeled on a client reference screenshot (colored header band + logo, red "News" ticker, 3-col icon grid on a branding-tinted background, footer). **Reuse (key point):** the navigation ENGINE is untouched — the 8 parent destinations live in one `features` list (single source of truth) consumed by BOTH the bottom `NavigationBar` and the tile grid, and the feature→screen wiring is one shared `FeatureContent(index)` `when` in `HomeScreen.kt`; only the *presentation* differs. **Toggle:** a "Tile view / Tab view" item in the existing ⋮ overflow menu (parent + teacher), persisted in new `TokenStore.tilesView` (a boolean, default false = Tabs) which `clear()` **preserves** across logout (like deviceId/school). Tiles mode has no bottom bar: tapping a tile opens that feature full-screen with a back arrow (`BackHandler` returns to the grid). **Reference layout (parent, `HomeScreen.kt`):** (1) **Logo band** — full-width band in the school's branding **primary color** carrying the logo (falls back to `getApplicationIcon()`) + name — "reuse the logo on a colored band", no photo (user decision); (2) **News ticker** — a red "News" chip + `Modifier.basicMarquee()` auto-scroll of the latest announcement titles (from the already-loaded `AnnouncementsViewModel`), tap → opens Notices; (3) **3-column `IconTile` grid** — each tile a rounded gradient square + white glyph with the label below, on a page tint **derived from the branding primary color** (user decision), with **live badges** (Fees "₹X due" red, Homework/Notices/Events counts) computed from the collected VM states — no extra network calls; (4) **Footer** "Powered by Ascent Info Solutions" pinned at the bottom. The app bar also adopts the branding color in tiles mode. **Teacher home** (`TeacherHomeScreen.kt`) got the same toggle + `IconTile` style (2×2, gated on class/section), no hero/ticker. **Branding color plumbing (Android-only — `/branding` already returns `PrimaryColor`, the mobile DTO just didn't map it):** `BrandingDto.primaryColor` added, `AuthRepository.loadBranding` stores `tokenStore.brandingPrimaryColor` (preserved across logout), `HomeScreen` calls `loadBranding()` on mount (works for baked flavors too — `/branding` resolves from the school code) and holds color/logo in state so the UI recomposes when it arrives; `parseBrandColor(hex, fallback=NavyBlue)` in `IconTile.kt` converts the hex. **Removed (superseded):** the earlier 2-col image-card `FeatureTile.kt` and its `TILE_IMAGES.md` unDraw-drop-in guide — the new tiles are solid gradient squares + Material icons, not background illustrations. `basicMarquee` compiles clean on Compose BOM 2024.08 (foundation 1.6). `versionCode` 32→**34**, `versionName` "3.2"→**"3.4"** (33 was an interim unreleased build of the first card-tile version). **Android-only — no API/DB/backend change.** Compile-verified (`:app:compileDemoDebugKotlin`, fresh run, BUILD SUCCESSFUL). Not yet built into an APK / released. Notes: Fees badge reflects only the **School** fee category (what the Fees screen loads by default); if a school has no branding primary color set, band/tint fall back to the app navy.
Phase 100 ✅  Push notification school-scoping fix — a parent got a demo-school announcement on the "CHAK IN" app that was logged into a DIFFERENT school (server-only) — with all flavor apps installed and logged in as the same parent, a single announcement fanned out to EVERY app install (observed 5 notifications on one device; 1 for a single-app school). **Root cause:** `DevicePushTokenRepository` resolved parent push targets by joining `device_push_tokens` to `parent_children` **on `parent_id` only**, ignoring the token's own `school_id`/`group_id`/`db_name`. Because `parent_accounts` live in the **master DB and are shared across all schools** (one master `parent_id` per mobile number, linked to children in many schools via `parent_children`), a demo-school announcement reached every app install/token that same master parent had ever registered — including the generic CHAK IN app currently viewing another school, and every baked per-school flavor on the device. Registration was already correct (`MobileNotificationsController` stores the currently-selected school's `GroupId/SchoolId/DbName` on the token via `UpsertParentToken`, refreshed after child-selection on login/cold-start/child-switch, deleted on logout/change-school); only the **resolution queries** didn't use it. **Fix:** scoped all three parent-token resolvers to the target school — `GetSchoolParentTokens` and `GetParentTokensForStudents` (gained a `schoolId` param) now add `AND dpt.db_name=@dbName AND dpt.group_id=@groupId AND dpt.school_id=@schoolId` to the token join, and `GetTokensForParent` (teacher→parent message reply — same latent leak) gained `dbName`/`schoolId` params + the same filter (`PushNotifier.NotifyClass`/`NotifyParent` + `MobileTeacherMessagesController` pass them). Now a device only receives pushes for the school its app is currently logged into. **Inherent (not a bug):** one CHAK IN install = one FCM token = one *current* school, so a parent with children in two schools using a single generic-app install only gets the currently-selected school's pushes until they switch children. **Diagnostic that confirmed it was correct behavior:** tapping the notification opened the *baked demo app* (each flavor's `FirebaseMessagingService` posts its own notification and Android routes the tap to the posting app), proving the push went to demo's own app, not CHAK IN. **Server-only** — built (MSBuild Release, exit 0) + deployed (full `deploy-api.ps1`, 35 files); `GET /version` → fresh `buildDate 2026-07-25 08:44 UTC`. No APK/DB change; existing token rows self-correct on the next registration (login/cold-start). Files: `Repositories/Mobile/DevicePushTokenRepository.cs`, `Helpers/PushNotifier.cs`, `Controllers/Mobile/MobileTeacherMessagesController.cs`.
Phase 95 ✅  Deployed Phases 90–94 to production + reusable Play Store release guide — **API** (full `deploy-api.ps1`), **all 5 school web apps** (`deploy-school.ps1 -School all`), and the **4 per-school privacy-policy pages** are live. Verified: `GET /version` returns a fresh build; new endpoints (`school/class-teachers`, `school/message-reports`, `mobile/messages`, `mobile/teacher/messages`) return **401** (registered, not 404); each privacy page serves the new "Messaging and Reported Content" section + FCM-token disclosure. **Deploy-time fix (would have silently killed prod push):** the API publish failed on a stale `<Content Include="ascent-schools-firebase-adminsdk-fbsvc-….json" />` in `AscentSchools.API.csproj` — the raw Firebase download name at the PROJECT ROOT, which no longer existed (the file had been moved to `App_Data/firebase-service-account.json` but the csproj reference wasn't updated, AND the real file wasn't referenced as Content at all → even a good publish wouldn't have deployed the secret → `FcmPushService.IsConfigured=false` in prod → push silently no-ops). Fixed the reference to `App_Data\firebase-service-account.json`; publish now bundles the secret (IIS blocks HTTP access to App_Data). Combined with the FCM IAM role grant (decision #75), production push is functional. **New deploy script:** `deploy/deploy-privacy.ps1` — uploads the static per-school `{folder}/privacy-policy.html` to `/httpdocs/{folder}/` (docroot derived from `api.remotePath` minus `/api`); reuses `_common.ps1` FTP helpers; folders `chakin`/`stannsasf`/`holyspiritjm`/`depaul`; `-IncludeAccountDeletion` to also push those. **The per-school privacy pages ARE the live ones** (confirmed by user), served at `https://edu-care.in/{chakin|stannsasf|holyspiritjm|depaul}/privacy-policy.html` — NOT the root `privacy-policy.html`. **STILL PENDING — mobile:** the Android APK (messaging + due-date removal + push, `versionCode` 29) has NOT been built/submitted to Play. **Play Store release guide (REUSE for every future Play release — new schools, next CHAK IN version):** an interactive step-by-step artifact — build/sign AABs per flavor, Data safety (FCM token → "Device or other IDs"; Messages → "Other in-app messages"; both **Collected, not Shared**, App functionality, linked-to-user), the full Content-rating questionnaire **question→answer key** (educational=Yes, users-interact=Yes, UGC-primary=No, nudity/violence=No, block/report/chat-moderation=Yes, invited-friends-only=Yes), App access reviewer login (**parent 9999999999 / OTP 123456**, CHAK IN school code **1000**; the bypass lives in `MobileAuthController` `ReviewMobile`/`ReviewOtp`), UGC moderation attestation, submit + staged rollout. **Guide URL:** https://claude.ai/code/artifact/6769f494-9ad8-42a3-be28-6c12066454a2 . **Reviewer-test setup reminder:** for messaging to be testable, the demo school needs a student with `father_mobile=9999999999` AND a class teacher assigned to that child's class (else the Messages tab shows "Messaging isn't set up"). Only the mobile app is on Play — the school web app needs no reviewer account.
Phase 91b ✅  Phase 91 push never actually built — csproj registration gap (found 2026-07-17) — the whole API solution failed to compile: `PushNotifier`/`FcmPushService`/`MobileNotificationsController` existed on disk with correct namespaces but were **never added to `AscentSchools.API.csproj`**. The API projects are **old-style (non-SDK) csproj**, so a new `.cs` file is invisible to the build unless listed as `<Compile Include>`. Phase 91's Core DTO (`PushDtos.cs`) and Data repo (`DevicePushTokenRepository.cs`) *were* registered, which is exactly why it looked finished and was recorded as "compile-verified". Had the csproj not been fixed, push would have deployed as dead code (and nothing else would have built either). Fixed by adding the three `<Compile Include>` lines. **Rule: after creating ANY new API .cs file, add it to the csproj and build `API/AscentSchools.sln` (NOT the repo root — there is no solution there) with MSBuild Release, and confirm exit code 0.**
Phase 86 ✅  Self-service Change Password (school app) — before this, only an admin (`USER_MGMT.MANAGE`) could reset a user's password via User Management; regular staff had no way to change their own. Added a self-service flow for **any authenticated school user**. **Backend:** `POST /school/auth/change-password` (`SchoolAuthController`, plain `ApiController` — action tagged `[SchoolAuth]` so `TenantContext.Current` is populated/non-null) → verifies `CurrentPassword` against the stored hash (`SchoolAuthRepository.GetById().PasswordHash` vs `JwtHelper.HashRefreshToken(current)`), rejects with 400 if wrong, enforces min 6 chars, then `SchoolAuthRepository.UpdatePassword(userId, newHash)` sets `password_hash` **and revokes all active refresh tokens** (forces re-login everywhere). `ChangePasswordRequest` DTO added to `Core/DTOs/Auth/LoginRequest.cs`. **Frontend:** `AppLayout` header user dropdown gains a **"Change Password"** item (above Logout) → a Modal (current / new / confirm, with match validation) → `POST /school/auth/change-password` → on success shows a message and **logs the user out** (`logout()` + navigate to `/login`) since the tokens were revoked. API + all 5 school apps deployed. (Note: like the admin reset-password endpoint, this is protected by `[SchoolAuth]` = any authenticated user — which is correct here since it only changes the caller's own password from `TenantContext.UserId`.)
Phase 85 ✅  Cloudflare R2 storage — Phase B (3 upload buttons on homework/announcements/events + mobile) — replaces the manual youtube/image/attachment URL text fields with **up to 3 R2 file uploads** per item; parents see them in the app. **DB:** new tenant table `media_uploads` (generic: entity_type [homework|announcement|event], entity_id, file_name, file_url [R2 public URL], file_type [image|doc|audio|video], file_size_kb, school_id) — `tenant_tables.sql` (table 48) + `Database/media_uploads_migration.sql` (also **relaxes `school_events.media_url` to NULL** — it now holds only the optional YouTube URL; R2 uploads live in `media_uploads`). **Backend:** `MediaUploadsRepository` (Insert/GetByEntity/GetForEntities/Delete/CountByEntity/GetCurrentAcademicYear); `UploadsController.Presign` extended for `homework` (key `homeworks/{academicYear}/{id}/…` — year folders → delete old years to save space), `announcement` (`announcements/{id}/…`), `event` (`events/{id}/…`), with a longer presign window (1 h) for `video/*` (1 GB); new `POST /school/media/attach`, `GET /school/media?entityType=&entityId=`, `DELETE /school/media/{id}`; `SchoolEventsController` no longer requires `MediaUrl` (optional YouTube); mobile `HomeworkDto`/`AnnouncementDto`/`SchoolEventDto` gain a `Media` list, populated in `MobileDataRepository` (`LoadMedia` per entity). **School app:** reusable `components/MediaUploader.jsx` (3 upload buttons → presign → direct R2 PUT → `media/attach`; lists with delete; size/type caps enforced client-side — image 1 MB, PDF 2 MB, audio 10 MB, video 1 GB); wired into `HomeworkPage`/`AnnouncementsPage`/`EventsPage` (manual URL fields removed; **events keep an optional YouTube URL field** + image/video uploads; the uploader shows in **edit mode**, and after **Create** the modal now **stays open in edit mode** carrying the new id from the `Created(id,…)` response — so you attach files immediately without reopening). **Mobile (needs new APK):** `ApiModels.kt` `MediaUploadDto` + `media` on the 3 DTOs; new `ui/components/MediaAttachments.kt` (images inline via Coil, others as tappable buttons → `ACTION_VIEW`) wired into Homework/Announcements/Events screens; `versionCode` 20→**21**, `versionName` "1.9"→**"2.0"**. **Go-live per school:** run `media_uploads_migration.sql` (before using the new UI — events save with no YouTube URL needs the nullable media_url). API + all 5 school apps deployed; mobile pending APK build.
Phase 84 ✅  Cloudflare R2 storage — Phase A (config page + presigned uploads + student photo→R2) — moving file storage to per-school Cloudflare R2 (S3-compatible). **Approach:** browser uploads **directly to R2 via SigV4 presigned PUT URLs** (no file bytes through our API/IIS — needed for the 1 GB event videos coming in Phase B); our API only signs the URL and records the resulting public URL. **DB:** new tenant table `r2_configs` (per school, `UNIQUE(school_id)`: account_id, access_key_id, secret_access_key [never returned], bucket_name, public_base_url, is_enabled) — in `tenant_tables.sql` (table 47) + idempotent `Database/r2_config_migration.sql`. **Backend:** `R2ConfigRepository` (Get [safe, `hasSecretKey` flag] / GetInternal [with secret] / Upsert [blank secret keeps existing]); `R2Service` (manual SigV4 presign — `PresignPut` host-only signed header, UNSIGNED-PAYLOAD, region `auto`; `PublicUrl`; **no AWS SDK dependency**); `UploadsController` `GET/PUT /school/settings/r2` + `POST /school/uploads/presign` (Phase A handles `purpose=student-photo` → key `student-images/{AdmissionNo}_{academicYear}.{ext}`; homework/announcement/event added in Phase B); `StudentsController` gains `PUT /school/students/{id}/photo` (JSON `{photoUrl}`) to save the R2 URL after upload. New files registered in the old-style `.csproj`s. **Frontend:** `pages/settings/R2StoragePage.jsx` (Settings → "R2 Storage", `SETTINGS.MANAGE`); `api/r2Upload.js` helper (presign → raw `fetch` PUT to R2 → return public URL; size/type caps: 1 MB images, 2 MB docs, 1 GB video, jpg/png/webp/pdf/mp3/mp4); `StudentFormPage` photo now uploads to R2 (validates ≤1 MB image) and saves via the new PUT; student `photo_path` is now a full R2 URL, so `Avatar` rendering handles absolute vs legacy-relative (`/^https?:\/\//` check) in `StudentFormPage` + `StudentsPage`. **Go-live per school:** run `r2_config_migration.sql`; set up R2 (public access via r2.dev/custom domain, an Object-Read&Write API token, and a CORS rule allowing PUT from the school site); enter creds in the R2 Storage page. Until configured, presign returns 400 and the student still saves (photo skipped with a warning). API + all 5 school apps deployed. **Phase B (next):** 3 upload buttons on homework/announcements/events → `media_uploads` table, remove the manual youtube/image/attachment URL fields (keep YouTube field on events), mobile app renders the attachment list (new APK).
Phase 83 ✅  API version/deploy-date in the school-app footer — the footer already showed UI version + build date (Phase 66); added the **live API build info** next to it so you can tell which API is deployed. **API:** new public `GET /version` (`PublicController`, AllowAnonymous) → `{ version, buildDate }` — `version` = executing assembly version, `buildDate` = last-write time of `~/bin/AscentSchools.API.dll` via `HostingEnvironment.MapPath` (the actual FTP-deploy time; more accurate than the shadow-copied `asm.Location`), formatted `yyyy-MM-dd HH:mm`, best-effort (null on error). **Frontend:** `AppLayout.jsx` fetches `/version` on mount (`useEffect`) and the footer now reads `Powered by … · UI v{__APP_VERSION__} ({__BUILD_DATE__}) · API v{version} ({buildDate})`. API (full `deploy-api.ps1`) + all 5 school apps deployed. **API version is now a real bumpable number:** `AssemblyVersion`/`AssemblyFileVersion` in `API/AscentSchools.API/Properties/AssemblyInfo.cs` set to **`1.1.0.0`** (was default `1.0.0.0`) — `GET /version` reads `AssemblyVersion` via `asm.GetName().Version`. **Bump both lines per API release.** Verified live: `/version` → `{"version":"1.1.0.0","buildDate":"2026-07-06 01:45"}`. (`__APP_VERSION__` UI version is still `1.0.0` from `vite.config` — bump per release; `__BUILD_DATE__` auto-updates each build.) **UTC fix:** `buildDate` was read with `File.GetLastWriteTime` (server LOCAL tz — the box is US Eastern ~UTC-4, so a 07:00 UTC deploy displayed as 03:00) → switched to `GetLastWriteTimeUtc(...) + " UTC"`. The UI `__BUILD_DATE__` (`new Date().toISOString()`) was already UTC — just added a `" UTC"` label so both footer times read unambiguously and match.
Phase 82 ✅  OTP request timeout fix (mobile + API) — Android login sometimes showed "timeout" after entering the mobile number because `/mobile/auth/parent/request-otp` **synchronously calls the smslogin.mobi gateway**, which is sometimes slow; the app's 30s `readTimeout` fired before the server responded (OTP often still sent, late). Coordinated fix so **app timeout > API gateway timeout**: **Android** (`RetrofitClient.kt`, both `okHttpClient` + `refreshClient`) `readTimeout` 30→**60s**, added `writeTimeout` 30s + `callTimeout` 75s (main client); **API** (`SmsHelper.SendSms`) replaced the plain `WebClient` (which hangs up to the 100s default `HttpWebRequest` timeout) with a `TimedWebClient : WebClient` overriding `GetWebRequest` to set `request.Timeout = 45000` (45s) — so the server returns a real result/error within the app's 60s window instead of the app timing out on its own. Applies to both OTP (global account) and SMS Center (per-school account) since both go through `SendSms`. **API deployed** (full `deploy-api.ps1`). **Android change needs an APK rebuild** (Android Studio; no gradlew in repo) + redistribution — compile-safe timeout bump only, no logic change. `app/build.gradle.kts` `versionCode` 19→**20**, `versionName` "1.8"→**"1.9"**.
Phase 81 ✅  Dashboard "Recent Receipts" ordered by payment date — the dashboard's recent-5 receipts (`DashboardRepository`, `SELECT TOP 5 … FROM fee_receipts`) ordered by `created_at DESC` (row-insert time), so back-dated/legacy-imported receipts sorted by when they were entered, not when paid. Changed to `ORDER BY fr.payment_date DESC, fr.created_at DESC` (payment date first, insert time as same-day tie-breaker). API-only (`DashboardPage.jsx` renders in backend order, no client-side sorter). **Deploy gotcha (learned):** `deploy-api.ps1 -SkipBuild` skips the FolderProfile **publish** step and re-uploads whatever is already in `bin/Release/Publish` — a manual `msbuild /t:Build` writes to `bin/Release/`, NOT the publish folder, so `-SkipBuild` uploaded a STALE publish (change didn't reach the server). **Rule: after ANY API source change, run the full `deploy-api.ps1` (no `-SkipBuild`)** — it publishes fresh then uploads. `-SkipBuild` is only for re-uploading an already-published build unchanged. Redeployed full; live. **Follow-up:** the recent-receipts table also gained a **Type** column (payment mode — Cash/Cheque/Online): `DashboardRepository` `LEFT JOIN payment_modes` → `RecentReceiptDto.PaymentMode`; `DashboardPage.jsx` renders it as a Tag between Date and Status. Deployed.
Phase 80 ✅  Mobile long-lived sessions (365-day refresh) — cut SMS re-OTP cost — parents were logged out after 7 days of not opening the app (both the DB refresh token AND the `parentRefreshToken` cookie were 7-day), forcing an SMS OTP re-login (a real cost to the school). Extended the **mobile** refresh window to **365 days** (sliding — every use rolls it forward), while **leaving web apps at 7 days** (control/school use passwords, no SMS cost; long web sessions are a security downgrade). `JwtHelper` gains `MobileRefreshDays` (reads `Jwt:MobileRefreshTokenDays`, **defaults to 365 when the key is absent** — so it works even if Web.config isn't re-uploaded) + `MobileRefreshTokenExpiry()`. Applied ONLY in the mobile controllers: `MobileAuthController` (parent + student — its shared `SetCookie` cookie `Expires` + the 4 `CreateRefreshToken` expiry calls) and `MobileTeacherAuthController` (`SetCookie` + 2 calls). `ControlAuthController`/`SchoolAuthController` untouched (still `RefreshTokenExpiry()`=7d + `AddDays(7)` cookie). `Web.config` adds `Jwt:MobileRefreshTokenDays=365`. **No APK change** — this is server-side; combined with the existing PersistentCookieJar (Phase 34/70) + cold-start silent refresh (Phase 34/71) + Path=/ cookie (Phase 56), a parent on a current APK who reopens the app stays logged in without OTP unless they go a full year without opening it, reinstall, or switch phones. **One-time transition:** existing users keep their old 7-day cookie until their next login; after that single OTP they get the 365-day cookie. API deployed (mobile sessions live at 365d via the code default). Security note: longer refresh window widens the lost-phone exposure, mitigated by device binding (one device/parent), token rotation on each refresh, HttpOnly + SHA-256-hashed-in-DB tokens, and server-side revocability.
Phase 79 ✅  Students audit trail via SQL Server temporal (system-versioned) table — the `audit_logs` tenant table is event-level only (no old/new values) and unused; instead of instrumenting the ~10 student write paths, made `students` a **system-versioned temporal table** so EVERY change (web form, bulk import, sync tool, promote, block/detain, change-section, transport/hostel update) is snapshotted automatically into `dbo.students_history` — **zero application code**. Added hidden `valid_from`/`valid_to` `DATETIME2 GENERATED ALWAYS AS ROW START/END` PERIOD columns + `SYSTEM_VERSIONING = ON (HISTORY_TABLE = dbo.students_history)`. The "who/when" comes from `students.updated_by`/`updated_at` (Phase 78) carried on every history row, plus the system period. **Files:** `tenant_tables.sql` (two `ALTER TABLE students` after the table def — for new groups) + idempotent `Database/students_temporal_migration.sql` (guarded by `sys.periods` / `sys.tables.temporal_type=2`; for existing tenant DBs). **Requires SQL Server 2016+** (any edition). Query history: `SELECT * FROM students FOR SYSTEM_TIME ALL WHERE student_id=@id ORDER BY valid_from`. Period cols are HIDDEN so `SELECT *`/existing queries and the explicit-column INSERT/UPDATE paths are unaffected (GENERATED ALWAYS cols are auto-populated, never in the insert list). Caveats: future `ALTER TABLE students` MUST wrap with `SYSTEM_VERSIONING = OFF` … `ON` (see decision #74); DROP requires it off. **Viewer:** `GET /school/students/{id}/history` (`StudentsController.GetStudentHistory` → `StudentRepository.GetHistory` querying `students FOR SYSTEM_TIME ALL AS s`, newest-first, joined to classes/sections/academic_years for display names) → `StudentHistoryDto` (valid_from/valid_to + created_by/updated_by/updated_at + key snapshot fields); `StudentFormPage.jsx` edit view gained a **"View change history"** button → a Drawer with a table (one row per saved version, newest first, UTC times). **Migrations run + API and all 5 school apps deployed** (Phases 76–79 shipped together in this deploy).
Phase 78 ✅  Student create/update audit — `created_by` on web create + new `updated_by`/`updated_at` — the web "Register Student" form (`StudentRepository.Create`) never wrote `created_by` (only the Promote path did; bulk got it in Phase 77), and there was no update-audit trail at all. **created_by:** `Create(...)` gains a `createdBy` param + `created_by` in the INSERT; `StudentsController.CreateStudent` passes `Tenant.FullName`. **updated_by/updated_at:** NEW `students` columns `updated_by VARCHAR(25) NULL` + `updated_at DATETIME NULL` (added to `tenant_tables.sql` after `created_at`; idempotent migration `Database/students_updated_audit_migration.sql` for existing tenant DBs); `Update(...)` gains an `updatedBy` param and sets `updated_by = @updatedBy, updated_at = GETDATE()`; `StudentsController.UpdateStudent` passes `Tenant.FullName`. **UI:** `GetById` SELECT + `StudentDto` now return `CreatedBy/CreatedAt/UpdatedBy/UpdatedAt`; `StudentFormPage.jsx` shows a read-only audit footer on edit ("Created by X on … · Last updated by Y on …", `dayjs`-formatted). Both bulk-create paths keep the Phase-77 behavior (bulk sets `created_by`; upsert never touches it). API (MSBuild Release) + school app (vite) compile-verified. **Deployed** (`students_updated_audit_migration.sql` run per tenant DB; API + all 5 school apps live).
Phase 77 ✅  Sync tool — extended student fields + created_by='synctool' — the sync tool's Export Students carried only ~20 columns; extended it to cover 25 more legacy→new fields (mappings taken from `AscentMigration.StudentsMigrator`, the authoritative source) plus `created_by`. **New fields** (legacy `SAS_StudentMaster` col → `students` col): `StuJoinTyp`→join_type, `StuFatherOccup`→father_occupation, `StuFatherEmpTyp`→father_employment_type, `StuMotherOccup`→mother_occupation, `StuDOJ`→date_of_joining, `StuNationlity`→nationality, `StuDoorNo`→door_no, `StuAddrArea`→address_area, `StuAddrCity`→address_city, `StuAddrState`→address_state, `StuAddrPerminent`→permanent_address, `StuMailID`→email, `StuAnnualIncome`→annual_income, `StuFamilyChildres`(varchar→int)→family_children_count, `StuDOBStat`→dob_proof_submitted, `StuCasteStat`→caste_cert_submitted, `StuTransportTyp`→transport_type, `AdminDate`→admission_date, `StudentType`→student_type, `BloodGrp`→blood_group, `JoinTerm`→join_term, `StuFirstLang`→first_language, `StuThirdLang`→third_language, `StuUdiseNo`→udise_no (mother_tongue`←MothrLang` was already mapped). **fee_category_id:** resolved server-side to the school's **'General'** category (`SELECT TOP 1 fee_category_id FROM fee_categories WHERE category_name='General'`), **NULL if none** — applied only for sync inserts (gated on `createdBy=='synctool'`), so web CSV import behaviour is unchanged. **created_by:** `BulkStudentImportRequest.CreatedBy` (new) — sync tool sends `"synctool"`; `StudentsController.BulkImport` computes `createdBy = request.CreatedBy ?? Tenant.FullName` and passes it to `StudentRepository.BulkCreate(..., string createdBy)`; INSERT now writes `created_by` (previously never set on the bulk/single-create paths — only Promote did). **Upsert:** per user decision, the UPDATE path now refreshes **all** these fields including enrollment-specific (`fee_category_id`, `join_type`, `date_of_joining`, `admission_date`, `transport_type`, `student_type`, `join_term`) — but **never** `created_by`, and still leaves class/section/roll/status/academic_year untouched. Dates `date_of_joining`/`admission_date` parsed leniently (blank/bad → NULL, row not failed) via a new `TryParseDate` helper. **Files:** AscentSyncTool `LegacyDb.GetStudents` (SELECT), `LegacyStudent` model, `StudentBulkRow`+`BulkStudentImportRequest`(`CreatedBy="synctool"`) in `ApiModels.cs`, `Mappers.ToBulkStudent`; API Core `StudentBulkRow`+`BulkStudentImportRequest.CreatedBy`, `StudentsController.BulkImport`, `StudentRepository.BulkCreate`. All 27 target columns verified present in `tenant_tables.sql` students table. No DB schema change. API (MSBuild Release) + AscentSyncTool (MSBuild) compile-verified. **API deployed**; redistribute the rebuilt `AscentSyncTool.exe` for the extended fields to flow on the next sync.
Phase 76 ✅  Sync tool — export legacy StuUnqID + match students by student_unique_id (fixes promotion duplicates) — legacy VB6 changes a student's `admission_no` on promotion (UKG→1st etc.) while `StuUnqID` stays stable; the sync's upsert matched existing students by `admission_no + academic_year_id`, so a changed admission_no wasn't found → INSERT → **duplicate**. Fix: carry `StuUnqID → students.student_unique_id` and match on the **stable** `student_unique_id + academic_year_id`, updating `admission_no` on match. **AscentSyncTool:** `LegacyDb.GetStudents` SELECT adds `StuUnqID`; `LegacyStudent` gains `double? StuUnqID` (legacy float); `StudentBulkRow` (`Models/ApiModels.cs`) gains `int? StudentUniqueId`; `Mappers.ToBulkStudent` maps `StuUnqID`(float)→`StudentUniqueId`(int). Also moved the **Export Students** tab to first place in `MainForm.cs`. **API:** Core `StudentBulkRow` (`BulkImportDtos.cs`) gains `int? StudentUniqueId`; `StudentRepository.BulkCreate` upsert path now: if `StudentUniqueId` is supplied → look up existing by `student_unique_id + academic_year_id + school_id`, else fall back to `admission_no + academic_year_id` (so the **web CSV import**, which sends no unique id + `Upsert=false`, is unaffected); the UPDATE now also sets `admission_no = @admissionNo` (writes the promoted/changed number) alongside the personal/contact fields (class/section/roll/status/fee_category/year still untouched); the INSERT uses the supplied `StudentUniqueId` when present (`ISNULL(@providedUniqueId, <reuse-by-admission_no else MAX+1>)`), else the prior generation logic. No DB schema change; no new endpoint. **Migrated-via-AscentMigration schools already align** (StudentsMigrator stored `student_unique_id = StuUnqID`); only students created by the **sync tool before this change** have a web-generated unique_id ≠ StuUnqID. For those, one-time `Database/align_student_unique_id.sql` (cross-DB, tenant + legacy on the same instance; preview-first, transaction-wrapped, ROLLBACK-by-default; matches `admission_no + academic_year` → sets `student_unique_id = StuUnqID`; includes a same-year duplicate report) — run per tenant DB before relying on the new matching, then redeploy the API. Both API (MSBuild Release) + AscentSyncTool (MSBuild) compile-verified. **API deployed**; redistribute the rebuilt `AscentSyncTool.exe`.
Phase 75 ✅  School users created from staff records (`users.employee_id → staff.staff_id`) — school-app **User Management** now creates a login only from an existing **staff** member (except the admin, which is still created via the Control app and has `employee_id = NULL`). Leverages the pre-existing unused `users.employee_id INT NULL` column — **no structural DB change**, only constraints. **Decisions (user):** staff selection **mandatory**; **one login per staff** (enforced); username **auto-suggested + editable**; **email & mobile read-only** (locked to the staff record); full name prefilled + editable. **Backend:** `CreateTenantUserRequest.EmployeeId` (int?) + `TenantUserRepository.Create` writes `employee_id`; `TenantUserDto` gains `EmployeeId`/`StaffName`/`Designation` and `GetAll` `LEFT JOIN staff` for the grid; new `TenantUserRepository.GetAssignableStaff(schoolId)` (Active staff of the branch whose `staff_id NOT IN (SELECT employee_id FROM users WHERE employee_id IS NOT NULL)` — returns `StaffDto`, reused, no new file) + `IsStaffAvailableForLogin(schoolId, staffId)`; new endpoint `GET /school/users/staff`; `SchoolUsersController.CreateUser` now requires `EmployeeId` and validates the staff is Active + unlinked (400 otherwise) before the existing username-taken check. The **control-app admin path** (`SchoolGroupsController`, same `CreateTenantUserRequest`) simply omits `EmployeeId` → NULL, so it's unaffected (the school-app "required" check lives only in `SchoolUsersController`). **Frontend (`ascent-school-app/src/pages/rbac/UsersPage.jsx`):** loads assignable staff on mount; Add-User modal gains a mandatory searchable **Staff Member** select → `onStaffSelect` auto-fills Full Name/Email/Mobile + suggests a username (`employeeCode` else normalized name via `suggestUsername`); **Email & Mobile inputs are `readOnly`**; grid gains a **Staff · Designation** column (admin row → "—"); **Add User** is disabled with an info alert when no assignable staff remain (all linked or none exist); after create, `fetchAll()` refreshes so the just-linked staff drops off the dropdown. **DB:** new idempotent `Database/users_staff_link_migration.sql` adds `FK_users_staff (employee_id → staff.staff_id)` + filtered `UNIQUE INDEX UQ_users_employee ON users(employee_id) WHERE employee_id IS NOT NULL` (one login per staff; multiple NULLs allowed for admin); `tenant_tables.sql` adds the same **after** the `staff` table (staff is table 35, created after `users` table 4 — the FK can't be inline on `users`). Both API (MSBuild Release) + school app (vite) compile-verified. **Deployed** (API + all 5 school apps). **Pending go-live step:** run `users_staff_link_migration.sql` on each tenant DB for the DB-level FK + one-login-per-staff enforcement — the app already works without it (column/join pre-exist; the dropdown hides already-linked staff), so the migration only adds the hard DB guarantee.
Phase 74 ✅  Delete attendance for a date (school app) + Half Day save-validation fix — (1) **Delete feature:** new **"Delete Attendance"** tab in `ascent-school-app` `pages/attendance/AttendancePage.jsx` (third tab beside Mark Attendance / Monthly Summary). Class + Date required, **Section optional** ("All Sections (whole class)") → **Load** (read-only preview of the marked statuses via the existing `GET /school/attendance` grid, with a "N marked" / "Not marked" tag + a Section column) → **Delete Attendance** button behind a `Popconfirm` (disabled when nothing is marked; the confirm text says "this section" vs "the whole class (all sections)"). Hard delete (rows removed) so the day can be re-marked cleanly. **Backend:** `DELETE /school/attendance?classId=&sectionId=&date=` (`AttendanceController.DeleteAttendance`) → `AttendanceRepository.DeleteAttendance` hard-`DELETE`s `student_attendance` rows for **current-academic-year** students of that class on that date (same year-scoping subquery the mark grid uses, so prior-year stray rows aren't touched), returns the deleted row count. **`sectionId` is optional (`int?`)** on both `DELETE` and `GET /school/attendance` — when omitted, `WHERE (@sectionId IS NULL OR section_id = @sectionId)` covers the whole class across all sections. `GetAttendanceGrid` gained a nullable `sectionId`: null → roster of all sections (ordered by section then name), `ClassName` shows just the class name, and `AttendanceStudentDto.SectionName` (new, `LEFT JOIN sections`) lets the class-level preview show each student's section. The Mark tab always passes a section, so it's unaffected (backward-compatible). No DB change. **Web API gotcha (fixed):** a `[FromUri] int? sectionId` param is still treated as REQUIRED for action selection unless it has a **default value** — without `= null`, `GET /school/attendance?classId=&date=` (no sectionId) matched no GET action → 405 `"The requested resource does not support http method 'GET'."` Fix: `[FromUri] int? sectionId = null` (and move it last, C# requires optional params after required ones) on BOTH `GetAttendance` and `DeleteAttendance`. Nullable alone is not enough — the default value is what makes it optional for the URI action selector. **Belt-and-suspenders:** the frontend also always sends `sectionId=${delSection || 0}` (0 = whole class; server maps `sectionId > 0 ? sectionId : null`), so the query param is always present regardless. Verified live by probing the prod endpoint: no-section GET and `sectionId=0` GET/DELETE all return **401** (auth reached = action selected) instead of **405**. Deploy lesson: a `deploy-api.ps1 -SkipBuild` push did NOT pick up the change (running DLL not recycled) — a full `deploy-api.ps1` (rebuild + upload) was needed before the `=null` took effect. (2) **Half Day save-validation fix (completes Phase 73):** the `validStatuses` allow-list in **both** `AttendanceController.SaveAttendance` AND `MobileTeacherController` (the two attendance save paths) was hardcoded `{Present, Absent, Late, Holiday}` and **rejected `HalfDay` with 400** — Phase 73 added Half Day to the UIs but saving it would have failed. Added `"HalfDay"` to both allow-lists (and their error messages). Deploy the API with this before relying on Half Day end-to-end. Both API (MSBuild Release) and school app (vite) compile-verified.
Phase 73 ✅  Student attendance — "Half Day" status added everywhere — extends student attendance from Present/Absent/Late to include **`HalfDay`** (stored value, no space — matches the staff-attendance convention and the parent mobile calendar which already had a `HalfDay` day-cell color). **No DB change** — `student_attendance.status` is `VARCHAR(10)` with no CHECK constraint, so `HalfDay` (7 chars) just stores; save path was already status-agnostic (`AttendanceRepository.SaveAttendance` MERGE). **Decisions (user):** value = `HalfDay`; counts as **0.5 day present** in every attendance %; half-day students do **NOT** get the Absent SMS (only `Absent` triggers it — unchanged). **Backend (.NET):** `AttendanceMonthlySummaryDto.HalfDayDays` + `AttendanceRepository.GetMonthlySummary` SUM; mobile `AttendanceSummaryDto.HalfDayDays` + `MobileDataRepository.GetAttendance` count; `DashboardDto.TodayHalfDay` + `DashboardRepository` (SUM + `AttendanceRow.TodayHalfDay`; attendance % numerator now `Present+Late+0.5×HalfDay`); `ReportDtos` (`AttendanceSheetRowDto.HalfDay`, `DailyAttendanceSummaryRowDto.HalfDay`, `MonthlyAttendanceRowDto.TotalHalfDay`, `MonthCounts.HalfDay`, `AttendanceRegisterRowDto.HalfDay`) + the 4 `ReportsRepository` attendance methods (`GetAttendanceSheet`/`GetAttendanceRegister`/`GetMonthlyAttendanceSheet`/`GetDailyAttendanceSummary`) now count `HalfDay` and map the day-cell to `"H"`. No new endpoints/migration. **School web app:** `AttendancePage.jsx` — `HalfDay` added to `STATUS_OPTIONS`/`STATUS_COLOR` (blue) with a `STATUS_LABEL` map rendering "Half Day"; mark-grid radio, header count tag (`HD:`), and Monthly Summary tab get a Half Day column + the 0.5-weighted %. `DashboardPage.jsx` — half-day in the inline P/A/L line + a Half Day progress row. All 4 attendance report pages (`AttendanceSheetReport`/`AttendanceRegisterReport`/`MonthlyAttendanceSheetReport`/`DailyAttendanceSummaryReport`) get an `H`/`HD` legend+color (blue `#1677ff`), a Half Day count column, grand-total cell, and PDF/CSV export column; their % formulas use `(present + 0.5×halfDay) / total`. **Android:** `ApiModels.kt` `AttendanceSummaryDto.halfDayDays` (default 0); parent `AttendanceScreen.kt` — Half Day `StatPill` + legend entry + % includes `0.5×halfDay` (DayCell already handled `"HalfDay"`); teacher marking — `AttendanceStatus` enum gains `HalfDay` (every exhaustive `when` updated: load-mapping `"halfday"`, quick-tap toggle → resets to Present like Late, status-color/icon in `TeacherAttendanceScreen` [HalfDay = blue, reuses Schedule icon], bottom-sheet status list, summary `HD` chip, badge label "HALF DAY"); long-press status sheet is how Late/HalfDay are set (quick tap stays Present↔Absent). `status.name` = `"HalfDay"` posts the correct value. **Build:** API compiles (MSBuild Release); school app builds (vite, demo mode). Android edits are compile-ready (all exhaustive `when`s covered; DTO field has a default so JSON parse is safe) but need an Android Studio/Gradle build + versionCode bump for the APK (no gradlew wrapper in repo). Deploy: API first (old clients ignore the new status), then school app, then mobile. Not yet deployed.
Phase 72 ✅  Serial-number columns (school app, frontend-only) + migrator TC status — (1) **S.No columns:** added a leading `S.No` column to five `ascent-school-app` grids that previously had none: Students list (`pages/students/StudentsPage.jsx`), Fee Receipts (`pages/fee/ReceiptsPage.jsx`), and three Reports — Class-wise Students (`pages/reports/ClassStudentsReport.jsx`), Absents Statement (`pages/reports/AbsentsReport.jsx`), Attendance Sheet (`pages/reports/AttendanceSheetReport.jsx`). For **paginated** grids (Students, Receipts, Absents) the numbering is continuous across pages — pagination was made controlled (`page`/`pageSize` state + `onChange`) and the cell renders `(page - 1) * pageSize + index + 1`; for the **non-paginated** report grids (Class-wise Students, Attendance Sheet — `pagination={false}`) it renders `index + 1`. Attendance Sheet's S.No is `fixed:'left'` alongside its existing fixed Adm No/Name columns. Frontend-only; the PDF/CSV exports on the report pages were **not** changed (still omit S.No). Deployed to all 5 schools via `deploy/deploy-school.ps1 -School all`. (2) **Migrator TC status:** `AscentMigration/Migrators/StudentsMigrator.cs` `MapStatus` gained `case "TC": return "TC";` (between `D→Inactive` and `R→R`) so VB6 `SAS_StudentMaster.StuStatus='TC'` migrates as the `TC` status the student form recognizes ("TC Issued"). Existing mappings unchanged (`A→Active`, `D→Inactive`, `R→R`, else→null). Note: `switch` is case-sensitive — expects source value exactly `TC` (uppercase). The student-form status dropdown (`StudentFormPage.jsx` `STATUS_OPTIONS`) remains a hardcoded 3-option list (Active/Inactive/TC); the Students-list filter additionally includes Blocked (managed via Block/Unblock actions, not the form dropdown). (3) **New migrator — exam_types** (`SAS_ExamNam` → `exam_types`): `AscentMigration/Migrators/ExamTypesMigrator.cs` + `Models/Legacy/LegacyExamType.cs` (POCO: `ExamNam`/`acdyear`/`examremarks`/`examstatus`). Source `SELECT DISTINCT ExamNam, acdyear, examremarks, examstatus FROM SAS_ExamNam`. Mapping: `ExamNam`→`exam_type_name` (trimmed; row skipped if blank), `acdyear`→`academic_year_id` (lookup `academic_years.academic_year=acdyear` school-scoped, **fallback** to `ResolveDefaultAcademicYearIdAsync` on miss — same pattern as Terms/Subjects), `examremarks`(varchar)→`display_order` via `int.TryParse` (NULL when unparseable; column is `INT NULL`), `examstatus` A→Active/D→Inactive/else→Active (column is `VARCHAR(10)`, not int despite the brief's "status int"); `school_id`=`Config.SchoolId`, `created_by`="migration", `created_at`=`DateTime.Now`. **Dedup is per-year** — composite skip key `exam_type_name|academic_year_id` (so the same exam name across two years → two rows, matching how the app filters exam types by year); a second source row with the same name+year but different remarks/status is counted as Skipped (first wins), guarded both against existing dest rows (Skip mode) and within the same batch. Registered in `MigrationRunner.BuildMigrators` after `SubjectsMigrator` (only dep is academic_years). Added to `MigrationRunner.CleanupAsync` (after fee_receipts — nothing in the migrated set references it; `student_marks` does but isn't migrated) and to `Database/cleanup_migration_data.sql`. Truncate/Skip modes + `exam_types.force` re-run supported like the rest. Compile-verified. (4) **New table + migrator — grade_types** (`SAS_MarksGrade` → `grade_types`): NEW tenant table `grade_types` (table 45) — `id` IDENTITY PK, `grade_name` VARCHAR(200), `subject_id` INT NULL (FK → subjects), `max_marks`/`min_marks` FLOAT, `grade` VARCHAR(100), `remarks` VARCHAR(200), `status` VARCHAR(10) DEFAULT 'Active', `created_date` DATETIME DEFAULT GETDATE() (note: `created_date`, not `created_at`), `created_by` VARCHAR(200), `school_id` INT. Added to `Database/tenant_tables.sql` (new groups) + new idempotent `Database/grade_types_migration.sql` (`IF NOT EXISTS` guard — run per existing tenant DB). Migrator `AscentMigration/Migrators/GradeTypesMigrator.cs` + `Models/Legacy/LegacyMarksGrade.cs`. Mapping: `GradName`→`grade_name`, `MaxMrks`→`max_marks`, `MinMrks`→`min_marks`, `Grad`→`grade`, `Rrmrks`→`remarks`, `TraStatus` A→Active/D→Inactive/else→Active; `created_date`=`DateTime.Now`, `created_by`="migration", `school_id`=`Config.SchoolId`. **`SubjID`→`subject_id` is a two-step lookup** (like FeeStructures' category): step 1 legacy `SAS_MarksGrade.SubjID` → `SAS_Subjects.SubjectNam` (source map `SubJectID`→`SubjectNam`); step 2 `subject_name` → dest `subjects.subject_id`. Since dest `subjects` are per-academic-year (same name repeats), the dest map is ordered `academic_year_id DESC` so a name collision maps to the **latest year's** subject_id (warned); a miss at either step sets `subject_id=NULL` (row still inserted, error logged — grade not lost). Skip-mode composite key `grade_name|subject_id|grade|min_marks|max_marks`. Registered in `BuildMigrators` after `ExamTypesMigrator` (dep: subjects, migrated earlier); cleanup deletes `grade_types` **before** `subjects` (FK) — added to `CleanupAsync` and `cleanup_migration_data.sql` near the top. Truncate/Skip + `grade_types.force` supported. Compile-verified. (Note: `grade_types` is migrate-only — no school-app UI/API consumes it yet.) (5) **New table + migrator — exam_master** (`SAS_ExamNam` (full rows) → `exam_master`): NEW tenant table `exam_master` (table 46) — `id` IDENTITY PK, `exam_type_id` INT NULL (FK → exam_types), `class_id` INT NULL (FK → classes), `exam_total_marks`/`exam_min_marks`/`subject_min_marks`/`sub_max_marks` INT, `exam_remarks` VARCHAR(300), `academic_year_id` INT NULL (FK → academic_years), `subject_id` INT NULL (FK → subjects), `exam_status` VARCHAR(10) DEFAULT 'Active', `created_by` VARCHAR(200), `created_date` DATETIME DEFAULT GETDATE(), `school_id` INT, `exam_category` VARCHAR(200), `exam_date` DATE, `grade_type_id` INT NULL (FK → grade_types). Added to `tenant_tables.sql` + new idempotent `Database/exam_master_migration.sql`. Migrator `AscentMigration/Migrators/ExamMasterMigrator.cs` + `Models/Legacy/LegacyExamMaster.cs`. **Source is the SAME `SAS_ExamNam` table that feeds ExamTypesMigrator** — but read in full (`SELECT *`, one exam_master row per legacy row), whereas ExamTypesMigrator reads only DISTINCT names. Direct maps: `ExamTotalMarks`→`exam_total_marks`, `ExamMinMarks`→`exam_min_marks`, `SubMinMrks`→`subject_min_marks`, `SubMaxMrks`→`sub_max_marks`, `ExamRemarks`→`exam_remarks`, `ExamCatgry`→`exam_category`, `ExamDatTim`(datetime)→`exam_date`(date), `ExamStatus` A→Active/D→Inactive/else→Active; `created_date`=now, `created_by`="migration", `school_id`=`Config.SchoolId`. **Lookups:** `AcdYear`→`academic_year_id` (academic_years map, fallback default year); `ExamNam`→`exam_type_id` via exam_types keyed `name|academic_year_id` (per-year; falls back to name-only/latest-year, NULL+error on miss); `ClasId`→`class_id` two-step (legacy `SAS_ClassNames.ClassID→ClassNam` then dest `classes.class_name→class_id`); `SubjectID`→`subject_id` two-step (legacy `SAS_Subjects.SubJectID→SubjectNam` then dest subjects keyed `name|academic_year_id`, fallback name-latest); `GradeTyp`→`grade_type_id` — legacy `SAS_MarksGrade.MrksGradId→GradName`, then dest `grade_types` matched by **`grade_name|subject_name`** (per user decision: grade_types has NO class_id, so the class condition in the original spec is dropped; subject matched by NAME via `grade_types LEFT JOIN subjects` to decouple from per-year subject_id). Every lookup miss → that FK column NULL + a per-row error logged (row still inserted; nothing lost). Skip-mode composite key `exam_type_id|class_id|subject_id|academic_year_id|exam_category`. Registered in `BuildMigrators` after `GradeTypesMigrator` (all 5 parent tables migrated earlier); cleanup deletes `exam_master` **first** (it's the most-child table) — added at the TOP of `CleanupAsync` and `cleanup_migration_data.sql`. Note: `LoadExamTypeMap`/`LoadSubjectMap` are SYNC (not async) because they use `out` params (C# forbids `out` on async) — they call sync `dest.Query`. Truncate/Skip + `exam_master.force` supported. Compile-verified. Migrate-only (no UI/API yet).
Phase 70 ✅  Daily (multi-subject) homework entry — legacy-style "enter all subjects' homework for a class on one date" page, alongside the existing single-entry Homework List. **Backend:** `POST /school/homework/batch` (`HomeworkController.CreateBatchHomework`) takes `BatchHomeworkRequest` (`ClassId`, `SectionId`, `AssignedDate`, `Items: [{SubjectId, Description}]`; new DTOs in `HomeworkDto.cs`); validates ≥1 filled subject. `HomeworkRepository.CreateBatchHomework` runs in a transaction: resolves subject names in one query (row title = `"{subjectName} Homework"`), **soft-cancels (`status='Cancelled'`) that day's existing Active rows for the same Class+Section** (matched with `ISNULL(class_id,0)`/`ISNULL(section_id,0)`), then inserts one `homework` row per filled subject with `due_date = assigned_date` (legacy form: H.W. date IS the due date) — so re-saving the same Class+Section+Date acts as create-or-edit. Returns the count of subjects saved. **Frontend:** `ascent-school-app/src/pages/homework/DailyHomeworkPage.jsx` (Class/Section/Date pickers → grid of subjects with a Description field each → Save). Homework nav restructured into a parent menu with two leaves: **"Daily Homework"** (`/homework/daily`) and **"Homework List"** (`/homework`). Both gated by the `HOMEWORK.MANAGE` permission (Phase 50). The mobile homework feed (`section_id IS NULL OR = @sectionId`, Phase 19) reads these rows unchanged.
Phase 69 ✅  Pending online-payment reconciliation — recovers gateway payments where the parent paid but left the app/browser before the JS success callback ran AND no webhook is configured (the order stays `Pending` forever, parent is debited, no receipt). New admin page **"Pending Online Payments"** under Fees (`/fees/pending-payments`, gated by `STUDENT_FEE` perms). Uses the gateway API keys already in `gateway_configs` — **no Razorpay dashboard access needed**. **Backend (`FeeController` + `GatewayRepository` + `IGatewayService`/`RazorpayGatewayService`):** new `IGatewayService.FetchOrderPayment(keyId, keySecret, externalOrderId)` → `OrderPaymentStatus` (Found/PaymentId/Status/AmountInRupees/Method); Razorpay impl calls `GET orders/{id}/payments` (Basic auth), prefers a `captured` payment else the latest attempt. Endpoints: `GET /school/fees/payment-orders/pending?createdAfter=&createdBefore=` (lists Pending orders joined to student → `PendingPaymentOrderDto`), `GET /school/fees/payment-orders/{id}/status` (live single-order status → `PaymentOrderStatusDto`), `GET /school/fees/payment-orders/scan?...&onlyReconcilable=true` (checks every pending order at the gateway in one pass → `ScannedPaymentOrderDto[]`, default returns only captured/reconcilable rows so the admin needn't click each), `POST /school/fees/payment-orders/{id}/reconcile` (creates the receipt for a genuinely-captured order). A shared private `ReconcileOrderToReceipt(...)` is now the single receipt-creation path for the verify callback, the webhook, AND the manual reconcile (each tags receipt remarks with its source: `""`/` via webhook`/` via reconcile`) — replaces the duplicated collect-request reconstruction. `EvaluateStatus(...)` centralizes the reconcilable decision (captured + amount matches within ₹0.01 + not already Paid) and its human-readable note, shared by single-status and scan. Reconcile is idempotent (a late webhook that landed first short-circuits to the existing receipt) and refuses on non-captured/amount-mismatch. `GatewayRepository.GetPendingOrders` + `GatewayOrderListItem` (data-layer row shape) added. **Frontend:** `ascent-school-app/src/pages/fee/PendingPaymentsPage.jsx` (date-range filter → list pending orders, per-row "Check status", bulk "Scan", "Reconcile" on captured rows). No DB change.
Phase 67 ✅  Attendance current-year scope — more spots — extending the Phase 66 attendance fix to the remaining attendance reads that lacked the current-academic-year filter (multi-year student model: one Active row per student per year, classes/sections shared across years → prior-year/promoted students leaked in / counts inflated). Fixed: (1) `ReportsRepository.GetAttendanceSheet` (the only attendance report missing it — the others already take `@academicYearId`; this one has no year picker so it uses the current-year subquery); (2) `DashboardRepository` "today's attendance" — now JOINs `students` + filters current year (was a raw `student_attendance` COUNT with no student join, so stray marks on prior-year student rows inflated the dashboard count); (3) `ReportsRepository.GetDailyAttendanceSummary` — added the current-year filter to the grouped query. All use the same `(SELECT TOP 1 academic_year_id FROM academic_years WHERE school_id=@schoolId AND status='Active' ORDER BY academic_year_id DESC)` subquery. API-only. Note: attendance rows previously SAVED against prior-year student_ids (via the old all-years grid) still exist in `student_attendance` — now excluded from counts but not deleted; a cleanup query can remove them if desired.
Phase 66 ✅  School-app footer version stamp (frontend-only) — the `ascent-school-app` footer ("Powered by Ascent Info Solutions") now also shows `· v{version} ({build date})` so you can tell which build is live. `vite.config.js` reads `package.json` and injects two build-time globals via `define`: `__APP_VERSION__` (from `package.json.version`, set to `1.0.0` — bump per release) and `__BUILD_DATE__` (`new Date()` at build, `yyyy-MM-dd HH:mm`, auto-updates every build so a fresh deploy is distinguishable even without a version bump). `AppLayout.jsx` renders them (with a top-of-file `/* global __APP_VERSION__, __BUILD_DATE__ */` for lint). Each white-label flavor stamps its own build time. Frontend-only; school app only (parent/control not changed). Also fixed attendance: `AttendanceRepository` (grid + existing-marks subquery + monthly summary) now filters students by the current academic year (`academic_year_id = (SELECT TOP 1 ... FROM academic_years WHERE school_id=@schoolId AND status='Active' ORDER BY academic_year_id DESC)`) — the multi-year student model previously returned prior-year/promoted students for a class/section (classes & sections are shared across years), so the roster was wrong; API-only, redeploy.
Phase 65 ✅  Android session-keepalive + refresh (Android-only) — (1) **Stay logged in:** the app had NO in-session token refresh (only a cold-start `silentRefresh()`), so a mid-session 401 (access token expired during inactivity) surfaced "session expired" and forced re-login — unlike the web apps whose HTTP layer refreshes-and-retries on 401. Added an **OkHttp `Authenticator`** in `RetrofitClient.kt`: on 401 it calls the parent/teacher refresh endpoint via a separate cookie-sharing client (no recursion; skips `/auth/` calls; caps retries via `responseCount`; thread-safe via `refreshLock`), saves the new access token to `TokenStore`, and retries the original request → app stays logged in for the full 7-day sliding window. **Prereqs (same as web):** API deployed with Phase 56 cookie `Path=/` + 480-min token, and ONE re-login to replace the old `/mobile/auth/parent`-path cookie (which never reaches `/api/...`). (2) **Data refresh** (staying logged in does NOT auto-refresh screen data — each tab loads once on its ViewModel init): added **pull-to-refresh per tab** via a reusable `ui/components/PullToRefresh.kt` wrapper (Material3 1.2.x experimental `PullToRefreshContainer` — BOM 2024.08 lacks `PullToRefreshBox`), applied in `HomeScreen`'s `when(tab)` around each screen (no per-screen edits — LazyColumn nested-scroll feeds the gesture; brief spinner then the tab's own load runs); plus a **global Refresh icon** in the `TopAppBar` that calls every tab VM's `load()` / `feeVm.loadFees()` (all VMs are created upfront in HomeScreen). Android-only; compile-verified. (3) **Refresh button also re-checks app version:** since long-lived sessions may never cold-start, the global Refresh button now also re-runs the Phase 57 update gate — `MainActivity` exposes a `recheckUpdate` callback (re-runs `appConfigRepo.checkVersion()`, resets `softDismissed`) passed to `HomeScreen` as `onCheckUpdate`; the refresh button invokes it, so Forced → `ForceUpdateScreen` (blocking) / Soft → `UpdateAvailableDialog`, reusing the existing UI (no new API/DB; `/mobile/app/config` + `app_config`).
Phase 64 ✅  Attendance → SMS absentees on save (frontend-only) — after Attendance "Save" succeeds, if any student is marked Absent, `AttendancePage.jsx` shows a `modal.confirm` "Send SMS to absentees?" ; on Yes it reuses the SMS Center ABSENT flow: `GET /school/sms/recipients?smsType=Absent&date=&classId=&sectionId=` (server recomputes the just-saved absentees WITH parent mobile + class + admissionNo) then `POST /school/sms/send` with `smsType:'Absent'`. Uses the school's configured ABSENT template/gateway + `{name}/{class}/{admissionNo}/{date}` substitution (Phase 60). Recipients are read from the server (not the grid) because the attendance grid lacks parent mobile/class; the recipients endpoint also auto-excludes absentees without a mobile, so the toast reports actual sent/failed (may be fewer than the popup's grid count). No API/DB change — reuses existing endpoints; deploy the school app only. Prerequisite: SMS gateway + active ABSENT template configured (Phase 55/60) and API deployed with the Phase 61 Task.Run fix, else the send returns a clear error (attendance still saves).
Phase 63 ✅  School-app UI tweaks (frontend-only) — (1) **Full-width layout:** `ascent-school-app/src/index.css` `#root` was locked to a fixed centered column (`width:1126px; margin:0 auto; border-inline; text-align:center`) left over from the Vite starter, causing left/right empty space — changed to `width:100%` (removed the fixed width, centering, side border, inherited center-align; AntD handles its own layout). The rest of `index.css`/`App.css` are still unused starter styles (`.counter`/`.hero`/`#social`/big `h1`) — harmless. The parent (`ascent-parent-app`) and control (`ascent-control-app`) apps likely share the same starter `#root` and would need the same fix if their gap matters. (2) **Attendance serial number:** added a `#` column (row index + 1) as the first column of the mark-attendance table (`markColumns` in `AttendancePage.jsx`); the Monthly Summary table doesn't have it. Both are school-app-only; no API/DB change.
Phase 62 ✅  SMS Center "SMS Type" dropdown labels from template Titles (frontend-only) — the Send-tab "SMS Type" dropdown was hardcoded (`SMS_TYPES` = Absent/FeeDue/Custom, fixed labels). Now the **labels** are loaded from each school's templates: on mount `SMSPage.jsx` (SendTab) calls `GET /school/sms/config`, maps each fixed type to its template via `SMS_TYPE_KEY` (`Absent→ABSENT`, `FeeDue→FEE_DUE`, `Custom→CUSTOM`), and uses that template's `title` as the label (falls back to the hardcoded label when a template/title is missing). So renaming a template's Title in Settings → SMS Gateway renames the dropdown. **Still only the 3 fixed types** — the dropdown `value`s and their per-type recipient logic (date vs year filters, `GetRecipients` queries) are unchanged; a brand-new template key does NOT create a new type (that needs code in ~4 places: `SMS_TYPES`/dropdown, `GetRecipients`+repo query, the type's filter UI, and `TemplateKeyFor`). The History-tab type filter still uses the static labels. No API/DB change; deploy the school app only.
Phase 61 ✅  SMS Center send crash fix — `TenantContext.Current` is null inside `Task.Run` — sending from SMS Center returned 500 "An error has occurred / Object reference not set" (shown as "Network Error" per recipient in the grid), while the SMS still went out. Cause: `SmsController.Send` dispatches each recipient on a parallel `Task.Run` thread and called `_repo.LogSms(Tenant.TenantDbName, Tenant.SchoolId, ...)` inside it; **`TenantContext.Current` reads `HttpContext.Current.Items`, and worker threads have no `HttpContext`** → `Tenant` is null → NRE at the LogSms call (after the SMS was already sent). Latent since the Phase 12 parallel dispatch; surfaced now that SMS Center is used end-to-end. Fix: hoist `var dbName = Tenant.TenantDbName; var schoolId = Tenant.SchoolId;` (and `sentBy`) into locals **before** the `Task.Run` loop and use those inside the lambda — never touch `Tenant`/`TenantContext.Current`/`HttpContext.Current` inside `Task.Run`/`Task.WhenAll` worker threads. API-only fix; redeploy required. (Companion content note: a template whose Message Text still contains the DLT `{#var#}` syntax sends literally — the server only substitutes the named tokens `{name}/{class}/{admissionNo}/{date}/{amount}`, so put those in Message Text, not `{#var#}`.)
Phase 60 ✅  SMS Center placeholders extended — `{class}` + `{admissionNo}` — the SMS template substitution (`SmsController.BuildMessage`) previously only filled `{name}`/`{date}`/`{amount}`, and the send payload didn't even carry class/admission-no to the server. Added so schools can use real DLT templates like `Dear Parent, Your ward {name} of {class}, Admin No : {admissionNo} is Absent on {date}.ST.ANNE'S`. **Full token set now:** `{name}` (student name), `{class}` (class name), `{admissionNo}` (admission no), `{date}` (Absent date, `yyyy-MM-dd`), `{amount}` (Fee Due outstanding). Changes: `SmsRecipient` DTO (`SmsDtos.cs`) gains `AdmissionNo` + `ClassName`; `BuildMessage` adds `.Replace("{class}",...)` + `.Replace("{admissionNo}",...)`; `SMSPage.jsx` send payload now includes `admissionNo`/`className` per recipient (`GetRecipients` already returned them — they were dropped before); `SmsGatewayPage.jsx` hint text lists the full set. Tokens go in the template's **Message Text** field; the "Placeholders (hint)" field is cosmetic only (never read by code). **Both API + school app must be deployed together** — until then `{class}`/`{admissionNo}` render literally. No DB change. **UI discoverability (frontend-only follow-up):** `SmsGatewayPage.jsx` Add/Edit-Template modal now shows the available placeholders as **clickable chips** (click inserts the token into Message Text) + a legend `{name} = Student name · {class} = Class name · …`, driven by a `PLACEHOLDERS` constant that mirrors `BuildMessage` (keep in sync when adding server-side tokens); the "Placeholders (hint)" field is labelled optional/for-reference-only. The stored message_text's static text + variable order must match the school's DLT-approved template exactly or the operator rejects it. (Date currently sent as `yyyy-MM-dd`; format in `BuildMessage` if the DLT template needs `dd-mm-yyyy`.)
Phase 59 ✅  One-click deploy scripts + build fixes — replaced the manual "build each school -> overwrite dist -> copy to Plesk -> repeat" workflow with PowerShell deploy scripts under `deploy/` (native `System.Net.FtpWebRequest`, no WinSCP). **Files:** `deploy/deploy-school.ps1`, `deploy/deploy-parent.ps1` (`-School <name>|all`, `-SkipBuild`), `deploy/deploy-api.ps1` (`-SkipBuild`), `deploy/_common.ps1` (FTP upload + config loader + `Invoke-ViteBuild`), `deploy/deploy.config.example.json` (committed template) + `deploy/deploy.config.json` (**gitignored** — FTP creds + per-school remote paths), `deploy/README.md`. Each script **builds then FTP-uploads**: React apps build each flavor into its **own** `dist/<name>` via `npm run build:<mode> -- --outDir dist/<name> --emptyOutDir` (fixes the shared-`dist` clobber), API publishes via MSBuild FolderProfile (`bin/Release/Publish`). **API safety rules:** `Uploads/` is never uploaded (`api.excludeDirs`, preserves server media); `web.config` uploaded only if `uploadWebConfig=true` AND it does NOT still contain the placeholder `CHANGE_THIS_TO_A_STRONG_SECRET` (guard against pushing template secrets to prod). Native FTP has no diff-sync → full upload each run, additive (nothing deleted). **Build fixes made along the way:** (1) `AscentSchools.API.csproj` web-publish import was hardcoded to `v15.0` (`...\VisualStudio\v15.0\WebApplications\Microsoft.WebApplication.targets`) which fails on machines with only newer VS (e.g. VS18) — replaced with the **version-agnostic** `$(VSToolsPath)` pattern (`VisualStudioVersion`/`VSToolsPath` defaults + `Import ... Condition=Exists(...)`); `deploy-api.ps1` locates the installed `Microsoft.WebApplication.targets` via `vswhere` and passes `/p:VSToolsPath=...`. Works under any VS, CLI or IDE. (2) Parent app `package.json` build scripts realigned to its `.env.*` files (added `build:depaulemyv`/`build:holyspiritjm`, removed orphan `build:srividya`). (3) SPA fallback: `.htaccess` + `web.config` (URL-rewrite all non-file paths → `/index.html`) live in each web app's `public/` so a browser refresh on a deep route (e.g. `/students`) loads the app instead of a host 404. Run scripts from repo root: `.\deploy\deploy-api.ps1`, `.\deploy\deploy-school.ps1 -School stannsasf`. Only `stannsasf` + API path (`/httpdocs/api`) confirmed; other school/parent docroots in the config are pattern-guessed — verify in Plesk. **Remote-path model:** the scripts upload to the **literal** `schoolRemote` / `parentRemote` string per school — there is NO prefix/convention logic in code (the `parent.<sub>` defaults are just placeholder guesses; real Plesk dirs vary, e.g. demo's parent is `/p.demo.edu-care.in/`). `deploy-school.ps1` uses `schoolRemote`; `deploy-parent.ps1` builds from `parentApp.appDir` and uploads to `parentRemote`. A school whose `parentRemote` is `""`/`null` is skipped by `deploy-parent.ps1`. Edit each path in the gitignored `deploy.config.json` to the exact Plesk Document Root.
Phase 59b ✅  Android debug cleartext fix — testing the `stannsasf` flavor in DEBUG against the local API (`http://localhost:62845/`) threw "CLEARTEXT communication to localhost not permitted". Root cause: the manifest sets `usesCleartextTraffic="true"` but ALSO `networkSecurityConfig="@xml/network_security_config"`, and **when a network security config is present, `usesCleartextTraffic` is ignored**; the `stannsasf` (and `depaulemyv`) flavor ship their own `res/xml/network_security_config.xml` with `cleartextTrafficPermitted="false"` (prod-locked), overriding `main`'s `true`. Fix: added **`app/src/debug/res/xml/network_security_config.xml`** with `cleartextTrafficPermitted="true"` — in Android resource merging the **build-type (`debug`) source set overrides the flavor**, so ALL `*Debug` builds permit cleartext while every release build keeps its flavor's secure (https-only) config. Verified via `mergeStannsasfDebugResources` → packaged_res resolves to `true`. (Note: a variant source set like `src/stannsasfDebug` also works but the single build-type file covers every flavor.) Reminder for physical-device debug: `localhost` is the phone itself, so run `adb reverse tcp:62845 tcp:62845` once per adb session so the phone reaches the host's IIS Express; rebuild/reinstall the debug app to pick up the new config.
Phase 58 ✅  In-app receipt print (Android + parent web) + fee-collection fixes — (A) **Concession double-count fix:** `li.outstanding` already nets out concession (structure − paid − concession), but all three payment clients also sent `ConcessionAmount` separately, so the server subtracted it twice → online order failed with "Total amount must be greater than zero" (and offline made negative receipts). Fixed so each item sends `Amount = outstanding + concession`, `ConcessionAmount = concession` → server stores `net_amount = outstanding` (correct paid total) while the receipt still shows the concession line (`FeeCollectionBase.jsx` school, `FeeHomePage.jsx` parent web, `FeeViewModel.kt` Android). (B) **Mobile popup "₹0" fix:** Android `MobilePaymentResultDto.amount` didn't match the server's `totalAmount` JSON → always 0.0; renamed field to `totalAmount` (the receipt + money were always correct — display only). (C) **Receipt "Collected by":** Android sends header `X-Client-App: mobile` (RetrofitClient); `MobileFeesController.ResolveClientSource()` → `"Mobile App"` for the app, defaults to `"Parent Portal"` for the web (no header). Stored as `created_by` on the order (so the Razorpay webhook fallback inherits it) and the receipt. (D) **Print receipt:** new `GET /mobile/fees/receipts/{id}` (MobileFeesController, validated to the child by `student_unique_id` via `FeeRepository.ReceiptBelongsToUniqueId`); `GetCrossYearFeeSummary` line items gained `ReceiptId` + `CreatedBy` (correlated subqueries in school/transport/hostel branches). **Android:** `MobileFeeLineItemDto.canPrint` = paid AND receiptId != null AND `createdBy == "Mobile App"`; a Print icon on paid rows + a "View / Print Receipt" button on the success dialog; `ReceiptPrinter.kt` builds branded HTML → offscreen WebView → `PrintManager` (Print or Save-as-PDF, no library). **Parent web:** Print button on paid rows where `createdBy == "Parent Portal"`; fetches the receipt and reuses `PaymentSuccessPage` (with a `reprint` flag → header shows "Fee Receipt"). Tapping a paid term prints the whole receipt it belongs to (may include other terms paid together). OTP and the school web app receipt print (Phase 42) unchanged.
Phase 57 ✅  Mobile app update gate (server-driven force/soft update) — lets you force every installed app onto a safe version by flipping a DB flag, with no app redeploy. **DB (master):** `app_config` table (`application_id`, `platform`, `min_supported_version_code`, `latest_version_code`, `update_message`, `store_url`; UNIQUE(application_id, platform)); migration `Database/app_config_migration.sql` seeds one row per published applicationId (`in.educare.app`/`stannsasf`/`holyspiritjm`/`depaulemyv`/`demo`) + a `'*'` fallback, armed but forcing nobody (`min=1, latest=12`); added to `master_tables.sql` (table 16). **Backend:** `GET /mobile/app/config?applicationId=&platform=android&versionCode=` (`MobileAppConfigController`, AllowAnonymous — called on cold start before login); `AppConfigRepository.GetConfig` (exact app row, falls back to `'*'`); computes `updateRequired` (versionCode < min) / `updateAvailable` (< latest); **fails open** (returns "no update" when no config row); DTOs `AppConfigDto`/`AppVersionStatusDto` in `Core/DTOs/Mobile/AppConfig/AppConfigDtos.cs`. **Android:** `AppConfigRepository.checkVersion()` (fail-open) returns `UpdateStatus` (Ok/Soft/Forced); `MainActivity` checks on launch → `ForceUpdateScreen` (full-screen, back-disabled) when required, dismissible `UpdateAvailableDialog` when optional, else proceeds; `ui/update/UpdateScreens.kt` + `openPlayStore()` (uses `store_url` or `market://details?id=<pkg>`). To force later: `UPDATE app_config SET min_supported_version_code=<N> WHERE application_id='...'`. Must be shipped in v1 so it's in the field before a breaking bug (zero installs at build time = clean). iOS-ready via the `platform` column. **OPERATIONAL GOTCHAS (learned the hard way):** (1) `min_supported_version_code` / `latest_version_code` are Gradle **`versionCode` INTEGERS** (e.g. 13, 16) — NOT `versionName` strings ("1.5"). Putting "1.5" in the INT column stores `1`/`2` and breaks the gate. (2) **Never set `min` above the versionCode actually LIVE in Play Production** — doing so force-loops every user (the live app is below min, and there's no newer version to update to). Sequence: promote the new versionCode to **Production** (not just a beta/closed-testing track — a beta build is NOT what normal installs get) → confirm it's rolled out and a device reports it (the school-app/login footer shows `(build N)`; or `adb shell dumpsys package <appId> | findstr versionCode`) → THEN raise `min`/`latest`. (3) Android **Settings → Apps shows versionName, not versionCode** — to read the real installed versionCode use the login-screen footer `(build N)` (Phase 65) or `adb dumpsys`. (4) Unblock a force-loop instantly (no redeploy) by lowering `min_supported_version_code` to ≤ the live versionCode.
Phase 56 ✅  Session/refresh fix — "logged out after idle" — two parts. (1) **Root-cause fix:** silent token refresh always failed in production, so users were kicked to the login page the moment the 30-min access token expired (only noticeable after idle, since within 30 min the in-memory access token covered everything; tabs left open felt fine until idle). Cause: every refresh cookie was scoped to its route path (`schoolRefreshToken` → `Path=/school/auth`, `controlRefreshToken` → `/control/auth`, `parentRefreshToken` → `/mobile/auth/parent`, `studentRefreshToken` → `/mobile/auth/student`, `teacherRefreshToken` → `/mobile/auth/teacher`), but the API is hosted under `/api` in production (`https://edu-care.in/api`), so the real refresh URLs are `/api/school/auth/refresh` etc. — the browser/OkHttp never sent a `/school/auth`-scoped cookie to a `/api/school/auth/refresh` request (path-prefix mismatch) → refresh 401 → logout. Worked in local dev only because dev has no `/api` prefix (`http://localhost:62845/school/auth/refresh` matched). Fix: set `Path=/` on ALL refresh cookies (set + clear) in `SchoolAuthController`, `ControlAuthController`, `MobileAuthController` (parent + student), `MobileTeacherAuthController`. `/` is a strict superset so it's safe regardless of deployment topology (subdomain vs path, with/without `/api`). Existing orphaned cookies (old path) are harmless and expire in 7 days; a fresh login re-issues at `/`. (2) **Mitigation:** bumped `Jwt:AccessTokenMins` 30 → 480 (8 h) in Web.config so an idle user stays active a full school day even before a refresh is needed (Web.config edit auto-restarts the app pool, no rebuild). Note: cookies still `Secure=false` (left as-is so local http dev works; same-site Lax default sends them fine on https — revisit if ever served cross-site, which needs `SameSite=None; Secure`). Both web apps' page-reload session restore was separately fixed earlier via the `authChecked` render gate (don't redirect to /login until the mount silent-refresh resolves).
Phase 55 ✅  Per-school SMS gateway config — the SMS Center previously sent via a single hardcoded smslogin.mobi account in `SmsHelper.cs` (username `mushkin` / sender `ASNTNF`) with **placeholder** DLT template ids (`PLACEHOLDER_ABSENT_TPL_ID` etc.), so Absent/FeeDue/Custom SMS could never actually send. Made the gateway account + DLT templates **per-school, DB-driven** (normalized two-table design so new SMS types are data, not schema). **DB (tenant):** `sms_configs` (one row per school — provider, api_url, username, api_key, sender_id, is_enabled) + `sms_templates` (one row per school per `template_key` — template_id, message_text with `{name}/{date}/{amount}` placeholders, placeholders hint, is_active; UNIQUE(school_id, template_key)); migration `Database/sms_config_migration.sql` (seeds existing schools from DISTINCT `students.school_id` with the old shared account + 3 default templates `ABSENT`/`FEE_DUE`/`CUSTOM`, template_id blank); added to `tenant_tables.sql` (structure) + `seed_tenant_data.sql` (per-`@SchoolId` seed). **Backend:** `SmsHelper.SendSms` gained a per-account overload `(apiUrl, username, apiKey, senderId, mobile, message, templateId)`; old global overload kept for OTP; the 3 placeholder template consts removed. `SmsRepository` gained `GetSmsAccount` (incl. api_key, send path), `GetSmsConfig` (api_key omitted → `HasApiKey` flag, includes templates), `UpsertSmsConfig` (blank api_key keeps existing), `GetTemplates`/`GetTemplate`/`UpsertTemplate`/`DeleteTemplate`. `SmsController` `Send` now loads the school's account + template by key (Absent→ABSENT, FeeDue→FEE_DUE, Custom→CUSTOM), errors clearly if not configured/disabled/no DLT id, fills placeholders from recipient/context (Custom uses the typed message); new endpoints `GET/PUT /school/sms/config`, `POST /school/sms/templates`, `DELETE /school/sms/templates/{key}`; DTOs `SmsConfigDto`/`UpdateSmsConfigRequest`/`SmsTemplateDto`/`SaveSmsTemplateRequest`/`SmsAccount` added to `SmsDtos.cs`. **Frontend:** new `pages/settings/SmsGatewayPage.jsx` (gateway-account card with write-only API key + a templates table with add/edit/delete modal), under **Settings → SMS Gateway** (`/settings/sms-gateway`), gated by `SETTINGS.MANAGE`. **OTP is intentionally unchanged** — parent app + mobile login OTP still use the hardcoded global account + `OtpTemplateId` in `SmsHelper.cs`. Go-live: run `sms_config_migration.sql` on each tenant DB, deploy API + school app, then each school sets its own sender id + DLT template ids in the new page (Send fails with a clear message until configured).
Phase 54 ✅  Exam Types management UI — the Marks Entry "Exam Type" dropdown was always empty because exam types are per-academic-year rows in the `exam_types` tenant table, the backend CRUD existed (`GET/POST/PUT /school/marks/exam-types` in `MarksController`/`MarksRepository`) but **no frontend screen ever created them** (only `MarksEntryPage` + the 3 exam report pages read them). Added `ascent-school-app/src/pages/master/ExamTypesTab.jsx` (modeled on `FeePeriodsTab`) and wired it into `MasterDataPage.jsx` as a new "Exam Types" tab between Subjects and Payment Modes: academic-year filter (auto-selects current year via `?activeOnly=true` + `isCurrent`), table (Order / Exam Type / Status / edit), add/edit modal (Academic Year required, Exam Type Name required max 50, Display Order). Uses the existing endpoints — DTO `SaveExamTypeRequest` = `ExamTypeName` + `AcademicYearId` + `DisplayOrder`; no backend change. Notes: backend `GetExamTypes` returns Active-only and create/update expose no status field (no delete/deactivate endpoint yet), so the form omits status (shows a green "Active" tag); exam types must be created per academic year (Marks Entry filters by year). Gated by the `MASTER_DATA` permission like the rest of Master Data (Phase 50). Quick SQL unblock without the UI: `INSERT INTO exam_types (exam_type_name, academic_year_id, display_order, school_id, created_by, status) VALUES ('FA 1', @YearId, 1, @SchoolId, 'manual', 'Active'), ...`.
Phase 53 ✅  RBAC fix — role→permission mapping matched by name, not `school_id IS NULL` — after Phase 50 (page gating), an admin logged in and saw ONLY the Dashboard. Root cause: `GetUserPermissions` (`SchoolAuthRepository`) returned ZERO permissions because the role→permission seed/migration mapped permissions with `WHERE r.role_name = '...' AND r.school_id IS NULL`. In some tenant DBs (e.g. migrated ones) the default roles (`Principal`, `Admin Clerk`, ...) are created with a **non-NULL `school_id`** (observed: Principal role_id=1 had `school_id=5`), so the `school_id IS NULL` filter matched nothing and NO `role_permissions` rows were ever inserted — so every JWT came back with an empty `perm[]` and the gated UI hid everything except Dashboard (the one route with no permission requirement). This was a latent bug from day one; Phase 50 surfaced it because nothing consumed the permissions array before. The login `schoolId` resolution itself was fine (`users.school_id` if set, else first `user_roles.school_id`) and matched the assignment — the gap was purely the missing `role_permissions` rows. Fix: both `Database/seed_tenant_data.sql` and `Database/admin_permissions_migration.sql` now match roles by `role_name` ONLY (dropped `AND r.school_id IS NULL`), so every same-named role row gets mapped regardless of its `school_id`. `admin_permissions_migration.sql` was also upgraded from a new-perms-only patch into a **complete role-permission repair**: Principal → ALL active permissions (CROSS JOIN), and Admin Clerk / Fee Clerk / Class Teacher / Librarian → their **full intended sets (original 24 subset + new Phase-50 codes)**, all idempotent (`NOT EXISTS` guard) — so re-running it repairs an existing broken DB in one pass. Diagnostic that confirmed it: `SELECT ... FROM user_roles ur JOIN role_permissions rp ... WHERE ur.user_id=@uid AND ur.school_id=@schoolId` returned no rows while `user_roles` showed Principal at school_id=5. Per-DB hotfix (no redeploy): `INSERT INTO role_permissions SELECT DISTINCT ur.role_id, p.permission_id FROM user_roles ur CROSS JOIN permissions p WHERE ur.user_id=(SELECT user_id FROM users WHERE username='admin') AND p.status='Active' AND NOT EXISTS(...)`, then re-login. Note: the `permissions.status='Active'` filter in `GetUserPermissions` is fine — `permissions.status` defaults to `'Active'`, so seeded/migrated perms (which don't set status) are Active.
Phase 52 ✅  Migration tool — duplicate-key crash fix + academic_years status mapping — (1) **Duplicate-key crash:** `TermsMigrator`, `FeePeriodsMigrator`, and `SubjectsMigrator` built their `academic_years` lookup (`academic_year` string → `academic_year_id`) with LINQ `.ToDictionary(...)`, which throws `System.ArgumentException: An item with the same key has already been added` when the dest `academic_years` table has duplicate `academic_year` values for the school (caused by earlier re-runs without cleanup). Because the dictionary is built BEFORE any source rows are read, the migrator crashed even when the source had zero rows (e.g. `subjects` failing despite `SAS_Subjects`/`SAS_StuSubjects` being empty). Fixed by replacing the `ToDictionary` in all three with the same defensive `new Dictionary<string,int>(OrdinalIgnoreCase)` + `foreach` + `ContainsKey` loop already used by the larger migrators (`FeeStructuresMigrator`, `StudentsMigrator`, `FeeReceiptsMigrator`, `BusFeeStructuresMigrator`) — keeps the FIRST id on a duplicate and logs a `Warning: duplicate academic_year '...'` instead of throwing. Note: this only makes the lookup tolerant; duplicate `academic_year` rows remain a data problem (downstream FKs become ambiguous) — the real reset is `Cleanup=true` / `Database/cleanup_migration_data.sql` then re-migrate. Detect with `SELECT academic_year, COUNT(*) FROM academic_years WHERE school_id=@id GROUP BY academic_year HAVING COUNT(*)>1`. (2) **academic_years status mapping:** `AcademicYearsMigrator.MapStatus` maps legacy `SAS_AcdYear.AcdStatus` A→`Active`, D→`Inactive` (unchanged), but the default/unknown case changed from `Inactive` to `Active` (a year with a missing/odd status is now treated as Active rather than silently hidden). (3) Also fixed (Phase 51 follow-up) stray unfiltered `delete from homework`/`delete from subjects` lines in `cleanup_migration_data.sql` → now filter by `@SchoolId` with aligned `@@ROWCOUNT` prints.
Phase 51 ✅  Migration tool — fee_periods migrator — added `FeePeriodsMigrator` to `AscentMigration` to migrate monthly fee periods from the legacy VB6 DB. Source: `SELECT * FROM SAS_TermMonthData WHERE Descrpt = 'Monthly'` (the same legacy table `TermsMigrator` reads with `LIKE '%Term%'` — monthly rows are the `Descrpt='Monthly'` slice). Dest: `fee_periods` (tenant table 17). Reuses the existing `LegacyTerm` POCO (columns identical — no new model). Column mapping: `TrmMnthData`→`period_label`; `month_no` extracted from the label (`"October-2026"`→10; supports full month name "MMMM", 3-letter "MMM", and numeric, via `TryExtractMonthNo` splitting on `-`/`/`/space and `DateTime.TryParseExact`); `YearNam`→`year_no` (smallint; `"2026-27"`→2026 by taking the part before `-`, **fallback** to the trailing year embedded in the label via `TryParseYearNo`); `OrdrNo`→`sequence_no`; `TraStatus` A→`Active`/D→`Deleted`/else→`Active` (note: differs from `TermsMigrator` which maps D→Inactive — intentional, per the supplied mapping); `AcdYear`→`academic_year_id` via `academic_years.academic_year` lookup (school-scoped), **fallback** to `ResolveDefaultAcademicYearIdAsync` on miss (NOT null, so monthly fee structures stay linkable) — same pattern as `TermsMigrator`; `school_id`=`Config.SchoolId`, `created_by`="migration", `created_at`=`DateTime.Now`. Truncate mode deletes existing `fee_periods` for the school; Skip mode dedups on composite key `period_label|academic_year_id`. Rows with empty label / unrecognizable month / undeterminable year are recorded as per-row errors and skipped (non-fatal). Registered in `MigrationRunner.BuildMigrators` **after `TermsMigrator`, before `FeeStructuresMigrator`** (academic_years already migrated; runs before anything that could reference `fee_period_id`). Cleanup: `fee_periods` added to `MigrationRunner.CleanupAsync` AND `Database/cleanup_migration_data.sql` right after `fee_structures` (deleted after both `fee_receipt_items` and `fee_structures`, which carry `fee_period_id` FKs, so the reverse-FK-order transaction won't violate a constraint). Also fixed stray unfiltered `delete from homework`/`delete from subjects` lines in `cleanup_migration_data.sql` to filter by `@SchoolId` with proper `@@ROWCOUNT` prints. Idempotent via `_migration_log`; force re-run with `<add key="fee_periods.force" value="true" />`.
Phase 50 ✅  Role-based page access — Phase A (frontend UX gating) only — RBAC data model, JWT `perm[]` claims, and the `RequirePermissionAttribute` filter all already existed but were never wired up: every authenticated school user could reach every route and see every nav item, and **no backend endpoint enforces any permission**. Phase A adds frontend page/nav gating; **Phase B (backend endpoint enforcement via `[RequirePermission]` on controller actions) is NOT yet implemented — to be done later**. Added 15 new permission codes across 8 new modules (the original 24 codes covered only Student/Fee/Attendance/Marks/Library/Transport/Hostel, leaving Master Data, Homework, Announcements, Events, Staff, Reports, SMS, Settings, and User Management ungated): `MASTER_DATA.VIEW/MANAGE`, `HOMEWORK.VIEW/MANAGE`, `ANNOUNCEMENT.VIEW/MANAGE`, `EVENTS.VIEW/MANAGE`, `STAFF.VIEW/MANAGE`, `REPORTS.VIEW`, `SMS.SEND`, `SETTINGS.MANAGE`, `USER_MGMT.MANAGE`. **Frontend (`ascent-school-app`):** new `src/config/permissions.js` is the single source of truth — `P` (all permission-code constants, mirrors backend `PermissionCodes.cs`) + `PATH_PERM` (every absolute route path → required permission; `null` = always allowed, e.g. Dashboard) + `canAccessPath()` helper; new `src/pages/NoAccess.jsx` (AntD `Result` 403 + "Back to Dashboard"); `App.jsx` gains a `Protected({path, children})` wrapper that looks up `PATH_PERM[path]` and renders `<NoAccess/>` when `hasPermission()` is false — all 32 protected routes wrapped (Dashboard `index` left open so users with few permissions still land somewhere); `AppLayout.jsx` gains `filterNav(items, hasPermission)` — hides nav leaves the user lacks and drops parent menus (Students/Fees/Staff/Settings) when all children are filtered out; `Menu items` + `openKeys` now use the filtered `navItems`. The `hasPermission()` helper in `authStore.js` (previously dead code) is now the basis for both the route guard and nav filter. **Backend constants:** `PermissionCodes.cs` gains nested classes `MasterData/Homework/Announcement/Events/Staff/Reports/Sms/Settings/UserManagement` (ready for Phase B; not yet referenced by any controller). **DB:** `tenant_tables.sql` seeds the 15 new permissions for new onboarding; `seed_tenant_data.sql` maps them to default roles (Principal auto-gets all via existing `CROSS JOIN permissions`; Admin Clerk gains master-data/content/reports/SMS/staff-view; Fee Clerk gains reports; Class Teacher gains homework/announcements/events/master/reports); new idempotent migration `Database/admin_permissions_migration.sql` inserts the perms + maps them to the 4 default roles on existing tenant DBs. **Go-live actions:** (1) run `admin_permissions_migration.sql` on every existing `ascent_group_{N}` tenant DB — WITHOUT it the new pages gate on codes nobody holds and vanish for everyone including admins; (2) users must LOG OUT + LOG IN so the new perms land in their JWT; (3) rebuild/deploy the school app. **Important:** frontend gating is UX only, NOT a security boundary — the API is still open to any authenticated token until Phase B decorates the controllers. Custom roles created via the Roles UI need the new perms assigned by an admin (they appear automatically in the role permission drawer after the migration). VB6 API-key integration unaffected (granted all permissions in `JwtAuthenticationFilter`).
Phase 49 ✅  CHAK IN legal pages (generic app) — the generic single-app build is published on the Play Store as **CHAK IN** (applicationId `in.educare.app`) and needs its own Play-required Privacy Policy + Account Deletion URLs, separate from the per-flavor pages. Created a `chakin/` folder at repo root with `privacy-policy.html` + `account-deletion.html`, mirroring the existing per-flavor pattern (e.g. `stannsasf/`, `holyspiritjm/`, `depaul/` — each school folder has both files served at `https://edu-care.in/{folder}/...`, cross-linked). Kept consistent with the existing flavor pages: same HTML/CSS, developer "Sesh Rao", contact `support@edu-care.in`, Razorpay + smslogin.mobi third-party sections, 90-day retention, page cross-links. Adapted for the multi-school nature of CHAK IN (vs single-school flavors): app name "CHAK IN" throughout; single-school references generalized to "your school"; added a "School code" item to data-collected list + a "connect you to your school using the school code" usage point; account-deletion form gained a "School name" field (included in the generated mailto body). URLs after upload: `https://edu-care.in/chakin/privacy-policy.html` + `https://edu-care.in/chakin/account-deletion.html` — set these in the CHAK IN Play Console listing (Privacy Policy URL + Data safety deletion link). Note: per-flavor pages use developer "Sesh Rao" while the root `privacy-policy.html` ("Ascent Schools") uses company "Ascent Info Solutions"; CHAK IN pages follow the "Sesh Rao" per-flavor convention (revisit if the Play listing is under the company name).
Phase 48 ✅  Android generic-flavor logo on school-code + login screens — for the `ascent` (generic) flavor, a CHAK logo was added via Android Studio Image Asset, which generated launcher icons (`ic_launcher`/`ic_launcher_round`/`ic_launcher_foreground` + adaptive-icon XML) under `app/src/ascent/res/mipmap-*` — overriding only the generic app (baked flavors stannsasf/depaulemyv/holyspiritjm untouched) and replacing the placeholder indigo "A" from Phase 45. **Login screen (`SmsAuthScreen`)** already displayed a logo (school branding logo from API `tokenStore.brandingLogoUrl`, falling back to `getApplicationIcon()`), so the new flavor icon automatically became its fallback — no change needed. **School-code screen (`SchoolCodeScreen`)** had no logo (it appears before any school is selected, so only a generic app-bundled logo is possible there); added an `AsyncImage` at the top of the card showing `context.packageManager.getApplicationIcon(context.packageName)` (Coil), clipped to a `CircleShape` at 96dp, with `Spacer(16.dp)` before the "CHAK IN" title (new imports: `CircleShape`, `clip`, `LocalContext`, `coil.compose.AsyncImage`). Used `getApplicationIcon()` deliberately, NOT `painterResource(R.mipmap.ic_launcher_round)` — Image Asset now generates an adaptive-icon XML for `ic_launcher_round` too, so `painterResource` on it would crash on API 26+ (extends decisions #48/#49: the safe way to show the launcher icon inside Compose is Coil + `getApplicationIcon()`). Net: school-code screen always shows the generic CHAK logo; login screen shows the selected school's branding logo (CHAK fallback). Caveat: CHAK is a square text logo so the circle clip may crop edges slightly — switch to no-clip/rounded-rect if it looks tight.
Phase 47 ✅  Android Gradle release signing — release builds were never wired to a signing config, so the *Generate Signed Bundle / APK* wizard showed all `*Debug` variants greyed out (debuggable builds can't be release-signed — that's by design; pick a `*Release` variant) and required manually re-entering the keystore each time. A keystore already existed at repo root `keystore/ascent-keystore.jks` (alias `key0`, store+key password identical, valid until 2051) but wasn't connected to Gradle. Added `AscentSchoolsMobile/keystore.properties` (storeFile/storePassword/keyAlias/keyPassword; **gitignored** — added `keystore.properties` + `**/keystore.properties` to root `.gitignore`); `app/build.gradle.kts` loads it via `rootProject.file("keystore.properties")` + `Properties().load(FileInputStream(...))` (imports `java.io.FileInputStream` + `java.util.Properties` at file top), adds a `signingConfigs { create("release") { ... } }` block, and attaches it in `buildTypes.release` — all guarded by `if (keystorePropertiesFile.exists())` so a fresh clone without the file still builds unsigned and the wizard still works. Result: `gradlew bundleAscentRelease` (AAB) / `assembleAscentRelease` (APK) now produce a **signed** artifact automatically (verified `jar verified` on `app-ascent-release.aab`), and Android Studio's **Build → Build Bundle(s)/APK(s)** signs without the wizard. Same for other flavors: `bundle{Stannsasf,Depaulemyv,Holyspiritjm,Demo}Release`. **Security caveat (not yet addressed — user's call):** `keystore/ascent-keystore.jks` AND `keystore/strong pwd.txt` are committed to git; recommend `git rm --cached` both + gitignore `keystore/` and keep a private backup (losing the key = can never update the Play Store listing).
Phase 46 ✅  Android "Change School" on home page — the generic single-app build previously only exposed "Change School" on the login screen (`SmsAuthScreen`, pre-login); after login the home `⋮` menu had just "Switch Child" + "Logout", so switching school post-login required a manual Logout first. Added a **"Change School"** `DropdownMenuItem` (School icon) to the `HomeScreen` overflow menu, between "Switch Child" and "Logout". `HomeScreen.kt` gains an `onChangeSchool: (() -> Unit)? = null` param; the menu item renders only when non-null. `MainActivity.kt` wires it **only for the generic flavor** (`isGenericApp`; null for baked flavors so it never shows there — their school is fixed). Because the home page is post-login (session is scoped to the current school's group), the callback ends the session: `authRepo.logoutParent()` (revokes refresh token) + `tokenStore.clear()` (session) + `tokenStore.clearSchool()` (school code + cached branding) + reset `feeViewModel`/`isLoggedIn`/`userType` + `needsSchool = true` → routes back to `SchoolCodeScreen`. No confirmation dialog (per decision). Contrast with login-screen "Change School" which runs pre-login (no session) and only does `clearSchool()` + `needsSchool=true`.
Phase 45 ✅  Android build-error + launcher-icon cleanup — (1) **Build fix:** `assembleDebug` failed on `mergeDemoDebugResources` because a stray `app/src/Demo/res/mipmap-xxhdpi/StAnnsAsf.png` had been dropped into the Demo flavor — Android resource filenames must be lowercase `a-z`/`0-9`/`_`, so the uppercase name aborted the resource merge; deleted the stray file. (2) **Wrong-logo fix:** the generic `ascent` flavor (applicationId `in.educare.app`, SCHOOL_CODE blank) inherits icons from `src/main`, and `src/main` still held DePaul's "OPES" launcher icons (`ic_launcher*.webp` + adaptive foreground) — so the generic "Ascent Schools" app showed the DePaul logo on the home screen AND on `SmsAuthScreen` (baked flavors use `getApplicationIcon()` as the login logo when no API branding is loaded yet). The Demo flavor was likewise wrongly baked with St. Ann's "Light of Life" logo. Replaced `src/main` icons with a neutral generic default (indigo `#3F51B5` background + white "A"): regenerated `ic_launcher.png`/`ic_launcher_round.png`/`ic_launcher_foreground.png` for all 5 densities (mdpi→xxxhdpi) via a PowerShell `System.Drawing` script (no ImageMagick/cwebp available; PNG is valid in mipmap folders), deleted the DePaul `.webp` files, and set `drawable/ic_launcher_background.xml` to a solid `#3F51B5` vector. minSdk=24 so bitmap fallbacks are still required for API 24/25 (adaptive icons only apply on 26+). Demo flavor's icon overrides (`mipmap-*`, `mipmap-anydpi-v26`, `drawable/ic_launcher*`) were deleted so it inherits the new generic `src/main` icon. The 3 published flavors (stannsasf, depaulemyv, holyspiritjm) keep their own correct logos untouched. **Local debugging note:** debug build's `API_BASE_URL = http://localhost:62845/`; on a physical device `localhost`/`127.0.0.1` is the phone itself, so run `adb reverse tcp:62845 tcp:62845` (cleared on unplug/reboot/adb restart — re-run each session) to tunnel to the host's IIS Express. Hitting `http://localhost:62845/` in a browser returns 403 Forbidden — that's normal (no route at `/`); test a real endpoint like `/branding` instead. Placeholder icon only — replace via Android Studio Image Asset (target `main`) when a real Ascent logo exists. No gradlew wrapper in repo; build with Studio JDK + the cached Gradle 8.7 dist, or from Android Studio (`Ctrl+F9` Make, `Shift+F10` Run; pick variant in Build Variants panel).
Phase 44 ✅  Single-app (multi-school) Android — one shared "Ascent Schools" app for NEW schools (parent enters a 4-digit code shared by the school admin); the 3 published flavor apps (stannsasf, depaulemyv, holyspiritjm) stay baked/unchanged. School resolved at runtime → `X-School-Code`; in-app branding loaded dynamically from `/branding`; launcher icon+name stay generic (single-app constraint). Decisions: login_code set manually via control app, 4-digit numeric, flavor apps stay baked, school selection persists with "Change School". Build via Android Studio (no gradlew wrapper in repo): build variant `ascentRelease`. Build checklist:
  - [x] Part 1 — Backend + control app: `school_groups.login_code` (master_tables.sql + `school_login_code_migration.sql`, demo seeded `1000`); `SchoolGroupDto`/`UpdateSchoolGroupRequest` gain `LoginCode`; `SchoolGroupRepository` GetAll/GetById/Update + `GetByLoginCode`→`SchoolByCodeDto`; `GET /mobile/auth/school-by-code?code=` (AllowAnonymous) → {schoolCode=subdomain, name}; control app group-detail Edit modal has 4-digit "App Login Code" field
  - [x] Part 2a — Gradle: generic `ascent` flavor (applicationId in.educare.app, SCHOOL_CODE="", app_name "Ascent Schools", generic icon from src/main)
  - [x] Part 2b — `TokenStore.schoolCode`/`brandingName`/`brandingLogoUrl` (survive logout via updated `clear()`) + `clearSchool()`; `RetrofitClient.mediaBaseUrl`; interceptor sends `X-School-Code`+`X-Subdomain` = `BuildConfig.SCHOOL_CODE.ifBlank { tokenStore.schoolCode }`
  - [x] Part 2c — `ApiService.getSchoolByCode` + `getBranding`; `SchoolByCodeDto`/`BrandingDto`; `AuthRepository.getSchoolByCode` + `loadBranding` (caches name + absolute logo URL via `resolveMedia`)
  - [x] Part 2d — `ui/school/SchoolCodeScreen` + `SchoolCodeViewModel` (Idle/Loading/Confirm/Done/Error; enter 4-digit → resolve → confirm school name → store subdomain + load branding)
  - [x] Part 2e — `SmsAuthScreen` shows API logo (`brandingLogoUrl` via Coil) + `brandingName` for the generic flavor (baked flavors unchanged); "Change School" TextButton (generic only) via new `onChangeSchool` param
  - [x] Part 2f — `MainActivity` gate: `isGenericApp = SCHOOL_CODE.isBlank()`, `needsSchool` state renders `SchoolCodeScreen` before auth when no stored schoolCode; `onChangeSchool` = `clearSchool()` + `needsSchool=true`
  - [x] Part 3 — Reviewer path: demo `login_code='1000'` seeded in migration → demo school → `9999999999`/`123456` OTP bypass. **Go-live action: add "School code 1000, mobile 9999999999, OTP 123456" to Play/App Store review notes.**
Phase 43 ✅  Bulk receipt import — year-scoped term/period resolution — `FeeRepository.BulkImportReceipts` (used by web "Legacy Receipt Import" + sync tool Export Receipts) previously resolved `term_id`/`fee_period_id` by **name only**, so a term name like "Term 1" that repeats across academic years always matched the first year's id (wrong term on receipts for other years → outstanding wouldn't reconcile against that year's fee_structures). Fixed: `terms` and `fee_periods` lookups now keyed `name|academic_year_id` via new `ToCIByYear` helper + `YearKey(name, yearId)` (uses the group's resolved `yearId`); queries select `academic_year_id AS YearId`; `FeeBulkLookup` gained a `YearId` field. `fee_types` deliberately left name-only (per requirement — fee type names don't need year-scoping here). Routes/hostels/payment modes stay name-only (not year-scoped). Backend-only change — deploy for sync tool Export Receipts to assign the correct year's term/period.
Phase 42 ✅  Receipt print (school app) + school-name on receipts (both portals) — `ReceiptsPage.jsx` gains a **Print** option (button in row actions + in the detail-drawer header) rendering a print-only receipt that mirrors the parent portal's success page; `handlePrint(receiptId, existing?)` loads detail via `GET /school/fees/receipts/{id}` (or reuses the open drawer's receipt) into a hidden `ReceiptPrintArea`, then `window.print()` after a 150ms paint delay; print CSS hides `body *` and shows only `.receipt-print-area` (absolute top-left); `ReceiptPrintArea` lays out a branding header (logo + `branding.displayName` + "Fee Receipt"), receipt no/date, student+payment details, fee-items table (Fee Type/Term/Amount/Concession/Net + Total), `branding.receiptFooterText`, "Collected by", and a red CANCELLED marker + reason for cancelled receipts; **school-name fix:** the parent portal `PaymentSuccessPage.jsx` previously showed no school name — added a centred branding header (logo + `branding.displayName` title + "Fee Receipt"); both apps source the name from `branding.displayName`, which the backend `/branding` always returns non-null via `ISNULL(school_branding.display_name, ISNULL(group_branding.display_name, sg.group_name))` (falls back to the group/society name); no backend change
Phase 41 ✅  AscentSyncTool (.NET 4.8 WinForms EOD sync GUI) + bulk-student upsert — standalone `AscentSyncTool/` WinForms app (SDK-style net48; Dapper + System.Data.SqlClient + Newtonsoft.Json; `System.Net.Http` needs an explicit `<Reference>` on net48) — a GUI front-end for the Phase 39 EOD sync, reusing legacy table shapes from `AscentMigration` and the integration bulk/list endpoints; `TabControl` with 3 tabs, each a reusable `SyncTab` UserControl (From/To `DateTimePicker`s, Show/Save/Cancel buttons, read-only `DataGridView`, status label); auth via `X-Api-Key` (Phase 35); App.config holds `LegacyDb` connection string + `ApiBaseUrl` + `ApiKey` + batch sizes; **Tab 1 Export Receipts** — `SAS_FeeReceipts` by `FeeDat` range → `POST /school/fees/receipts/bulk` (batched 200, dedup by LegacyReceiptNo, source=legacy); **Tab 2 Export Students** — `SAS_StudentMaster` by `CrtDat` range → `POST /school/students/bulk` with `Upsert=true` (batched 100); **Tab 3 Download Receipts** — `GET /school/fees/receipts?source=webapp` by `created_at` range → per-receipt `GET /receipts/{id}` for line items → insert one `SAS_FeeReceipts` row per fee-type item, dedup by `FeeReciptID`; the legacy insert also fills `ClassNam` (from receipt API ClassName) and resolves `FeeMasterID`/`FeeID` via inline subqueries (FeeMasterID ← `SAS_FeeMaster` by ClassNam+NofPayments[=term]+FeeTyp+AcdYear, FeeMasterStat='A'; FeeID ← `SAS_FeeTypes.FeeTypID` by FeeTyp, FeeTypStatus='A'); files: `MainForm.cs`, `SyncTab.cs`, `Config/AppSettings.cs`, `Api/AscentApiClient.cs`, `Data/LegacyDb.cs`, `Models/` (standalone copied POCOs), `Mappers/Mappers.cs` (legacy↔bulk; receipt-detail item→legacy); **backend changes:** (1) `BulkStudentImportRequest.Upsert` (default false — web UI keeps strict insert-only) + `BulkImportResult.Updated` counter; `StudentRepository.BulkCreate` upsert path matches existing rows by `admission_no + academic_year_id + school_id`, UPDATEs personal/contact fields only (student_name, gender, date_of_birth, father/mother name, father/mother mobile, aadhar_no, caste, caste_code, religion, mother_tongue — NOT class/section/roll/fee_category/status/academic_year which are promotion/lifecycle-managed), inserts otherwise reusing the student's existing student_unique_id (subquery: latest-year unique_id for that admission_no, else MAX+1) so cross-year linkage holds; `Failed = Total - Imported - Updated`; (2) `FeeRepository.GetReceipts` + `FeeController` gain `createdBefore` DateTime? filter (mirrors createdAfter) so Tab 3 can range-filter created_at
Phase 40 ✅  Android in-app child switching — parents with >1 child can switch the active child without logging out/re-OTP (previously the only way was logout → login → re-select). No backend change — the backend already supported it (`GET /mobile/auth/parent/children` + `POST /mobile/auth/parent/select-child` work with the child-context parent token since it stays `tokenType=parent`). `HomeScreen.kt` — top-bar lone Logout button replaced with an overflow (⋮) `DropdownMenu` containing "Switch Child" + "Logout"; "Switch Child" item shown only when `children.size > 1` (children loaded once on screen mount via `authRepo.getChildren()`); tapping opens an `AlertDialog` listing each child (name · class · admissionNo) with a check-mark on the current one (matched by `child.studentId == tokenStore.studentId`, current not tappable); selecting another calls existing `authRepo.selectChild(linkId)` (updates TokenStore) then fires `onChildSwitched()`; `MainActivity.kt` — parent home wrapped in `key(childEpoch) { ... }`; `onChildSwitched` bumps `childEpoch` which recreates the whole parent subtree (feeVm + all 7 tab ViewModels) so every tab reloads fresh data for the new child; `studentRepo` + API read the swapped token live so all calls carry the new child context; top-bar title re-reads new `studentName` on recreation; parent web app already had this (Switch Child header dropdown), Android was the gap
Phase 39 ✅  EOD sync between legacy VB6 and new app — `source VARCHAR(10) NULL` column added to `fee_receipts` (`'legacy'`=VB6 EOD push / `'webapp'`=school web app + parent portal + gateway); `Database/eod_sync_migration.sql` adds column + tags all existing receipts as `'webapp'`; `tenant_tables.sql` updated for new tenant DBs; `BulkReceiptRow` DTO gains `Status` ('A'/'D'→Active/Cancelled) and `CancelReason` fields; `BulkImportResult` gains `Skipped` counter; `BulkImportReceipts` overhauled: uses `LegacyReceiptNo` directly as `receipt_no` when provided (no auto-generation for legacy data), dedup check `IF EXISTS receipt_no+school_id` → skip with `Skipped++` (idempotent re-runs safe), respects `Status` field (inserts Cancelled receipts with `cancel_reason`/`cancelled_by='legacy'`/`cancelled_at=GETDATE()`), sets `source='legacy'` on every bulk-imported receipt, `Failed = Total - Imported - Skipped`; `CollectFee` sets `source='webapp'` (covers school web app, parent portal, and gateway webhook — all three call this method); `GetReceipts` + `FeeController` gain `?source=` filter — legacy EOD pull calls `?createdAfter=&source=webapp` to get only webapp receipts without sync-looping its own data back; VB6 integration files updated: `AscentAPI.bas` — `GetAcademicYears` now calls `?activeOnly=true`; `GetReceipts` gains `sCreatedAfter` + `sSource` optional params; `BulkImportReceipts` comment fully rewritten with all field names mapped to SAS columns; new `BuildReceiptRow` helper constructs one receipt row with all fields including `Status`/`CancelReason`; `AscentAPI_Examples.bas` — Example 9 (historical import) rewritten to use `BuildReceiptRow` with correct `SAS_FeeReceipts` column names and `nSkipped` counter; Example 10 (EOD pull) URL fixed with `&source=webapp`; `"Amount"` → `"PaidAmount"` bug fixed; new Example 14 (`EodPushReceiptsExample`) pushes today's legacy receipts in batches of 200; `AppLayout.jsx` — "Dashboard" added as first nav item (`HomeOutlined` icon, key `'/'`) so users can navigate back to dashboard from any page; branding `<img>` tags in `AppLayout.jsx` and `LoginPage.jsx` gain `onError` handler that hides the element — prevents `ERR_BLOCKED_BY_ORB` when logo file doesn't exist on server (Chrome blocks the HTML 404 response used as image source)
Phase 38 ✅  Active academic year filtering + auto-selection — `GET /school/master/academic-years` extended with optional `?activeOnly=true` query param (filters `WHERE status = 'Active'`); `AcademicYearDto` gains `IsCurrent bool` — computed server-side in C# by marking the first Active year in DESC `academic_year_id` order (most recently created Active year); no DB column needed; `MasterDataController.GetAcademicYears` accepts `[FromUri] bool activeOnly = false`; all 24 frontend pages that use academic year dropdowns updated: call `?activeOnly=true` and auto-select the year with `isCurrent === true` on mount; exam-related pages (`ExamHallTicketReport`, `ExamToppersReport`, `FailedStudentsReport`) and `FeePeriodsTab` call their `onYearChange` handler on auto-select so dependent dropdowns (exam types, fee periods) populate automatically; `StudentFormPage` auto-selects current year only on create (not edit); `PromoteStudentsPage` auto-selects current year for From-year only (To-year stays empty); `FeeTypesTab`/`TermsTab`/`FeeCategoriesTab`/`SubjectsTab` filter to active years in modal form dropdowns (no list filter); `AcademicYearsTab` left unchanged (management CRUD must show all years including inactive); `FeeCollectionBase` unchanged (already uses `isCurrent` from fee endpoint); parent web app unchanged (no year picker — uses cross-year mobile fee endpoint); Android mobile unchanged (no year picker UI); `StudentsPage` year dropdown has no "All Years" option (removed — current year is always pre-selected); fee collection student search (`FeeCollectionBase.jsx`) passes `?latestOnly=true` to `GET /school/students` so multi-year students appear only once (latest academic year row returned via ROW_NUMBER partition); dashboard `TotalActiveStudents` query scoped to current academic year via subquery `SELECT TOP 1 academic_year_id FROM academic_years WHERE status='Active' ORDER BY academic_year_id DESC` (same Option A logic — counts only active students in the most recently created Active year); `FeeRepository.GetCrossYearFeeSummary` student-rows query extended with `AND ISNULL(ay.status, 'Active') = 'Active'` — mobile and parent portal outstanding now only shows Active academic years (Inactive years silently excluded; `ISNULL` guard retains rows with no matching academic year record)
Phase 37 ✅  VB6 legacy data migration tool — `AscentMigration/` standalone .NET 4.8 console app that reads from a legacy VB6 SQL Server DB (source) and writes into an `ascent_group_{N}` tenant DB (dest); configured entirely via `App.config` (Source/Dest connection strings, SchoolId, DefaultAcademicYearId, Modules=all|comma-list, DryRun, BatchSize, Cleanup, per-table mode and force flags); `DefaultAcademicYearId` is the fallback academic_year_id for migrators whose legacy source has no academic-year column (fee_categories, fee_types — used unconditionally) or whose AcdYear lookup misses (terms, fee_structures — used as fallback); `0` (default) = AUTO-resolve at runtime via `BaseMigrator.ResolveDefaultAcademicYearIdAsync` (`SELECT TOP 1 academic_year_id FROM academic_years WHERE school_id ORDER BY academic_year_id DESC` — the latest year AcademicYearsMigrator just inserted), `>0` = manual override; auto-resolve means a `Cleanup=true` re-migration that re-inserts academic_years with new IDENTITY values picks up the correct new ID without editing App.config; `MigrationRunner` runs migrators in FK-dependency order, auto-creates `_migration_log` table in dest, is fully idempotent (skips already-completed migrators, re-runs if `{name}.force=true` in App.config); each migrator inherits `BaseMigrator` (OpenSource/OpenDest, Log, LogProgress); two modes per table: `Truncate` (delete existing school rows then re-insert all) or `Skip` (composite-key dedup — skip rows already present); 14 migrators in order: `AcademicYearsMigrator` (SAS_AcdYear → academic_years), `ClassGroupsMigrator` (SAS_ClassGrps → class_groups), `FeeCategoriesMigrator` (SAS_FeeCategory → fee_categories), `ClassesMigrator` (SAS_ClassNames → classes; maps class_group via SAS_ClassGrps prefix lookup), `FeeTypesMigrator` (SAS_FeeTypes → fee_types), `TermsMigrator` (SAS_TermMonthData → terms; composite key `term_name|academic_year_id`), `FeeStructuresMigrator` (SAS_FeeMaster → fee_structures; two-step category lookup: FeaCatgID→name via SAS_FeeCategory, name→fee_category_id via dest; term key `"termName|academicYearId"`; maps status A→Active/D→Inactive), `BusRoutesMigrator` (SAS_BusRoutes → bus_routes; skip key: route_name; status A→Active/D→Inactive), `BusFeeStructuresMigrator` (SAS_BusMaster → bus_fee_structures; two-step route lookup: legacy RouteID→RouteNam via SAS_BusRoutes, RouteNam→route_id via dest bus_routes with Active-wins-on-duplicate; term key `"termName|academicYearId"`; skip key: route_id|term_id|academic_year_id), `SectionsMigrator` (no legacy sections table — reads `SELECT DISTINCT StuSect FROM SAS_StudentMaster`; inserts one row per section × per class; sequence_no resets 1..n per class; skip key: class_id|section_name), `StudentsMigrator` (SAS_StudentMaster → students; 5 preloaded lookup maps: acadYearMap, classMap, sectionMap `"section_name|class_id"`, legacyRouteNameMap RouteID→RouteNam, destRouteIdMap route_name→route_id Active-wins; ~50 mapped columns; StuFamilyChildres varchar→int via TryParse; StuUnqID float→int cast; status A→Active/D→Inactive/TC→TC/R→R/else→NULL; student_name NOT NULL — row skipped if blank; is_detained defaults to 0; StuMobile1/StuMobile2 may contain multiple numbers separated by `/` or `,` — `TakeFirstMobile` splits and takes first token capped at 10 digits for father_mobile/mother_mobile; full raw StuMobile1→spare_field_1, StuMobile2→spare_field_2; skip key: admission_no|academic_year_id), `BusesMigrator` (SAS_BussData → buses; no FK deps; BusCapacity varchar→int TryParse; skip key: bus_name), `PaymentModesMigrator` (SAS_PaymentMods → payment_modes; is_online=0 for legacy rows; appends fixed Online row mode_name='Online'/is_online=1 after legacy rows; skip key: mode_name), `FeeReceiptsMigrator` (SAS_FeeReceipts → fee_receipts + fee_receipt_items; academic year scope controlled by `fee_receipts.academicYear` App.config key: `latest`/omitted=`SELECT TOP 1 AcademicYear FROM SAS_AcdYear ORDER BY AcademicYear DESC`, `all`=no filter, or a specific year string e.g. `"2024-25"`; groups filtered rows by FeeReciptID in memory ordered by Id_new; header fields from first row, total_amount=SUM(FeeAmt) across group; 5 lookup maps: acadYearMap, studentMap `"admission_no|academic_year_id"` → StudentId+StudentUniqueId, paymentModeMap, feeTypeMap, termMap; each receipt group runs in a single transaction with rollback on error; fee_receipt_items: fee_type_id from FeeTyp, term_id from PaymentTyp+AcdYear composite key, amount=net_amount=FeeAmt, concession_amount=0; Truncate deletes items via JOIN before receipts; skip key: receipt_no); `MigrationLogger` tracks completion per migrator name + timestamps (+ `ClearAllAsync` to wipe the log); **Cleanup phase** — `<add key="Cleanup" value="true" />` in App.config triggers `MigrationRunner.CleanupAsync` before migration: deletes all migrated tables for the configured `SchoolId` in REVERSE FK-dependency order (children first: fee_receipt_items → fee_receipts → students → sections → bus_fee_structures → fee_structures → buses → bus_routes → terms → fee_types → classes → fee_categories → class_groups → academic_years → payment_modes), then clears `_migration_log` so every migrator re-runs; respects `DryRun` (prints planned deletes without executing); aborts loudly if a non-migrated table (e.g. student_attendance/marks from real usage) still references a row via FK — signals the school has live data and must not be wiped; solves the "re-migration fails on FK constraints" problem (each migrator's own forward-order Truncate can't delete a parent while children still reference it); `Database/cleanup_migration_data.sql` — manual SSMS alternative: same reverse-order deletes wrapped in `SET XACT_ABORT ON` + transaction (all-or-nothing rollback), per-table `@@ROWCOUNT` prints, single `@SchoolId` variable; `GET /school/master/terms` extended with optional `?academicYearId` filter (needed for term lookup accuracy); `Database/legacy_classdata_migration.sql` — SQL script to seed class groups (Pre-Primary/Primary/High School/General with IDENTITY_INSERT for predictable IDs) and 26 classes for a specific school (one-time manual alternative to running ClassesMigrator)
```

---

## Migration Tool Structure
```
AscentMigration/
├── AscentMigration.sln / .csproj   ← .NET 4.8 console app; Dapper + System.Data.SqlClient
├── Program.cs                       ← entry point; loads config, prompts Enter, runs MigrationRunner
├── MigrationRunner.cs               ← runs migrators in FK-dependency order; auto-creates _migration_log; idempotent (skip already-done, force re-run via App.config flag)
├── MigrationLogger.cs               ← async; EnsureLogTable / IsCompleted / WriteAsync / ClearAsync / PrintLog against dest DB
├── MigrationResult.cs               ← Total / Migrated / Skipped / ErrorCount / Errors list; Print() method
├── Config/
│   ├── MigrationConfig.cs           ← loads App.config: Source/Dest CS, SchoolId, DefaultAcademicYearId, Modules, DryRun, BatchSize; ShouldRun/IsForced/GetTableMode/GetOptionalSetting helpers
│   └── MigrationMode.cs             ← enum Truncate | Skip
├── Models/Legacy/                   ← POCO classes matching VB6 DB columns (LegacyAcademicYear, LegacyClass, LegacyClassGroup, LegacyFeeCategory, LegacyFeeMaster, LegacyFeeType, LegacyTerm, LegacyBusRoute, LegacyBusMaster, LegacyStudent, LegacyBus, LegacyPaymentMode, LegacyFeeReceipt, LegacyExamType, LegacyMarksGrade, LegacyExamMaster)
└── Migrators/
    ├── IMigrator.cs                  ← interface: Name + RunAsync()
    ├── BaseMigrator.cs               ← abstract; OpenSource/OpenDest; Log/LogProgress helpers
    ├── AcademicYearsMigrator.cs      ← SAS_AcdYear → academic_years (no FK deps — runs first)
    ├── ClassGroupsMigrator.cs        ← SAS_ClassGrps → class_groups (no FK deps)
    ├── FeeCategoriesMigrator.cs      ← SAS_FeeCategory → fee_categories
    ├── ClassesMigrator.cs            ← SAS_ClassNames → classes (depends on class_groups)
    ├── FeeTypesMigrator.cs           ← SAS_FeeTypes → fee_types (depends on academic_years)
    ├── TermsMigrator.cs              ← SAS_TermMonthData → terms (depends on academic_years; composite key term_name|academic_year_id)
    ├── ExamTypesMigrator.cs          ← SAS_ExamNam → exam_types (depends on academic_years; runs after SubjectsMigrator); DISTINCT ExamNam/acdyear/examremarks/examstatus; ExamNam→exam_type_name, acdyear lookup (fallback default year)→academic_year_id, examremarks→display_order (int.TryParse, NULL if bad), examstatus A→Active/D→Inactive/else→Active; composite skip key exam_type_name|academic_year_id (per-year dedup)
    ├── GradeTypesMigrator.cs         ← SAS_MarksGrade → grade_types (depends on subjects; runs after ExamTypesMigrator); two-step subject lookup (legacy SubjID→SAS_Subjects.SubjectNam→dest subjects.subject_id, latest-year on name collision, NULL on miss); GradName→grade_name, MaxMrks/MinMrks→max_marks/min_marks, Grad→grade, Rrmrks→remarks, TraStatus A→Active/D→Inactive/else→Active; composite skip key grade_name|subject_id|grade|min_marks|max_marks
    ├── ExamMasterMigrator.cs         ← SAS_ExamNam (full rows) → exam_master (depends on exam_types, classes, academic_years, subjects, grade_types; runs after GradeTypesMigrator); lookups: ExamNam→exam_type_id (name|year), ClasId→class_id (2-step via SAS_ClassNames), AcdYear→academic_year_id, SubjectID→subject_id (2-step via SAS_Subjects, name|year), GradeTyp→grade_type_id (MrksGradId→GradName, matched grade_name|subject_name — grade_types has no class_id); marks/remarks/category direct, ExamDatTim→exam_date, ExamStatus A→Active/D→Inactive; NULL+error on any lookup miss; composite skip key exam_type_id|class_id|subject_id|academic_year_id|exam_category
    ├── FeePeriodsMigrator.cs         ← SAS_TermMonthData (Descrpt='Monthly') → fee_periods (depends on academic_years; runs after TermsMigrator, before FeeStructuresMigrator); TrmMnthData→period_label, month extracted from label ("October-2026"→10) →month_no, YearNam (fallback label year)→year_no, OrdrNo→sequence_no, TraStatus A→Active/D→Deleted→status, AcdYear lookup (fallback default year)→academic_year_id; composite skip key period_label|academic_year_id
    ├── FeeStructuresMigrator.cs      ← SAS_FeeMaster → fee_structures (all deps); two-step category lookup; status A→Active/D→Inactive
    ├── BusRoutesMigrator.cs          ← SAS_BusRoutes → bus_routes (no FK deps); skip key: route_name; status A→Active/D→Inactive
    ├── BusFeeStructuresMigrator.cs   ← SAS_BusMaster → bus_fee_structures (depends on bus_routes, academic_years, terms); two-step route lookup (RouteID→RouteNam via source, RouteNam→route_id via dest, Active wins on dup); skip key: route_id|term_id|academic_year_id
    ├── SectionsMigrator.cs           ← no legacy sections table; reads DISTINCT StuSect from SAS_StudentMaster; inserts section × class cross-product; sequence_no resets 1..n per class; skip key: class_id|section_name
    ├── StudentsMigrator.cs           ← SAS_StudentMaster → students (depends on academic_years, classes, sections, bus_routes); 5 lookup maps; ~50 mapped columns; StuFamilyChildres varchar→int TryParse; StuUnqID float→int; status A→Active/D→Inactive/TC→TC/R→R/else→NULL; is_detained=0; TakeFirstMobile splits StuMobile1/2 on '/',',' caps at 10 digits for father_mobile/mother_mobile; full raw stored in spare_field_1/2; skip key: admission_no|academic_year_id
    ├── BusesMigrator.cs              ← SAS_BussData → buses (no FK deps); BusCapacity varchar→int TryParse; skip key: bus_name
    ├── PaymentModesMigrator.cs       ← SAS_PaymentMods → payment_modes (no FK deps); is_online=0 for legacy rows; appends fixed Online row (is_online=1) after legacy rows; skip key: mode_name
    └── FeeReceiptsMigrator.cs        ← SAS_FeeReceipts → fee_receipts + fee_receipt_items; year scope via `fee_receipts.academicYear` App.config key (latest/all/specific year); latest resolved via `SELECT TOP 1 AcademicYear FROM SAS_AcdYear ORDER BY AcademicYear DESC`; per-receipt transaction; skip key: receipt_no
```

## Sync Tool Structure (Phase 41 — ongoing EOD sync GUI)
```
AscentSyncTool/                 (.NET 4.8 WinForms; Dapper + System.Data.SqlClient + Newtonsoft.Json; net48 needs explicit <Reference Include="System.Net.Http" />)
├── App.config                  LegacyDb connection string + ApiBaseUrl + ApiKey + ReceiptBatchSize/StudentBatchSize
├── Program.cs                  STAThread Application.Run(new MainForm())
├── MainForm.cs                 `const string Version` (title bar + big navy header label); TabControl (3 tabs); enables ShowAcademicYearFilter on the 2 legacy tabs; Load → GetAcademicYears() populates both (latest pre-selected, try/catch); wires each SyncTab's OnShow/OnSave (legacy OnShow passes SelectedAcademicYear); batching + result dialogs
├── SyncTab.cs                  reusable UserControl: From/To DateTimePickers, optional Academic Year combo (ShowAcademicYearFilter / SelectedAcademicYear / SetAcademicYears), Show/Save/Cancel, read-only DataGridView, status label (code-only, no designer)
├── Config/AppSettings.cs       reads App.config (LegacyConnectionString, ApiBaseUrl, ApiKey, batch sizes)
├── Api/AscentApiClient.cs      HttpClient + X-Api-Key; GetWebappReceipts (createdAfter/createdBefore + source=webapp), GetReceiptDetail, BulkImportReceipts, BulkImportStudents (Upsert=true)
├── Data/LegacyDb.cs            Dapper on legacy SAS DB: GetAcademicYears (DISTINCT SAS_AcdYear.AcademicYear, DESC), GetReceipts(FeeDat range, +AcdYear), GetStudents(CrtDat range, +StuAcdYear), ReceiptExists(FeeReciptID), InsertReceipt
├── Models/                     standalone copied POCOs — LegacyModels.cs (LegacyFeeReceipt, LegacyStudent), ApiModels.cs (BulkReceiptRow, StudentBulkRow, BulkImportResult, ReceiptListItem, ReceiptDetail, ApiResponse<T>)
└── Mappers/Mappers.cs          ToBulkReceipt (legacy→export), ToBulkStudent (legacy→export, FirstMobile splits '/'',' caps 10), ToLegacyReceipt (api receipt-detail item→legacy row)
```
> **Tabs:** 1. Export Receipts (legacy `SAS_FeeReceipts` by FeeDat **+ academic year filter** → `POST /school/fees/receipts/bulk`), 2. Export Students (legacy `SAS_StudentMaster` by CrtDat **+ academic year filter** → `POST /school/students/bulk` Upsert), 3. Download Receipts (`GET /school/fees/receipts?source=webapp` by created_at → per-receipt detail → insert one legacy row per fee-type item, dedup by FeeReciptID; legacy insert fills ClassNam + resolves FeeMasterID/FeeID via SAS_FeeMaster/SAS_FeeTypes name lookups; **no academic-year filter — new-app API, not legacy**). The academic-year dropdown (Export tabs only) loads DISTINCT `SAS_AcdYear.AcademicYear` on startup, latest selected by default. Set `LegacyDb` connection string + `ApiKey` in App.config before first run.

---

## API Project Structure (current)
```
AscentSchools.sln
├── AscentSchools.API          ← Web API project (.NET 4.8)
│   ├── App_Start/
│   │   └── WebApiConfig.cs    ← CORS, JSON formatting, global filters, routes
│   ├── Controllers/
│   │   ├── BaseApiController.cs           ← response helpers only (no auth)
│   │   ├── Control/
│   │   │   ├── BaseControlController.cs   ← [ControlAuth], response helpers
│   │   │   ├── ControlAuthController.cs   ← POST /control/auth/login|refresh|logout
│   │   │   ├── SchoolGroupsController.cs  ← CRUD + school CRUD + user management; invalidates TenantConnectionFactory credential cache on group update
│   │   │   ├── SubscriptionsController.cs ← GET/PUT /control/school-groups/{id}/subscription
│   │   │   ├── ModulesController.cs       ← GET modules, GET/PUT group modules
│   │   │   └── BrandingController.cs      ← GET/PUT /control/school-groups/{id}/branding
│   │   └── School/
│   │       ├── BaseSchoolController.cs    ← [SchoolAuth], response helpers, Tenant property
│   │       ├── PublicController.cs        ← GET /branding (AllowAnonymous)
│   │       ├── SchoolAuthController.cs    ← POST /school/auth/login|refresh|logout
│   │       ├── RolesController.cs         ← CRUD roles + GET/PUT permissions per role
│   │       ├── SchoolUsersController.cs   ← CRUD users + reset-password + status
│   │       ├── MasterDataController.cs    ← [RoutePrefix("school/master")] — 10 entities (incl. sections + fee periods), 3 endpoints each
│   │       ├── StudentsController.cs      ← GET list (?bloodGroup, ?joinType, ?busRouteId, ?hostelId filters), GET by id, POST create, PUT update, POST /{id}/photo, POST /bulk, GET /promote/preview, POST /promote, PUT /{id}/block|unblock|detain|release
│   │       ├── FeeController.cs           ← [RoutePrefix("school/fees")] — structure, collect, receipts (GET ?createdAfter for sync), POST structure/bulk, POST receipts/bulk (legacy import), gateway settings, payment orders + webhook; pending-order reconciliation (GET payment-orders/pending|{id}/status|scan, POST {id}/reconcile); shared ReconcileOrderToReceipt + EvaluateStatus
│   │       ├── FeeConcessionController.cs ← [RoutePrefix("school/fees/concessions")] — GET list, POST individual (upsert), POST /bulk, DELETE /{id} (soft-cancel); validation: FeeTypeId==null && BusRouteId==null && HostelId==null → 400
│   │       ├── HostelController.cs        ← [RoutePrefix("school/hostel")] — GET/POST hostels, GET/POST fee-structure, POST fee-structure/bulk, GET/PUT students
│   │       ├── AttendanceController.cs    ← [RoutePrefix("school/attendance")] — mark by date + monthly summary; classId+sectionId both required
│   │       ├── MarksController.cs         ← [RoutePrefix("school/marks")] — exam types + marks grid + save
│   │       ├── HomeworkController.cs      ← [RoutePrefix("school/homework")] — server-paged list (classId/sectionId/assignedDate/page/pageSize), create, POST batch (daily multi-subject), update, delete
│   │       ├── AnnouncementsController.cs ← [RoutePrefix("school/announcements")] — list, create, update, delete
│   │       ├── ClassTeachersController.cs ← [RoutePrefix("school/class-teachers")] — list/assign/unassign class teachers (Master Data); drives parent→teacher message routing
│   │       ├── MessageReportsController.cs ← [RoutePrefix("school/message-reports")] — admin review of reported messages (SETTINGS.MANAGE); resolve = Removed/Reviewed
│   │       ├── ReportsController.cs       ← [RoutePrefix("school/reports")] — class-students, total-strength, absents, attendance-sheet, transport-students, study-certificate, daily-attendance-summary, monthly-attendance-sheet, attendance-register, exam-hall-ticket, toppers, failed-students, homework, staff-salary-statement, detained-students, regular-absentees
│   │       ├── SmsController.cs           ← [RoutePrefix("school/sms")] — GET recipients (by type), POST send (per-school account+template from sms_configs/sms_templates; parallel dispatch via Task.WhenAll), GET logs, GET/PUT config, POST/DELETE templates
│   │       └── SchoolSettingsController.cs ← [RoutePrefix("school/settings")] — GET (returns row or empty defaults), PUT (upserts via SchoolSettingsRepository using master DB)
│   │   └── Mobile/
│   │       ├── MobileAuthController.cs        ← student auth (backend-only) + parent request-otp (gates on student in school DB by father_mobile)/verify-otp (auto-creates parent_account + upserts child links by admission_no — updates class_name/student_id on existing links)/refresh/logout/children (scoped to current school group)/select-child (validates link.GroupId == tenant.GroupId)/link-child
│   │       ├── MobileStudentController.cs     ← profile, attendance, marks, homework, announcements (student + parent JWT)
│   │       ├── MobileFeesController.cs        ← [MobileAuth(requireChildContext:true)] GET /mobile/fees/outstanding (cross-year by category), GET /mobile/fees/gateway-config, POST /mobile/fees/payment-orders (accepts MobileParentOrderRequest; looks up student_unique_id + year student_id + online payment mode id server-side), POST /mobile/fees/payment-orders/{id}/verify (HMAC verify + receipt creation)
│   │       ├── MobileTeacherAuthController.cs ← POST /mobile/auth/teacher/login|refresh|logout (tokenType=teacher)
│   │       ├── MobileTeacherController.cs     ← [MobileTeacherAuth] GET classes, GET sections, GET/POST attendance, GET/POST homework, GET/POST announcements
│   │       ├── MobileMessagesController.cs    ← [MobileAuth(requireChildContext:true)] parent messaging (mobile/messages) — GET chat view, POST send, read, report, block, unblock
│   │       ├── MobileTeacherMessagesController.cs ← [MobileTeacherAuth] teacher messaging (mobile/teacher/messages) — thread list, GET/POST reply/read/report/block/unblock per {threadId}
│   │       ├── MobileNotificationsController.cs ← register/unregister FCM device token (parent or teacher; reads MobileContext/TeacherContext)
│   │       └── MobileAppConfigController.cs   ← [AllowAnonymous] GET /mobile/app/config — force/soft update gate (auto-prompt gated by auto_update_enabled)
│   ├── Filters/
│   │   ├── ControlAuthAttribute.cs        ← validates tokenType=control, optional role check
│   │   ├── SchoolAuthAttribute.cs         ← validates TenantContext populated (school JWT)
│   │   ├── MobileAuthAttribute.cs         ← validates MobileContext populated; requireChildContext flag for data endpoints
│   │   ├── MobileTeacherAuthAttribute.cs  ← validates TeacherContext populated (tokenType=teacher)
│   │   ├── JwtAuthenticationFilter.cs     ← global filter, validates tokens, populates TenantContext/MobileContext/TeacherContext
│   │   ├── RequireModuleAttribute.cs      ← checks group_modules + school_modules hierarchy → 403
│   │   └── RequirePermissionAttribute.cs  ← checks permissions[] in TenantContext → 403
│   ├── Helpers/
│   │   ├── JwtHelper.cs                   ← GenerateAccessToken (tokenType=school), GenerateControlAccessToken, GenerateMobileStudentToken, GenerateMobileParentToken, GenerateMobileTeacherToken
│   │   └── SmsHelper.cs                   ← SendOtp + SendSms (two overloads: global account for OTP; per-account for SMS Center). School SMS templates/keys now live in DB (sms_configs/sms_templates), not constants. OTP stays on the hardcoded global account.
│   ├── Middleware/
│   │   ├── TenantContext.cs               ← per-request state for school users
│   │   ├── MobileContext.cs               ← per-request state for mobile users (student + parent)
│   │   ├── TeacherContext.cs              ← per-request state for teacher mobile users (tokenType=teacher)
│   │   └── ControlContext.cs              ← per-request state for control users
│   └── Global.asax.cs
│
├── AscentSchools.Core         ← models, DTOs, constants
│   ├── Constants/
│   │   ├── ModuleCodes.cs
│   │   └── PermissionCodes.cs
│   ├── DTOs/
│   │   ├── Auth/              ← school app: LoginRequest (username, password, schoolId?), LoginResponse, TokenClaims
│   │   ├── Branding/          ← BrandingResponse
│   │   ├── Control/
│   │   │   ├── Auth/          ← ControlLoginRequest, ControlLoginResponse, ControlTokenClaims, ControlUserRecord
│   │   │   ├── Branding/      ← GroupBrandingDto, UpdateGroupBrandingRequest
│   │   │   ├── Modules/       ← ModuleDto, GroupModuleDto, UpdateGroupModuleRequest
│   │   │   ├── SchoolGroups/  ← SchoolGroupDto (incl. DbUsername, DbPassword), CreateSchoolGroupRequest, UpdateSchoolGroupRequest (incl. DbName, DbUsername, DbPassword)
│   │   │   ├── Schools/       ← SchoolDto, CreateSchoolRequest, UpdateSchoolRequest
│   │   │   ├── Subscriptions/ ← SubscriptionDto, UpdateSubscriptionRequest
│   │   │   └── Users/         ← TenantUserDto, TenantRoleDto, CreateTenantUserRequest, ResetPasswordRequest, SetUserStatusRequest
│   │   └── School/
│   │       ├── Auth/          ← SchoolUserRecord (internal)
│   │       ├── Rbac/          ← SchoolRoleDto, SchoolPermissionDto, CreateSchoolRoleRequest, UpdateSchoolRoleRequest, RolePermissionsRequest
│   │       ├── Master/        ← FeePeriodDtos.cs (FeePeriodDto, SaveFeePeriodRequest)
│   │       ├── Students/      ← StudentDtos.cs (StudentListDto now incl. StudentUniqueId + JoinType) + BulkImportDtos.cs (StudentBulkRow, FeeStructureBulkRow, BulkStudentImportRequest, BulkFeeStructureImportRequest, BulkImportResult, BulkRowError, PromoteStudentsRequest, PromoteStudentsResult, PromotePreviewDto)
│   │       ├── Fee/           ← GatewayDtos.cs (GatewayConfigDto, SaveGatewayConfigRequest, MobileParentOrderRequest [renamed from MobileCreateOrderRequest to avoid ambiguity with Mobile.Data version], CreatePaymentOrderRequest, CreatePaymentOrderResponse, VerifyPaymentRequest); FeeDtos.cs (CollectFeeRequest, CollectFeeItem, FeeLineItemDto, CrossYearFeeSummaryDto etc.)
│   │       ├── Transport/     ← TransportDtos.cs (BusDto, SaveBusRequest, BusRouteDto, SaveBusRouteRequest, BusFeeStructureDto, BusFeeTermDto, SaveBusFeeStructureRequest, BusFeeTermEntry, StudentTransportDto (incl. StudentUniqueId + AcademicYearId), UpdateStudentTransportRequest (incl. AcademicYearId))
│   │       └── Settings/      ← SchoolSettingsDtos.cs (SchoolSettingsDto, UpdateSchoolSettingsRequest) — 3 groups: Admission, Fee & Billing, Reports & Institution
│   └── Models/
│       └── ApiResponse.cs     ← standard envelope: { success, data, message, errors }
│
└── AscentSchools.Data         ← Dapper repositories, connection factory
    ├── ConnectionFactory/
    │   ├── IConnectionFactory.cs
    │   └── TenantConnectionFactory.cs     ← GetMasterConnection, GetTenantConnection (looks up per-DB credentials from school_groups with 15-min ConcurrentDictionary cache; falls back to TenantDb:ConnectionTemplate), ProvisionTenantDatabase, InvalidateCredentialCache
    └── Repositories/
        ├── Control/
        │   ├── ControlUserRepository.cs           ← GetByUsername, GetById, UpdateLastLogin
        │   ├── MasterRefreshTokenRepository.cs    ← Create, GetByHash, Revoke, RevokeAllForUser
        │   ├── SchoolGroupRepository.cs           ← GetAll, GetById, SubdomainExists, GetGroupIdBySubdomain, Create, Update (SELECT/UPDATE include db_username, db_password)
        │   ├── SchoolRepository.cs                ← GetByGroupId, GetById, Create, Update
        │   ├── SubscriptionRepository.cs          ← GetByGroupId, Upsert
        │   ├── ModuleRepository.cs                ← GetAll, GetGroupModules, UpdateGroupModule, IsModuleEnabled
        │   ├── BrandingRepository.cs              ← GetGroupBranding, UpsertGroupBranding
        │   └── TenantUserRepository.cs            ← GetAll, UsernameExists, GetRoles, Create, ResetPassword, SetStatus
        └── School/
            ├── SchoolAuthRepository.cs            ← GetById, GetByUsername, GetUserSchoolIds, GetUserPermissions, UpdateLastLogin, Create/Get/RevokeRefreshToken
            ├── SchoolBrandingRepository.cs        ← GetBySubdomain (school_branding → group_branding fallback)
            ├── SchoolRbacRepository.cs            ← GetRoles, CreateRole, UpdateRole, GetAllPermissions, GetRolePermissionIds, SetRolePermissions
            ├── MasterDataRepository.cs            ← Get/Create/Update for all 10 master tables (academic_years, class_groups, fee_categories, classes, sections, fee_types, terms, subjects, payment_modes, fee_periods)
            ├── StudentRepository.cs               ← GetAll (filtered, incl. bloodGroup, joinType, busRouteId, hostelId; SELECT includes student_unique_id + join_type + hostel_id), GetById, Create (auto-assigns student_unique_id via MAX+1), Update, UpdatePhotoPath, BulkCreate, GetForPromotion (status IN 'Active','Y', excludes detained), CountDetained, Promote, BlockStudent, UnblockStudent, DetainStudent, ReleaseStudent
            ├── FeeRepository.cs                   ← GetFeeStructure (incl. fee_period_id/payment_type), SaveFeeStructure (scoped by admission_type slice), GetStudentFeeSummary, GetCrossYearFeeSummary (by student_unique_id; transport branch reads bus_fee_structures; hostel branch reads hostel_fee_structures via GetHostelLineItems; outstanding = structure - paid - concession); CollectFee (stores bus_route_id + hostel_id in receipt items), GetReceipts (?createdAfter for end-of-day sync), GetReceiptById (JOINs bus_routes + hostels for display names), CancelReceipt, BulkSaveFeeStructure, BulkImportReceipts (groups by LegacyReceiptNo or AdmissionNo+date+mode; name-based lookups; per-receipt transaction)
            ├── FeeConcessionRepository.cs          ← GetConcessions (JOINs fee_types+bus_routes+hostels+terms+fee_periods; returns BusRouteId/RouteName/HostelId/HostelName), SaveConcession (upsert by student+fee_type+bus_route+hostel+term/period match; INSERT includes bus_route_id + hostel_id), BulkSaveConcessions (same), DeleteConcession (soft-cancel status→Cancelled)
            ├── HostelRepository.cs                 ← GetAll, Create, Update; GetFeeStructure/SaveFeeStructure (Term/Monthly, delete+insert per hostel+year); BulkSaveFeeStructure; GetStudentHostels (filters: academicYearId, classId, sectionId, hostelId), UpdateStudentHostel (WHERE student_unique_id + academic_year_id)
            ├── MarksRepository.cs                 ← GetExamTypes, CreateExamType, UpdateExamType, GetMarksGrid, SaveMarks
            ├── HomeworkRepository.cs              ← GetHomework (teacher mobile — unpaged by class), GetHomeworkPaged (web — classId/sectionId/assignedDate filters + OFFSET/FETCH, returns (items,total); two queries not QueryMultiple — GridReader IAsyncDisposable won't compile on net48), CreateHomework, CreateBatchHomework (daily multi-subject — soft-cancels the day's rows then inserts one per subject, in a transaction), UpdateHomework, DeleteHomework
            ├── AnnouncementsRepository.cs         ← GetAnnouncements, CreateAnnouncement, UpdateAnnouncement, DeleteAnnouncement
            ├── ClassTeacherRepository.cs          ← GetAssignments/GetAssignableTeachers/AssignmentExists/CreateAssignment/DeleteAssignment; routing: GetTeachersForClass (current-year class+section → assigned teachers), GetAssignmentsForTeacher
            ├── MessagingRepository.cs             ← parent↔teacher threads (key student_unique_id) + messages + reports; GetStudentClassContext, FindThreadId/GetOrCreateThread, GetThreadsForTeacher, TeacherCanAccessThread, SendMessage/MarkRead, SetThreadBlocked/GetThreadBlocker, ReportMessage/GetReports/ResolveReport/CountOpenReports
            ├── ReportsRepository.cs               ← GetClassStudents, GetTotalStrength, GetAbsents, GetAttendanceSheet, GetTransportStudents, GetStudyCertificate, GetDailyAttendanceSummary, GetMonthlyAttendanceSheet, GetAttendanceRegister, GetExamHallTicket, GetExamToppers, GetClassToppers, GetFailedStudents, GetAcademicYearToppers, GetHomeworkStatement, GetSubjectHomework, GetStaffSalaryStatement, GetDetainedStudents, GetRegularAbsentees
            ├── TransportRepository.cs             ← GetBuses/CreateBus/UpdateBus, GetRoutes/CreateRoute/UpdateRoute, GetBusFeeStructure/SaveBusFeeStructure, GetStudentTransport (filters: routeId, academicYearId, classId, sectionId; SELECT includes student_unique_id, academic_year_id; IN ('Active','Y')), UpdateStudentTransport (WHERE student_unique_id + academic_year_id — stable cross-year, not student_id)
            ├── HostelRepository.cs                ← GetAll/Create/Update hostels; GetFeeStructure/SaveFeeStructure (Term/Monthly, delete+insert per hostel+year); BulkSaveFeeStructure; GetStudentHostels (filters: academicYearId, classId, sectionId, hostelId; IN ('Active','Y')), UpdateStudentHostel (WHERE student_unique_id + academic_year_id)
            ├── SmsRepository.cs                   ← GetAbsentRecipients, GetFeeDueRecipients (OUTER APPLY for outstanding), GetCustomRecipients, LogSms, GetLogs; per-school SMS config: GetSmsAccount (incl. api_key, send path), GetSmsConfig (HasApiKey flag + templates), UpsertSmsConfig (blank key keeps existing), GetTemplates/GetTemplate/UpsertTemplate/DeleteTemplate
            └── SchoolSettingsRepository.cs         ← Get (SELECT from master DB school_settings by schoolId; returns empty DTO if no row), Upsert (IF EXISTS UPDATE ELSE INSERT; writes master DB)
        └── Mobile/
            ├── MobileStudentAuthRepository.cs     ← student_mobile_accounts + student_refresh_tokens CRUD; GetStudentByAdmissionNo uses ORDER BY academic_year_id DESC; GetAccountByAdmissionNo fallback for post-promotion login; UpdateAccountStudentId re-points account after promotion; GetStudentsByParentMobile (looks up active students by father_mobile, latest year per student)
            ├── MobileParentAuthRepository.cs      ← parent_accounts + parent_children + parent_refresh_tokens + parent_otp (master DB); CreateOtp, GetValidOtp, MarkOtpUsed, GetRecentOtpCount, BindDevice; GetOrCreateParentByMobile (auto-creates OTP-only account on first login); GetChildLinkByAdmissionNo (lookup by admission_no+group_id for upsert); UpdateChildLink (refreshes student_id/student_name/class_name on existing link); GetChildren now filtered by group_id
            ├── MobileDataRepository.cs            ← GetStudentProfile, GetAttendance, GetMarks, GetHomework (TOP 20, assigned_date DESC), GetAnnouncements
            ├── DevicePushTokenRepository.cs       ← master device_push_tokens; UpsertParent/TeacherToken (stores current school's group/school/db per token), Delete; target resolution is SCHOOL-SCOPED (Phase 100 — filters the token join by dpt.db_name+group_id+school_id so a shared master parent_id doesn't leak a push to an app logged into another school): GetParentTokensForStudents(dbName,groupId,schoolId,ids)/GetSchoolParentTokens(dbName,groupId,schoolId)/GetTokensForParent(dbName,groupId,schoolId,parentId)/GetTokensForTeachers
            └── AppConfigRepository.cs             ← GetConfig (master DB app_config; exact applicationId row, falls back to '*'; includes auto_update_enabled)
```

## React Project Structure (current)
```
ascent-control-app/src/
├── api/
│   └── axiosInstance.js       ← fetch wrapper: Bearer header, silent refresh on 401, same api.get/post/put/delete interface
├── store/
│   └── authStore.js           ← zustand: user, accessToken (sessionStorage), login/logout/hasRole
├── layouts/
│   └── MainLayout.jsx         ← Sider nav + Header (user menu + logout)
├── pages/
│   ├── auth/
│   │   └── LoginPage.jsx
│   └── school-groups/
│       ├── SchoolGroupsPage.jsx        ← list + create modal (triggers DB provisioning)
│       ├── SchoolGroupDetailPage.jsx   ← breadcrumb + 5-tab detail view + "Edit Settings" modal (GroupName, Description, Status, DbName, DbUsername, DbPassword)
│       └── tabs/
│           ├── SchoolsTab.jsx          ← branch list + create/edit modal
│           ├── UsersTab.jsx            ← tenant user list + create/reset-password/status
│           ├── SubscriptionTab.jsx     ← plan/dates/limits form
│           ├── ModulesTab.jsx          ← enable/disable modules (super_admin only)
│           └── BrandingTab.jsx         ← colors, logos, tagline form
└── App.jsx                    ← routes: /login, /school-groups, /school-groups/:id

ascent-school-app/src/
├── api/
│   └── axiosInstance.js       ← fetch wrapper: Bearer header, X-Subdomain header, silent refresh on 401
├── store/
│   ├── authStore.js           ← zustand: accessToken (memory only), user {userId,groupId,schoolId,fullName,permissions[]}
│   └── brandingStore.js       ← zustand: branding defaults, isLoaded, setBranding
├── layouts/
│   └── AppLayout.jsx          ← sticky header + collapsible sidebar nav + Outlet; Students sub-menu: List / Bulk Import / Promote Students / Blood Group Search; Fees sub-menu: Fee Structure / Bulk Import Structure / Admission Fee / School Fee / Transport Fee / Hostel Fee / Other Fee / Receipts / Legacy Receipt Import / Pending Online Payments / Fee Concession; Hostel nav item (HomeOutlined, after Transport); Homework sub-menu: Daily Homework / Homework List; SMS Center nav item; Settings sub-menu (SettingOutlined): School Settings / Roles & Permissions / User Management / Payment Gateway / SMS Gateway
├── pages/
│   ├── auth/
│   │   └── LoginPage.jsx      ← branding-aware login (logo, tagline, loginBgPath)
│   ├── rbac/
│   │   ├── RolesPage.jsx      ← role list + create/edit modal + permission drawer (grouped by module)
│   │   └── UsersPage.jsx      ← user list + create modal + reset-password + activate/deactivate
│   ├── students/
│   │   ├── StudentsPage.jsx       ← searchable/filterable list; status includes Blocked; Block/Unblock (with reason modal) + Detain/Release actions; detained badge shown in status column
│   │   ├── StudentFormPage.jsx    ← full-page 7-tab form (Basic, Academic, Family, Address, Identity, Transport, Other) + photo upload; section is a dropdown loaded by classId; Other tab: hostelId Select loaded from /school/hostel (replaces free-text hostelName); all tabs use `forceRender: true` to prevent AntD lazy-tab field wiping
│   │   ├── StudentsImportPage.jsx ← CSV bulk import; column reference table + example rows + template download + PapaParse preview + result with error download; POST /school/students/bulk; 20 columns including MotherMobile, AadharNo, Caste, CasteCode, Religion, JoiningClass, MotherTongue
│   │   ├── PromoteStudentsPage.jsx ← Step 1: from year+class+section → preview eligible students (status IN Active/Y, not detained) + detained count warning; Step 2: to year+class+section → confirm modal → result stats
│   │   └── BloodGroupSearchPage.jsx ← blood group dropdown (A+/A-/B+/B-/AB+/AB-/O+/O-) → table with colour-coded tags; PDF + CSV export
│   ├── fee/
│   │   ├── FeeStructurePage.jsx       ← matrix grid (fee types × terms or fee_periods) with InputNumber cells; Term/Monthly toggle; Admission Type filter (New/Old/All); load/save per class+category+year+admissionType
│   │   ├── FeeStructureImportPage.jsx ← CSV bulk import for fee structure; upserts per Class/Category/FeeType/Term row; POST /school/fees/structure/bulk
│   │   ├── FeeReceiptsImportPage.jsx  ← CSV bulk import for legacy receipts; 14-column format; groups rows by LegacyReceiptNo or AdmissionNo+Date+Mode; POST /school/fees/receipts/bulk
│   │   ├── FeeCollectionBase.jsx      ← shared fee collection component; student search by unique_id; loads cross-year summary (CrossYearFeeSummaryDto); year tabs; checkbox selection; online/offline payment
│   │   ├── AdmissionFeePage.jsx       ← feeTypeCategory=Admission; joinTypeFilter='New' (student search restricted to join_type=New)
│   │   ├── SchoolFeePage.jsx          ← feeTypeCategory=School
│   │   ├── TransportFeePage.jsx       ← feeTypeCategory=Transport
│   │   ├── HostelFeePage.jsx          ← feeTypeCategory=Hostel
│   │   ├── OtherFeePage.jsx           ← feeTypeCategory=Other
│   │   ├── ReceiptsPage.jsx           ← filterable receipts list + detail Drawer + cancel Modal; "Created after" DatePicker (for end-of-day sync) + "Export CSV" button (client-side papaparse)
│   │   ├── PendingPaymentsPage.jsx    ← reconcile Pending gateway orders (Phase 69): date-range filter → list pending orders, per-row "Check status", bulk "Scan", "Reconcile" on captured rows
│   │   └── FeeConcessionPage.jsx      ← 3-tab page; global Radio.Group toggle: "School Fee" | "Transport" | "Hostel"; Individual Entry (student search → school: fee type+term/period; transport: route+term; hostel: hostel+term/period → type+amount+remarks → save); Bulk Apply (school: fee type+term/period+load by class; transport: route+term+load by busRouteId; hostel: hostel+term/period+load by hostelId → editable amount/remarks per row + apply-to-all → save); Concession List (load by year+class+section; "Fee Type / Route / Hostel" column shows feeTypeName, routeName, or hostelName; Print Selected → multi-page jsPDF with amountToWords)
│   ├── master/
│   │   ├── MasterDataPage.jsx ← Tabs container; each tab loads its own data independently (no shared prop-drilling)
│   │   ├── AcademicYearsTab.jsx
│   │   ├── ClassGroupsTab.jsx
│   │   ├── FeeCategoriesTab.jsx
│   │   ├── ClassesTab.jsx
│   │   ├── SectionsTab.jsx    ← class dropdown → section list per class → create/edit modal
│   │   ├── FeeTypesTab.jsx
│   │   ├── TermsTab.jsx
│   │   ├── SubjectsTab.jsx
│   │   ├── PaymentModesTab.jsx
│   │   ├── FeePeriodsTab.jsx  ← academic year dropdown → fee periods list → create/edit modal (month, year, label, sequence)
│   │   └── ExamTypesTab.jsx   ← academic year dropdown → exam types list → create/edit modal (name, display order); uses /school/marks/exam-types
│   ├── sms/
│   │   └── SMSPage.jsx        ← two tabs: Send SMS (type selector → filters → load recipients → select rows → batch size 25/50 → message preview → send with progress) + SMS History (filterable log)
│   └── reports/
│       ├── ReportsPage.jsx    ← sidebar nav → 20 reports rendered in main card
│       ├── [existing reports...]
│       ├── DetainedStudentsReport.jsx ← year/class/section filters (all optional); loads on mount; volcano tags for reason; PDF+CSV
│       └── RegularAbsenteesReport.jsx ← date range + min days InputNumber (default 3) + class/section; absent days colour-coded tag; scroll={{x:'max-content'}}; PDF+CSV
├── marks/
│   └── MarksEntryPage.jsx     ← select year+exam+class+section (all required) → student×subject grid with InputNumber + absent checkbox
├── homework/
│   ├── DailyHomeworkPage.jsx  ← daily multi-subject entry (Phase 70): Class/Section/Date → subject grid with a Description per row → Save (POST /school/homework/batch, overwrites the day's rows)
│   └── HomeworkPage.jsx       ← list with class filter + create/edit modal; Section dropdown (loaded by class, optional); section column in table
├── announcements/
│   └── AnnouncementsPage.jsx  ← list (pinned first) + create/edit modal (scope School/Class)
├── transport/
│   └── TransportPage.jsx      ← 4-tab page: Buses (create/edit), Routes (create/edit), Bus Fee Structure (route+year → term amounts), Student Assignment (Year/Class/Section/Route filter bar → load → edit modal; PUT uses studentUniqueId; body includes academicYearId)
├── hostel/
│   └── HostelPage.jsx         ← 4-tab page: Hostels CRUD (hostelName, description, capacity, noOfRooms, contactNo, address); Fee Structure (hostel+year+paymentType Term/Monthly → term/period amount columns → save); Student Assignment (Year/Class/Section/Hostel filter bar → load → edit modal with hostel dropdown; PUT uses studentUniqueId + academicYearId); Bulk Import CSV (hostel fee structure rows: HoslelId/AcademicYearId/TermId/Amount → POST /school/hostel/fee-structure/bulk)
└── App.jsx                    ← branding load, silent refresh on mount, routes: /login, /master, /students, /students/import, /students/promote, /students/blood-group, /fees/structure, /fees/structure/import, /fees/collect/admission, /fees/collect/school, /fees/collect/transport, /fees/collect/hostel, /fees/collect/other, /fees/receipts, /fees/receipts/import, /fees/concessions, /hostel, /marks, /homework, /announcements, /sms, /reports, /settings/school (SchoolSettingsPage), /settings/roles (RolesPage), /settings/users (UsersPage), /settings/payment-gateway (GatewaySettingsPage)
│   ├── settings/
│   │   └── SchoolSettingsPage.jsx ← 3 AntD Cards (Admission, Fee & Billing, Reports & Institution); loads GET /school/settings on mount; PUT /school/settings on save

ascent-parent-app/src/
├── api/
│   └── axiosInstance.js     ← sends X-School-Code + X-Subdomain headers; silent refresh via /mobile/auth/parent/refresh; same api.get/post interface
├── store/
│   ├── authStore.js         ← accessToken (memory only), parent {parentId, displayName}, child {studentId, studentName, className, admissionNo}; login/setChild/logout/setToken
│   └── brandingStore.js     ← same pattern as school app
├── layouts/
│   └── AppLayout.jsx        ← header shows child name + class Tag + Switch Child / Logout dropdown
├── pages/
│   ├── auth/
│   │   ├── LoginPage.jsx        ← Step 0: 10-digit mobile → OTP; Step 1: AntD Input.OTP 6-digit + 30s resend countdown
│   │   └── ChildSelectorPage.jsx← loads /mobile/auth/parent/children, POST select-child, navigate to /fees
│   └── fees/
│       ├── FeeHomePage.jsx      ← 3 tabs (School/Transport/Hostel); each tab independent with own useEffect; FeeTab + YearSection components; Razorpay checkout; loadRazorpayScript lazy loader
│       └── PaymentSuccessPage.jsx← receipt Descriptions + items Table + print button (@media print CSS)
└── App.jsx                  ← RequireAuth + RequireChild guards; silent refresh on mount; routes: /login, /select-child, /fees, /fees/success
```

---

## School App API Endpoints (Phase 2a + 2b + 3)

| Method | URL | Auth | Notes |
|---|---|---|---|
| GET  | `/branding` | Public | X-Subdomain header + optional ?schoolId |
| GET  | `/version` | Public | API assembly version + deploy date (bin DLL write time); shown in the school-app footer |
| GET  | `/school/settings/r2` | School JWT | R2 storage config (secret omitted; hasSecretKey flag) |
| PUT  | `/school/settings/r2` | School JWT | Upsert R2 storage config (blank secret keeps existing) |
| POST | `/school/uploads/presign` | School JWT | Presigned R2 PUT URL + public URL (purpose: student-photo/homework/announcement/event) |
| POST | `/school/media/attach` | School JWT | Record an uploaded R2 file for an entity (media_uploads) |
| GET  | `/school/media` | School JWT | List a homework/announcement/event's uploaded files (?entityType=&entityId=) |
| DELETE | `/school/media/{id}` | School JWT | Remove an uploaded file record |
| PUT  | `/school/students/{id}/photo` | School JWT | Save R2 photo URL after a presigned upload (JSON {photoUrl}) |
| POST | `/school/auth/login` | Public | X-Subdomain header, returns token + cookie |
| POST | `/school/auth/refresh` | Cookie | X-Subdomain + ?schoolId, rotates cookie |
| POST | `/school/auth/logout` | Cookie | Revokes refresh token |
| POST | `/school/auth/change-password` | School JWT | Self-service — verify current password, set new, revoke refresh tokens |
| GET  | `/school/roles` | School JWT | List roles with permission count |
| POST | `/school/roles` | School JWT | Create role |
| PUT  | `/school/roles/{id}` | School JWT | Update role name/description |
| GET  | `/school/roles/permissions` | School JWT | All available permissions |
| GET  | `/school/roles/{id}/permissions` | School JWT | Permission IDs for a role |
| PUT  | `/school/roles/{id}/permissions` | School JWT | Replace full permission set (atomic) |
| GET  | `/school/users` | School JWT | List all users in group (incl. linked staff name/designation) |
| POST | `/school/users` | School JWT | Create user from a staff record (EmployeeId required) + assign role to branches |
| GET  | `/school/users/roles` | School JWT | Roles list (for create-user dropdown) |
| GET  | `/school/users/schools` | School JWT | Branches list (for create-user dropdown) |
| GET  | `/school/users/staff` | School JWT | Active staff without a login (create-user staff dropdown) |
| PUT  | `/school/users/{id}/reset-password` | School JWT | Reset password + revoke refresh tokens |
| PUT  | `/school/users/{id}/status` | School JWT | Activate / Deactivate |
| GET  | `/school/master/academic-years` | School JWT | List academic years (?activeOnly=true filters to Active status; IsCurrent=true on most recent Active year) |
| POST | `/school/master/academic-years` | School JWT | Create |
| PUT  | `/school/master/academic-years/{id}` | School JWT | Update |
| GET  | `/school/master/class-groups` | School JWT | List class groups |
| POST | `/school/master/class-groups` | School JWT | Create |
| PUT  | `/school/master/class-groups/{id}` | School JWT | Update |
| GET  | `/school/master/fee-categories` | School JWT | List fee categories |
| POST | `/school/master/fee-categories` | School JWT | Create |
| PUT  | `/school/master/fee-categories/{id}` | School JWT | Update |
| GET  | `/school/master/classes` | School JWT | List classes (with class group name via JOIN) |
| POST | `/school/master/classes` | School JWT | Create |
| PUT  | `/school/master/classes/{id}` | School JWT | Update |
| GET  | `/school/master/sections` | School JWT | List sections for a class (?classId required) |
| POST | `/school/master/sections` | School JWT | Create section |
| PUT  | `/school/master/sections/{id}` | School JWT | Update section |
| GET  | `/school/class-teachers` | School JWT | List class→teacher assignments (?academicYearId, ?classId); drives parent→teacher message routing (Phase 92) |
| GET  | `/school/class-teachers/teachers` | School JWT | Active logins for the assign-teacher dropdown |
| POST | `/school/class-teachers` | School JWT | Assign a teacher to a class (+ optional section; NULL = whole class) |
| DELETE | `/school/class-teachers/{id}` | School JWT | Unassign (hard delete) |
| GET  | `/school/message-reports` | School JWT | Reported messages for admin review (?status); gated on SETTINGS.MANAGE (Phase 92) |
| GET  | `/school/message-reports/open-count` | School JWT | Count of open reports (nav badge) |
| POST | `/school/message-reports/{id}/resolve` | School JWT | Resolve a report (action: Removed hides the message both sides / Reviewed) |
| GET  | `/school/master/fee-types` | School JWT | List fee types |
| POST | `/school/master/fee-types` | School JWT | Create |
| PUT  | `/school/master/fee-types/{id}` | School JWT | Update |
| GET  | `/school/master/terms` | School JWT | List terms (?academicYearId optional filter) |
| POST | `/school/master/terms` | School JWT | Create |
| PUT  | `/school/master/terms/{id}` | School JWT | Update |
| GET  | `/school/master/subjects` | School JWT | List subjects |
| POST | `/school/master/subjects` | School JWT | Create |
| PUT  | `/school/master/subjects/{id}` | School JWT | Update |
| GET  | `/school/master/payment-modes` | School JWT | List payment modes |
| POST | `/school/master/payment-modes` | School JWT | Create |
| PUT  | `/school/master/payment-modes/{id}` | School JWT | Update |
| GET  | `/school/master/fee-periods` | School JWT | List fee periods (?academicYearId optional) |
| POST | `/school/master/fee-periods` | School JWT | Create fee period |
| PUT  | `/school/master/fee-periods/{id}` | School JWT | Update fee period |
| GET  | `/school/students` | School JWT | List students (search, classId, sectionId, academicYearId, status, bloodGroup, joinType, busRouteId, hostelId, latestOnly); ?latestOnly=true returns only the highest academic_year_id row per admission_no via ROW_NUMBER() OVER (PARTITION BY admission_no, school_id ORDER BY academic_year_id DESC) — used by fee collection search to avoid duplicate rows for multi-year students |
| GET  | `/school/students/{id}` | School JWT | Full student detail |
| GET  | `/school/students/{id}/history` | School JWT | Temporal audit trail — all saved versions of the student (Phase 79) |
| POST | `/school/students` | School JWT | Create student |
| PUT  | `/school/students/{id}` | School JWT | Update student |
| POST | `/school/students/{id}/photo` | School JWT | Upload student photo (multipart) |
| POST | `/school/students/bulk` | School JWT | Bulk create students (max 500); default duplicate admissionNo returns error; `Upsert=true` in body updates existing rows (match admission_no+academic_year) personal/contact fields instead of erroring |
| GET  | `/school/students/promote/preview` | School JWT | Preview eligible students + detainedCount (fromYearId, fromClassId, fromSectionId optional) |
| POST | `/school/students/promote` | School JWT | Promote students — INSERT new rows for target year+class+section; skips already-promoted |
| PUT  | `/school/students/{id}/block` | School JWT | Set status=Blocked with mandatory reason |
| PUT  | `/school/students/{id}/unblock` | School JWT | Set status=Active, clear blocked_reason |
| PUT  | `/school/students/{id}/detain` | School JWT | Set is_detained=1 with mandatory reason |
| PUT  | `/school/students/{id}/release` | School JWT | Set is_detained=0, clear detained_reason |
| GET  | `/school/fees/structure` | School JWT | Get fee structure (classId, feeCategoryId, academicYearId, admissionType optional) |
| POST | `/school/fees/structure` | School JWT | Save fee structure (delete+insert scoped by admissionType slice; supports payment_type=Term|Monthly and fee_period_id) |
| POST | `/school/fees/structure/bulk` | School JWT | Bulk upsert fee structure rows from CSV (max 500); each row = one Class+Category+FeeType+Term |
| GET  | `/school/fees/student/{studentId}` | School JWT | Student fee summary with outstanding dues |
| POST | `/school/fees/collect` | School JWT | Collect fee + generate receipt |
| GET  | `/school/fees/receipts` | School JWT | List receipts (search, dateFrom, dateTo, status, createdAfter, createdBefore, source); ?source=webapp + createdAfter/createdBefore for EOD legacy pull |
| GET  | `/school/fees/receipts/{id}` | School JWT | Full receipt detail with line items |
| PUT  | `/school/fees/receipts/{id}/cancel` | School JWT | Cancel receipt (requires CancelReason) |
| POST | `/school/fees/receipts/bulk` | School JWT | Bulk import legacy receipts from CSV rows (max 1000); groups by LegacyReceiptNo or AdmissionNo+Date+Mode |
| GET    | `/school/fees/concessions` | School JWT | List concessions (?academicYearId, ?classId, ?sectionId) |
| POST   | `/school/fees/concessions` | School JWT | Save/upsert individual concession (one active record per student+fee_type+term/period) |
| POST   | `/school/fees/concessions/bulk` | School JWT | Bulk save concessions for a class — one row per student |
| DELETE | `/school/fees/concessions/{id}` | School JWT | Soft-cancel concession (sets status=Cancelled; outstanding recalculates on next load) |
| GET  | `/school/fees/gateway-config` | School JWT | Active gateway key_id (no secret) for checkout |
| GET  | `/school/fees/gateway-settings` | School JWT | Admin settings view (key_id + hasWebhookSecret flag) |
| PUT  | `/school/fees/gateway-settings` | School JWT | Save/update gateway API keys |
| POST | `/school/fees/payment-orders` | School JWT | Create Razorpay order, returns externalOrderId + keyId |
| POST | `/school/fees/payment-orders/{id}/verify` | School JWT | Verify HMAC signature, create receipt |
| POST | `/school/fees/payment-webhook` | Public (AllowAnonymous) | Razorpay server webhook — payment.captured event |
| GET  | `/school/fees/payment-orders/pending` | School JWT | List Pending orders (?createdAfter, ?createdBefore) for reconciliation |
| GET  | `/school/fees/payment-orders/{id}/status` | School JWT | Live gateway status for one pending order |
| GET  | `/school/fees/payment-orders/scan` | School JWT | Bulk-check all pending orders at gateway (?onlyReconcilable=true) |
| POST | `/school/fees/payment-orders/{id}/reconcile` | School JWT | Create receipt for a captured-but-pending order (idempotent) |
| GET  | `/school/marks/exam-types` | School JWT | List exam types (?academicYearId optional) |
| POST | `/school/marks/exam-types` | School JWT | Create exam type |
| PUT  | `/school/marks/exam-types/{id}` | School JWT | Update exam type |
| GET  | `/school/attendance` | School JWT | Attendance grid (classId + date required; sectionId optional — omit for whole class across all sections) |
| POST | `/school/attendance` | School JWT | Save/upsert attendance entries (status: Present/Absent/Late/HalfDay/Holiday) |
| DELETE | `/school/attendance` | School JWT | Delete attendance for a class on a date (classId + date required; sectionId optional — omit to delete the whole class); hard-deletes current-year rows |
| GET  | `/school/attendance/summary` | School JWT | Monthly summary (classId, sectionId, month, year — all required) |
| GET  | `/school/marks` | School JWT | Get marks grid (classId, sectionId, examTypeId, academicYearId — all required) |
| POST | `/school/marks` | School JWT | Save/upsert marks batch |
| GET  | `/school/homework` | School JWT | Server-paged list — `?classId=&sectionId=&assignedDate=&page=1&pageSize=20` → `{ items, total, page, pageSize }` (Phase 94) |
| POST | `/school/homework` | School JWT | Create homework (single entry) |
| POST | `/school/homework/batch` | School JWT | Daily multi-subject entry; overwrites the day's Active rows for Class+Section |
| PUT  | `/school/homework/{id}` | School JWT | Update homework |
| DELETE | `/school/homework/{id}` | School JWT | Soft-delete homework (status=Cancelled) |
| GET  | `/school/announcements` | School JWT | List announcements (?classId optional) |
| POST | `/school/announcements` | School JWT | Create announcement |
| PUT  | `/school/announcements/{id}` | School JWT | Update announcement |
| DELETE | `/school/announcements/{id}` | School JWT | Soft-delete announcement (status=Inactive) |
| GET    | `/school/events` | School JWT | List active events (?classId optional); ordered pinned-first then date DESC |
| POST   | `/school/events` | School JWT | Create event (title, eventDate, mediaType, mediaUrl required) |
| PUT    | `/school/events/{id}` | School JWT | Update event |
| DELETE | `/school/events/{id}` | School JWT | Soft-delete event (status=Inactive) |
| GET    | `/school/reports/detained-students` | School JWT | Detained students (academicYearId, classId, sectionId — all optional) |
| GET    | `/school/reports/regular-absentees` | School JWT | Students absent ≥ minDays within dateFrom–dateTo; optional classId/sectionId |
| GET    | `/school/sms/recipients` | School JWT | Load SMS recipients by smsType (Absent/FeeDue/Custom) with filters |
| POST   | `/school/sms/send` | School JWT | Send SMS batch; parallel dispatch via Task.WhenAll; logs results to sms_logs |
| GET    | `/school/sms/logs` | School JWT | SMS history (smsType, date filters) |
| GET    | `/school/sms/config` | School JWT | Per-school SMS gateway account (api_key omitted → hasApiKey) + templates |
| PUT    | `/school/sms/config` | School JWT | Upsert SMS gateway account (blank apiKey keeps existing) |
| POST   | `/school/sms/templates` | School JWT | Add/edit SMS template by template_key (DLT id + message text + placeholders) |
| DELETE | `/school/sms/templates/{key}` | School JWT | Delete an SMS template |
| GET  | `/school/settings` | School JWT | Get school settings (Admission, Fee & Billing, Reports & Institution); returns empty defaults if no row exists |
| PUT  | `/school/settings` | School JWT | Upsert school settings for the authenticated branch |
| GET  | `/school/transport/buses` | School JWT | List buses |
| POST | `/school/transport/buses` | School JWT | Create bus |
| PUT  | `/school/transport/buses/{id}` | School JWT | Update bus |
| GET  | `/school/transport/routes` | School JWT | List routes |
| POST | `/school/transport/routes` | School JWT | Create route |
| PUT  | `/school/transport/routes/{id}` | School JWT | Update route |
| GET  | `/school/transport/fee-structure` | School JWT | Get bus fee structure (?routeId, ?academicYearId required) |
| POST | `/school/transport/fee-structure` | School JWT | Save bus fee structure (delete+insert per route+year) |
| GET  | `/school/transport/students` | School JWT | List students with transport (?routeId, ?academicYearId, ?classId, ?sectionId optional) |
| PUT  | `/school/transport/students/{studentUniqueId}` | School JWT | Update student transport (body includes academicYearId; WHERE uses student_unique_id + academic_year_id) |
| GET  | `/school/hostel` | School JWT | List hostels for the school |
| POST | `/school/hostel` | School JWT | Create hostel |
| PUT  | `/school/hostel/{id}` | School JWT | Update hostel |
| GET  | `/school/hostel/fee-structure` | School JWT | Get hostel fee structure (?hostelId, ?academicYearId required) |
| POST | `/school/hostel/fee-structure` | School JWT | Save hostel fee structure (delete+insert per hostel+year; supports Term/Monthly) |
| POST | `/school/hostel/fee-structure/bulk` | School JWT | Bulk upsert hostel fee structure from CSV rows |
| GET  | `/school/hostel/students` | School JWT | List students with hostel assignment (?academicYearId, ?classId, ?sectionId, ?hostelId optional) |
| PUT  | `/school/hostel/students/{studentUniqueId}` | School JWT | Update student hostel assignment (body includes academicYearId + hostelId; WHERE uses student_unique_id + academic_year_id) |

## Mobile App API Endpoints (Phase 6B + 10)

> **Note:** Android app uses SMS OTP for parents (Phase 10) and teacher credentials for staff. Student auth endpoints exist on the backend only.

| Method | URL | Auth | Notes |
|---|---|---|---|
| POST | `/mobile/auth/student/register` | Public | *(backend only — not used by Android app)* |
| POST | `/mobile/auth/student/login` | Public | *(backend only — not used by Android app)* |
| POST | `/mobile/auth/student/refresh` | Cookie | *(backend only — not used by Android app)* |
| POST | `/mobile/auth/student/logout` | Cookie | *(backend only — not used by Android app)* |
| POST | `/mobile/auth/student/request-otp` | Public | *(backend only — not used by Android app)* |
| POST | `/mobile/auth/student/reset-pin` | Public | *(backend only — not used by Android app)* |
| POST | `/mobile/auth/parent/request-otp` | Public | Resolves school from X-School-Code; gates on student existing in school DB by father_mobile; rate-limits (5/10 min); sends OTP; always returns generic message |
| POST | `/mobile/auth/parent/verify-otp` | Public | Validates OTP; auto-creates parent_account if first login; auto-links all matching students from school; binds deviceId; revokes old tokens on device change; issues JWT + cookie |
| POST | `/mobile/auth/parent/register` | Public | Creates parent_accounts in master DB (legacy, used by support) |
| POST | `/mobile/auth/parent/login` | Public | PIN-based login (legacy, kept for backward compat) |
| POST | `/mobile/auth/parent/refresh` | Cookie | Rotates parentRefreshToken cookie |
| POST | `/mobile/auth/parent/logout` | Cookie | Revokes refresh token |
| GET  | `/mobile/auth/parent/children` | parent JWT | List linked children scoped to current school's group (X-School-Code) |
| POST | `/mobile/auth/parent/select-child` | parent JWT | Re-issues parent JWT with selected child's context; validates link belongs to current school group |
| POST | `/mobile/auth/parent/link-child` | parent JWT | Adds new child link (admission_no + school_code) |
| POST | `/mobile/auth/teacher/login` | Public | Validates teacher against tenant users table; issues tokenType=teacher JWT + cookie |
| POST | `/mobile/auth/teacher/refresh` | Cookie | Rotates teacherRefreshToken cookie |
| POST | `/mobile/auth/teacher/logout` | Cookie | Revokes refresh token |
| GET  | `/mobile/teacher/classes` | teacher JWT | List active classes for school |
| GET  | `/mobile/teacher/sections?classId=` | teacher JWT | List sections for a class |
| GET  | `/mobile/teacher/attendance?classId=&sectionId=&date=` | teacher JWT | Attendance grid for date |
| POST | `/mobile/teacher/attendance` | teacher JWT | Save/upsert attendance entries (MERGE) |
| GET  | `/mobile/teacher/homework?classId=` | teacher JWT | List homework for class |
| POST | `/mobile/teacher/homework` | teacher JWT | Create homework entry |
| GET  | `/mobile/student/profile` | student/parent JWT | Full student profile |
| GET  | `/mobile/student/attendance?month=&year=` | student/parent JWT | Monthly attendance summary + records |
| GET  | `/mobile/student/marks?academicYearId=` | student/parent JWT | Marks grouped by exam type |
| GET  | `/mobile/student/homework` | student/parent JWT | Recent homework for student's class |
| GET  | `/mobile/student/announcements` | student/parent JWT | School + class announcements (pinned first) |
| GET  | `/mobile/student/events` | student/parent JWT | School + class events (pinned first, most recent 50) |
| GET  | `/mobile/fees/outstanding` | parent JWT (child ctx) | Cross-year outstanding summary by feeTypeCategory for selected child |
| GET  | `/mobile/fees/gateway-config` | parent JWT (child ctx) | Active gateway key_id for Razorpay checkout |
| POST | `/mobile/fees/payment-orders` | parent JWT (child ctx) | Create Razorpay order; server resolves student_unique_id + year student_id + online payment_mode_id |
| POST | `/mobile/fees/payment-orders/{id}/verify` | parent JWT (child ctx) | Verify Razorpay HMAC signature, create receipt |
| GET  | `/mobile/fees/receipts/{id}` | parent JWT (child ctx) | Receipt detail for in-app print (validated to child by student_unique_id) |
| GET  | `/mobile/app/config` | Public (AllowAnonymous) | Force/soft update gate; auto-prompt only when `auto_update_enabled=1` (Phase 57/71) |
| GET  | `/mobile/teacher/announcements?classId=` | teacher JWT | List class announcements (Phase 90) |
| POST | `/mobile/teacher/announcements` | teacher JWT | Post an announcement to the class (optional section) (Phase 90) |
| POST | `/mobile/notifications/register-token` | parent/teacher JWT | Register this device's FCM token (Phase 91) |
| POST | `/mobile/notifications/unregister-token` | any | Remove an FCM token by value (logout) (Phase 91) |
| GET | `/mobile/messages` | parent JWT (child ctx) | The child's conversation + who it reaches (canMessage/reason/teachers/messages) (Phase 92) |
| POST | `/mobile/messages` | parent JWT (child ctx) | Send a message to the child's class teacher(s); creates the thread on first send |
| POST | `/mobile/messages/read` | parent JWT (child ctx) | Mark the teacher's messages read |
| POST | `/mobile/messages/report` | parent JWT (child ctx) | Report a message for school review (Play UGC) |
| POST | `/mobile/messages/block` \| `/unblock` | parent JWT (child ctx) | Block / unblock the conversation (Play UGC) |
| GET | `/mobile/teacher/messages` | teacher JWT | Threads for the classes this teacher is assigned to (with unread counts) |
| GET/POST | `/mobile/teacher/messages/{threadId}` | teacher JWT | Open a conversation / post a reply |
| POST | `/mobile/teacher/messages/{threadId}/read` | teacher JWT | Mark the parent's messages read |
| POST | `/mobile/teacher/messages/{threadId}/report` | teacher JWT | Report a message for school review (Play UGC) |
| POST | `/mobile/teacher/messages/{threadId}/block` \| `/unblock` | teacher JWT | Block / unblock the conversation (Play UGC) |

---

## Control App API Endpoints (Phase 1)

| Method | URL | Auth | Notes |
|---|---|---|---|
| POST | `/control/auth/login` | Public | Returns access token + sets HttpOnly cookie |
| POST | `/control/auth/refresh` | Cookie | Rotates refresh token (sliding expiry) |
| POST | `/control/auth/logout` | Cookie | Revokes refresh token |
| GET | `/control/school-groups` | Any control role | List all groups |
| POST | `/control/school-groups` | Any control role | Create group + provision tenant DB |
| GET | `/control/school-groups/{id}` | Any control role | Get single group |
| PUT | `/control/school-groups/{id}` | Any control role | Update name/description/status/dbName/dbUsername/dbPassword; invalidates credential cache |
| GET | `/control/school-groups/{id}/schools` | Any control role | List branches |
| POST | `/control/school-groups/{id}/schools` | Any control role | Add branch |
| PUT | `/control/school-groups/{id}/schools/{schoolId}` | Any control role | Update branch |
| GET | `/control/school-groups/{id}/users` | Any control role | List tenant users |
| POST | `/control/school-groups/{id}/users` | Any control role | Create initial user |
| PUT | `/control/school-groups/{id}/users/{uid}/reset-password` | Any control role | Reset password |
| PUT | `/control/school-groups/{id}/users/{uid}/status` | Any control role | Activate/Deactivate |
| GET | `/control/school-groups/{id}/roles` | Any control role | Tenant roles list |
| GET | `/control/school-groups/{id}/subscription` | Any control role | Get subscription |
| PUT | `/control/school-groups/{id}/subscription` | Any control role | Upsert subscription |
| GET | `/control/modules` | Any control role | Master module list |
| GET | `/control/school-groups/{id}/modules` | Any control role | Group module status |
| PUT | `/control/school-groups/{id}/modules/{moduleId}` | `super_admin` only | Toggle module |
| GET | `/control/school-groups/{id}/branding` | Any control role | Get branding |
| PUT | `/control/school-groups/{id}/branding` | Any control role | Upsert branding |

---

## Database Naming Conventions
- Tables: `snake_case`, plural
- Columns: `snake_case`
- PKs: `INT IDENTITY(1,1)` on all tables except `students` (BIGINT) and `school_settings` (school_id as PK)
- FKs: `FK_<table>_<referenced_table>`
- `school_id` on every tenant table — filters by branch within group
- `group_id` NOT in tenant tables — implied by the DB itself

## Key Design Decisions
1. **StuID FLOAT → BIGINT IDENTITY** — float is never valid as a PK
2. **AcademicYear string → academic_year_id FK** — normalized across all tables
3. **All VARCHAR IDs → INT IDENTITY** — performance + referential integrity
4. **MONEY → DECIMAL(12,2)** — portable across DB engines
5. **Signature image → file path string** — no binary blobs in DB
6. **Subdomain login** — chosen over username dropdown for tenant resolution
7. **Nullable columns retained** — `fee_type_name`, `trip_data`, `cleaner_name`, `owner_data`, `route_name`, `bus_no_data`, `bus_name`, `category_name` — kept in schema, UI sends null
8. **Access token in sessionStorage (control) / memory only (school)** — prevents XSS token theft
9. **Tenant DB name stored in `school_groups.db_name`** — set manually after provisioning the tenant DB. Login returns 503 if `db_name` is NULL. `master_db_migration.sql` adds the column and auto-fills existing groups with `ascent_group_{group_id}`.
10. **`Microsoft.AspNet.WebApi.Client` version 6.0.0** — 5.3.0 does not exist on NuGet; 6.0.0 is the correct package for `System.Net.Http.Formatting.dll`
11. **No axios** — both React apps use native `fetch` API with a thin wrapper to avoid third-party dependency vulnerabilities. The wrapper exposes `api.get/post/put/delete` and maintains the same `{ data }` response shape.
12. **schoolId in localStorage** — not sensitive (just an int); stored so the refresh endpoint can re-issue the correct school-scoped token after page reload.
13. **`tokenType` claim in JWT** — `tokenType=control` (control tokens), `tokenType=school` (school web app tokens), `tokenType=student`/`parent` (mobile student/parent), `tokenType=teacher` (mobile teacher). `JwtAuthenticationFilter` routes each type to its own context (TenantContext / MobileContext / TeacherContext / ControlContext).
14. **`fullName` claim in school JWT** — added to `TokenClaims`, `JwtHelper`, `TenantContext`, `JwtAuthenticationFilter`, and `SchoolAuthController.BuildAuthResponse`. Required by `FeeController` to store `created_by` / `cancelled_by` on receipts.
15. **Receipt number format** — `{year}-{receiptId:D5}` (e.g. `2024-00001`). Generated by INSERT with `receipt_no='TMP'`, then UPDATE after getting `SCOPE_IDENTITY()` — avoids race conditions.
16. **Fee structure storage** — delete+insert in transaction on every save. No partial updates; entire class/category/year combination is replaced atomically.
17. **Payment gateway abstraction** — `IGatewayService` interface + `GatewayServiceFactory` dictionary. Add new gateway: implement interface, add entry to factory, add option to UI dropdown. No other changes needed.
18. **Gateway key security** — `key_secret` stored in `gateway_configs` table; never returned in any API response. Frontend only ever receives `key_id`. `key_secret` fetched internally by server for Razorpay API calls.
19. **Online payment flow** — Staff initiates → server creates Razorpay order → frontend opens checkout (parent pays UPI/card/netbanking) → JS callback → server verifies HMAC signature → receipt created. Webhook (`payment-webhook`) provides fallback for browser-close edge cases; uses `notes.group_id` embedded in order to identify tenant.
20. **`is_online` flag on payment_modes** — determines whether a mode triggers gateway checkout vs offline collection. Migration adds column; existing "Online" mode auto-flagged.
21. **Gateway tables** — `gateway_configs` (API keys per school), `payment_gateway_orders` (order lifecycle tracking). Run `gateway_tables_migration.sql` on existing tenant DBs.
22. **Mobile tenant routing via `X-School-Code` header** — mobile app sends `X-School-Code: {subdomain}` (same value as `school_groups.subdomain`). API resolves it identically to `X-Subdomain`. School distributes their code to students at onboarding.
23. **Two mobile account types** — `student` accounts live in tenant DB (`student_mobile_accounts`); `parent` accounts live in `ascent_master` (`parent_accounts`). Both use SHA-256 PIN hashing (same as existing password hashing).
24. **Parent multi-child routing** — `parent_children` in master DB links a parent to students across any tenant DB (stores `student_id`, `group_id`, `db_name`, `school_id`). After parent login, selecting a child calls `POST /mobile/auth/select-child/{linkId}` which returns a `tokenType=student_view` JWT with the child's full context. All data endpoints work identically for student and student_view tokens.
25. **Mobile JWT token types** — `tokenType=student` (student login, has full child context), `tokenType=parent` (parent login; without child context after plain login, with child context after `select-child`). Claims: `accountId/parentId`, `studentId`, `schoolId`, `groupId`, `dbName`, `studentName`, `className`, `admissionNo`. Parent `select-child` re-issues parent token with the selected child's context embedded — same data endpoints work for both token types when `dbName` is present.
26. **Mobile new modules** — `student_attendance`, `exam_types`, `student_marks`, `homework`, `homework_attachments`, `announcements` added to tenant DB. `parent_accounts`, `parent_children`, `parent_refresh_tokens` added to master DB. Run `mobile_master_migration.sql` on `ascent_master` and `mobile_tenant_migration.sql` on each tenant DB.
27. **Sections** — A class can have 1 or more school-defined sections (A, B, C…). `sections` table in tenant DB; `students.section_id` is a nullable FK. Fee structure stays at class level (no per-section fees). Attendance and marks grids require class + section selection. Student form section field is a dropdown loaded by classId. Existing DBs: run `sections_migration.sql`, then `sample_sections.sql`, then `update_student_section_id.sql` (preview first, then uncomment UPDATE).
28. **Bulk CSV import** — Client parses CSV with `papaparse` (npm package), previews first 10 rows, sends rows as JSON array to server. Server builds lookup maps (names → IDs) in one query each, then validates + inserts row by row. Duplicate `admissionNo` within the batch OR already in DB → error row (never silent skip). Fee structure bulk upserts per row (delete+insert per Class+Category+FeeType+Term combination). Max 500 rows per upload. Response: `{ total, imported, failed, errors: [{ row, admissionNo/identifier, reason }] }`. Frontend shows progress bar + error table + download-errors CSV button.
29. **Multi-year student model** — `students` table has one row per student per academic year (same `admission_no`, different `student_id` and `academic_year_id`). Uniqueness enforced by filtered index `UQ_students_admno_school_year` on `(admission_no, school_id, academic_year_id)` WHERE both NOT NULL. Run `student_promotion_migration.sql` on existing tenant DBs. Promotion copies all personal fields into a new row with the target year+class+section; `section_id` is required (staff must pick it — no auto-assign). Students already present in the target year are skipped (idempotent).
30. **Mobile auth after promotion** — A promoted student gets a new `student_id`. On next login, `GetStudentByAdmissionNo` uses `ORDER BY academic_year_id DESC` to find the latest row. Login flow tries `GetAccountByStudentId(latest student_id)` first; if the account still references the old `student_id`, falls back to `GetAccountByAdmissionNo` (JOIN via students table), then calls `UpdateAccountStudentId` to silently re-point the account. No action needed by the student.
31. **Android white-label via product flavors** — One codebase, one repo, one Play Store developer account. Each school is a Gradle product flavor with its own `applicationId` (e.g. `in.educare.srividya`), `BuildConfig.SCHOOL_CODE` string, and `app_name` string resource. The `X-School-Code` API header is set from `BuildConfig.SCHOOL_CODE` in `RetrofitClient` — no runtime school selection. `TokenStore.schoolCode` field removed. Per-school icons go in `src/{flavorName}/res/mipmap-*/`. Each flavor is a separate Play Store listing under the same developer account.
32. **Parent-only mobile app** — Student login removed from Android app. Parent SMS OTP flow: mobile entry → OTP entry → child selector → home. Teacher login via "Staff Login" bottom sheet on auth screen. Backend student auth endpoints still exist for future use.
33. **SMS OTP auth** — `parent_otp` table in `ascent_master` stores SHA-256-hashed OTPs with 5-minute expiry and device_id. Rate limit: max 5 requests per mobile per 10 minutes. `SmsHelper.cs` calls smslogin.mobi HTTP API. Response is always generic — never reveals whether mobile is registered. OTP is only generated if a student with that `father_mobile` exists in the school's tenant DB (resolved from `X-School-Code`). `parent_accounts` has `device_id` + `device_bound_at` columns (run `sms_otp_migration.sql`).
34. **Device binding** — On OTP verify, `device_id` (UUID generated once on install, stored in `EncryptedSharedPreferences`, preserved across `TokenStore.clear()`) is bound to the parent account. If a different device logs in: all old refresh tokens are revoked (previous device silently logged out), new device is bound. Reinstall gets new UUID → treated as new device → OTP required → old session revoked.
35. **School events media storage** — No binary files in SQL Server (fastwebhost.in has limited disk). Media lives on free external services: YouTube (unlisted) for videos, Cloudinary free tier (25 GB) for images. `school_events` tenant table stores only metadata + URLs (~200 bytes/row). Android loads images with Coil (`AsyncImage`); videos open via YouTube Intent. School workflow: upload to YouTube/Cloudinary → paste URL in school web app → appears in mobile gallery tab.
36. **PDF/doc attachments on events and announcements** — `attachment_url` column (nullable VARCHAR 500) added to both `school_events` and `announcements` tables. Stores a Google Drive or Cloudinary PDF/doc link — no files on the server. Android shows an "Download / View Document" `OutlinedButton` when present; tapping opens with `Intent.ACTION_VIEW` (browser or PDF viewer). Homework uses the existing `homework_attachments` table; each attachment is now a tappable button in the Android app showing the filename.
37. **React web app white-label builds (Vite modes)** — Each school has a `.env.{school}` file (`VITE_API_BASE_URL`, `VITE_SUBDOMAIN`) and a `build:{school}` npm script. `npm run build:all` builds all schools at once. `build:all` output is one `dist/` per run — build schools sequentially and upload each `dist/` to the school's hosting directory. Currently configured schools: `demo`, `stannsasf`, `holyspiritjm`. Add a new school: create `.env.{school}`, add three lines to `package.json` scripts. `.env.{school}` files are gitignored.
38. **Coil image loading (Android)** — `io.coil-kt:coil-compose:2.6.0` added to `app/build.gradle.kts`. Used in `EventsScreen.kt` for thumbnail loading via `AsyncImage`. Required for any Compose screen that loads remote images.
39. **Block vs Detain distinction** — `Block` changes `status = 'Blocked'` (excluded from attendance marking, SMS recipient lists, and most active-student queries). `Detain` sets `is_detained = 1` while keeping `status = 'Active'` (student still attends, pays fees, gets marks — but is excluded from the Promote Students preview). Both require a mandatory reason.
40. **SMS bulk sending** — Two-layer approach prevents HTTP timeouts: client splits recipients into batches of 25 or 50 (user-selectable) and POSTs each batch separately; server uses `Task.WhenAll` to dispatch all SMS in a batch in parallel. Each sent/failed result is logged to `sms_logs`. DLT template IDs are placeholder constants in `SmsHelper.cs` — register templates with smslogin.mobi before go-live.
41. **Student promotion eligibility filter** — `GetForPromotion` uses `status IN ('Active', 'Y')` to handle both new-system students (`Active`) and VB6-migrated data (`Y`). Using strict `status = 'Active'` caused 0 results for schools with legacy data. Always use `IN ('Active', 'Y')` for "active student" queries rather than equality.
42. **Legacy status values** — VB6 system used `'Y'`/`'N'` for Active/Inactive. New system uses `'Active'`/`'Inactive'`/`'Blocked'`. Queries that need all active students should use `IN ('Active', 'Y')`. Run `subjects_status_migration.sql` to normalise subject status on existing DBs.
43. **Per-DB SQL credentials** — fastwebhost.in assigns a unique SQL Server username/password per database. `school_groups.db_username`/`db_password` store these. `TenantConnectionFactory.GetTenantConnection` looks them up from master DB (15-min `ConcurrentDictionary` cache), builds the connection string using the server from `TenantDb:ConnectionTemplate` but with per-DB credentials. Falls back to template credentials when columns are NULL (existing/demo groups). Cache is invalidated via `InvalidateCredentialCache(dbName)` after each `PUT /control/school-groups/{id}`. No JWT changes, no repository changes needed.
44. **Parent OTP auto-link** — `parent_accounts` and `parent_children` live in master DB (shared across all schools), enabling a parent with children in multiple schools to have one account. `request-otp` resolves tenant from `X-School-Code`, checks `students.father_mobile` in that school's tenant DB — OTP is only sent if a matching student is found (prevents OTP spam from unregistered numbers). `verify-otp` auto-creates `parent_accounts` if first-time login and upserts `parent_children` rows for every matching student (by `admission_no+group_id` — updates `class_name`, `student_name`, `student_id` if link exists, inserts if not). No pre-existing account or manual link-child flow required. Auto-created accounts have empty `pin_hash` (OTP-only; PIN login returns unauthorized for these accounts, which is correct).
45. **School-scoped child links** — Each white-label Android app (one per school) should only show children belonging to that school. `GET /mobile/auth/parent/children` filters `parent_children` by `group_id` resolved from `X-School-Code`. `POST /mobile/auth/parent/select-child` validates `link.GroupId == tenant.GroupId` as defense-in-depth (a linkId from a different school is rejected with 403). Child link upsert in `verify-otp` uses `GetChildLinkByAdmissionNo` + `UpdateChildLink` so existing links always have up-to-date `class_name` — fixes NULL class_name on links created before class data was assigned or before this fix was deployed.
46. **AntD multi-tab form — `forceRender: true`** — AntD v5 tabs render lazily by default; Form.Items on unvisited tabs are never mounted, so `form.validateFields()` returns `undefined` for their values and `form.setFieldsValue()` silently overwrites them with nulls on save. Fix: add `forceRender: true` to every tab item so all fields mount upfront. Apply to any AntD Tabs that wrap Form fields.
47. **Android app icon — `@mipmap/` not `@drawable/`** — `AndroidManifest.xml` must reference `@mipmap/ic_launcher` and `@mipmap/ic_launcher_round`. Using `@drawable/ic_launcher` loads the old vector XML directly, bypassing all mipmap density buckets generated by Image Asset Studio and producing the default blue rectangle on device. Always use `@mipmap/` for launcher icons in the manifest.
48. **Android Compose logo — use `ic_launcher_round` not `ic_launcher`** — `painterResource(R.mipmap.ic_launcher)` throws a runtime crash on API 26+ because `ic_launcher` resolves to an adaptive icon XML (`<adaptive-icon>`) which `painterResource` cannot decode. `ic_launcher_round` is a pre-rendered WebP bitmap and loads correctly. Always use `R.mipmap.ic_launcher_round` when displaying the launcher icon inside Compose.
49. **`Dp.Unspecified` crashes Material3 `NavigationBar`** — `Dp.Unspecified` equals `Float.NaN`. Passing it as `tonalElevation` to `NavigationBar` causes `surfaceColorAtElevation` to call `Color.copy(alpha = NaN)`, throwing `IllegalArgumentException` and crashing immediately. Never pass `Dp.Unspecified` to any composable that performs arithmetic on it — omit the parameter or use `0.dp`.
50. **R8 strips Google Tink in release builds — `EncryptedSharedPreferences` crash** — `EncryptedSharedPreferences` uses Google Tink internally and loads `KeyTypeManager` subclasses via reflection. R8 removes them as "unused", causing a crash in `Application.onCreate()` before any Activity starts (splash screen + immediate stop). Fix: add `-keep class com.google.crypto.tink.** { *; }` and matching `-keepnames` rules for `KeyTypeManager`/`PrivateKeyTypeManager` to `proguard-rules.pro`. Also wrap `EncryptedSharedPreferences.create()` in try-catch with a plain `SharedPreferences` fallback so future failures degrade gracefully.
51. **Fee type category detection via `description` LIKE** — The 5 fee collection screens filter `fee_types` by `description` column using LIKE patterns: Admission=`%admission%`, School=`%school%|%tuition%`, Transport=`%transport%|%bus%`, Hostel=`%hostel%`, Other=catch-all (NOT matching any of the above). School admins must set `description` on fee types to match the expected keyword (e.g. "Transport Fee", "Hostel Fee") for them to appear in the correct screen. Fee types with no description fall into the Other screen.
52. **Receipt number format change (Phase 24)** — New format: `{prefix}{yr6}{00001}`. `yr6` = `LEFT(academic_year,4) + RIGHT(academic_year,2)` e.g. "2024-25"→"202425". Prefix: T (Transport), H (Hostel), O (Other), or `class_groups.prefix` for Admission/School (falls back to "S"). Counter = MAX of last 5 digits matching `{prefix}{yr6}%` per school. Since the software was not live at the time of this change, no backfill of old receipts is needed.
53. **Cross-year fee outstanding** — `GET /school/fees/student-unique/{uniqueId}?feeTypeCategory=Transport` returns all academic year rows for a student (by `student_unique_id`), with outstanding per year. Each year row uses its own `student_id` for paid-amount calculation (since `fee_receipts.student_id` points to the year-specific student row). `student_unique_id` is also stored in `fee_receipts` for future cross-year reporting.
54. **`student_unique_id` — stable cross-year student identifier** — `students.student_unique_id` is an INT column that stays the same across all promoted rows for the same physical student. Different from `student_id` (IDENTITY, changes every year on promotion) and `admission_no` (can change at class 6 in some schools). Auto-assigned on INSERT via `MAX(student_unique_id)+1 WHERE school_id=@schoolId` — no sequence object needed. Promotion copies the value. Fee collection uses this to look up all academic year balances. `student_unique_id_migration.sql` backfills existing rows by grouping on `admission_no+school_id`.
55. **Monthly payment type for fee structures** — `fee_structures.payment_type` can be 'Term' or 'Monthly'. Term mode uses `term_id` FK; Monthly mode uses `fee_period_id` FK → `fee_periods` table. `FeeStructurePage` detects the type from loaded rows and shows the correct column set. Amount map key format: `{feeTypeId}_T_{termId}` (Term) or `{feeTypeId}_P_{periodId}` (Monthly). Switching payment type in the UI clears the map to prevent stale data. Fee periods are managed in Master Data → Fee Periods tab and loaded by academic year.
56. **Fee concession receipt number format** — `CFR{yr8}{D5}`: prefix "CFR" (3), yr8 = academic_year digits concatenated ("2026-2027"→"20262027", "2026-27"→"202627"), 5-digit counter per school per year. `FeeConcessionRepository.FormatYr8` splits on '-' and concatenates both parts. Counter = `MAX(TRY_CAST(RIGHT(receipt_no,5) AS INT))` filtered by pattern + exact length. Contrast with fee receipt format `{prefix}{yr6}{D5}` which uses only last 6 year digits.
57. **Fee concession outstanding integration** — `fee_concessions` stores one active concession per (student_id, fee_type_id, school_id, term_id) for Term-based fees and per (student_id, fee_type_id, school_id, fee_period_id) for Monthly fees. The outstanding subquery in `FeeRepository.GetStudentFeeSummary` and `GetCrossYearFeeSummary` matches by `ISNULL(fc.term_id,0)=ISNULL(fs.term_id,0)` and `ISNULL(fc.fee_period_id,0)=ISNULL(fs.fee_period_id,0)` so each term/period line only deducts its own concession. `outstanding = structure_amount - paid_receipts - concession_amount`. `ConcessionAmount` added to `FeeLineItemDto` — NOT included in PaidAmount. `FeeConcessionPage` loads terms+fee_periods when year changes and exposes Term/Period dropdowns in both Individual and Bulk tabs; term/period column shown in Concession List and printed on PDF receipt.
58. **Fee concession per-term unique index** — SQL Server treats NULLs as distinct in standard unique indexes (two NULL/NULL rows are allowed). For fee_concessions where term_id or fee_period_id is NULL for the other type, a single unique index on (student_id, fee_type_id, school_id, term_id, fee_period_id) would allow unlimited NULL pairs. Solution: two separate filtered indexes — `UQ_fee_concessions_term` WHERE term_id IS NOT NULL and `UQ_fee_concessions_period` WHERE fee_period_id IS NOT NULL. This enforces exactly one active concession per student+fee_type per term (or per period) while allowing both types to coexist.
59. **Transport update uses student_unique_id, not student_id** — `students.student_id` is a BIGINT IDENTITY that changes every year on promotion. Using it as the transport update key would silently fail after promotion (no matching row). `PUT /school/transport/students/{studentUniqueId}` uses `student_unique_id` (stable INT, same across promotions) + `academic_year_id` (from request body) in the WHERE clause. `StudentTransportDto.StudentUniqueId` and `AcademicYearId` are returned from `GetStudentTransport` so the React modal has these values available when submitting the update.
60. **Transport fee collection uses bus_fee_structures, not fee_structures** — `GetCrossYearFeeSummary` with `feeTypeCategory=Transport` bypasses `fee_structures` entirely. It reads amounts from `bus_fee_structures` (keyed by `route_id + term_id + academic_year_id`) and matches them to the student's `bus_route_id`. Years where the student has no route assigned are skipped (not shown at all). Paid-amount tracking uses the new `fee_receipt_items.bus_route_id` column. This means different students in the same class correctly see different outstanding amounts based on which route they ride.
61. **Transport concession via fee_concessions** — `fee_concessions.fee_type_id` is now nullable; when NULL, `bus_route_id` is set instead (transport concession). The concession subquery in `GetTransportLineItems` matches by `bus_route_id + term_id`. Existing school-fee unique indexes recreated with `AND fee_type_id IS NOT NULL` in filter so transport rows (NULL fee_type_id) are excluded; a separate `UQ_fee_concessions_transport` index enforces one active transport concession per `(student_id, bus_route_id, school_id, term_id)`. `FeeConcessionPage` global toggle "School Fee | Transport" switches both Individual Entry (route+term instead of fee type) and Bulk Apply (loads students by `?busRouteId=` via new filter on `GET /school/students`).
62. **Parent portal is gateway-only** — parents can only pay via Razorpay (UPI/card/netbanking); cash/cheque is not offered through the portal. `MobileFeesController` creates Razorpay orders and verifies HMAC signatures server-side; it never calls `CollectFee` with an offline payment mode. The fee items are sent with `Amount = li.outstanding` and `ConcessionAmount = 0` because `outstanding` already nets out concessions; the server-side outstanding calculation (`GetCrossYearFeeSummary`) applies concessions before returning line items.
63. **Parent portal child context — session-scoped** — after page reload the silent refresh (`POST /mobile/auth/parent/refresh`) restores the parent JWT (without child context, since the cookie is the parent refresh token). The app redirects to `/select-child` rather than persisting the child selection to localStorage. This is intentional: each session the parent explicitly picks which child to pay for, preventing accidental payment for the wrong child.
64. **`Input.OTP` requires AntD ≥ 5.16.0** — `Input.OTP` component was added in AntD 5.16.0. The parent app `package.json` is pinned to `^5.16.0`; the school app is on `^6.x`. Using `^5.14.0` (the original minimum) could install a version without `Input.OTP` if a lockfile is present. Always ensure AntD ≥ 5.16 when `Input.OTP` is used.
65. **Legacy receipt grouping key** — `BulkImportReceipts` groups CSV rows into receipts. When `LegacyReceiptNo` is non-empty, all rows sharing the same `{LegacyReceiptNo}+{AdmissionNo}+{AcademicYear}` form one receipt (preserving the original receipt boundary). When `LegacyReceiptNo` is blank, rows are grouped by `{AdmissionNo}+{AcademicYear}+{PaymentDate}+{PaymentMode}` — one receipt per student per date per payment mode. This covers both: schools that have legacy receipt numbers (use them as keys) and those that only have raw payment data (auto-group by date/mode). `LegacyReceiptNo` is stored in `fee_receipts.remarks` as `"Legacy: {no}"` for audit traceability.
66. **End-of-day receipt sync** — `GET /school/fees/receipts` accepts `?createdAfter=datetime&source=webapp` to return only webapp-originated receipts created after the given timestamp — prevents sync loops (legacy never gets its own data back). `fee_receipts.source` = `'legacy'` for VB6 EOD push / `'webapp'` for school web app, parent portal, and gateway. Legacy bulk import uses `LegacyReceiptNo` (=`FeeReciptID`) directly as `receipt_no`; re-running is safe (dedup by `receipt_no+school_id`). VB6 `EndOfDayReceiptPull` example in `AscentAPI_Examples.bas` — append `&source=webapp` to the URL.
67. **`MobileParentOrderRequest` vs `MobileCreateOrderRequest` — naming disambiguation** — `GatewayDtos.cs` (namespace `School.Fee`) originally defined `MobileCreateOrderRequest` for the parent portal endpoint (`POST /mobile/fees/payment-orders`). `Mobile/Data/FeeDtos.cs` already had a `MobileCreateOrderRequest` for the old student fee endpoint. Both were imported in `MobileStudentController.cs`, causing CS0104 ambiguous reference. Fix: renamed the `School.Fee` version to `MobileParentOrderRequest` throughout `GatewayDtos.cs` and `MobileFeesController.cs`. The two types have different shapes: `MobileParentOrderRequest` has `FeeTypeCategory + List<CollectFeeItem>` (server resolves payment mode); `MobileCreateOrderRequest` has `PaymentModeId + List<MobileFeeOrderItem>` (client-provided mode).
68. **CORS — `X-School-Code` must be in `Access-Control-Allow-Headers`** — `Global.asax.cs` `Application_BeginRequest` sets the preflight response headers. `X-School-Code` was missing from `Access-Control-Allow-Headers`, blocking the parent web app (`localhost:5174`) from sending that header. Added alongside `X-Subdomain`. Both headers must be listed whenever a new custom header is introduced; otherwise the browser blocks the preflight before any request reaches the API.
69. **Android fee screen — selection scoped to one academic year** — items from different academic years cannot be mixed in a single Razorpay order because the backend `POST /mobile/fees/payment-orders` accepts a single `academicYearId`. `FeeScreen.kt` tracks `selectedYearId`; tapping an item from a different year automatically clears the previous selection and starts fresh. This prevents a silent wrong-year order on the backend.
70. **Android persistent cookie jar — OkHttp default jar is in-memory** — OkHttp's `CookieJar.NO_COOKIES` (the default) discards all cookies when the process dies. The server's `parentRefreshToken` (7-day HttpOnly sliding-expiry) was lost on every app kill, forcing OTP re-login the next day. Fix: `PersistentCookieJar` implements `CookieJar`, serializes each `Cookie` as a `SerializedCookie` data class (name, value, domain, path, expiresAt, secure, httpOnly) to a plain `SharedPreferences` file via Gson. Cookies are reloaded into an in-memory `cache` map on construction (keyed `"domain|name"`) and flushed on every `saveFromResponse`. Existing ProGuard rule `-keep class com.ascentschools.mobile.data.api.**` covers `SerializedCookie` and `PersistentCookieJar` (both live in that package).
71. **Android startup silent refresh — ⚠️ REMOVED in Phase 97, do not reintroduce.** The original design (kept here as history): `MainActivity` tracked `isCheckingSession`, and a `LaunchedEffect(Unit)` awaited `authRepo.silentRefresh()`/`silentRefreshTeacher()` behind a `CircularProgressIndicator`, calling `tokenStore.clear()` on failure — mirroring the React web apps. **That was wrong on mobile and caused the daily OTP re-login for months:** a phone opens the app the moment it wakes, often before the network is up, and `runCatching` made "unreachable" indistinguishable from "revoked", so a transient failure deleted a valid 365-day session. A browser tab can afford to re-auth; a phone on a flaky morning network cannot. **Current model:** cold start does zero blocking auth work (the stored access token already carries child context and is long-lived); refresh is lazy via the OkHttp `Authenticator` (#65) plus one fire-and-forget `refreshSessionQuietly()`; the ONLY automatic logout is `RetrofitClient.sessionExpired`, raised when a live call 401s **and** the refresh is explicitly rejected 401/403. **Never copy the web silent-refresh pattern to mobile.**
72. **VB6 API key auth — no JWT needed for legacy integration** — VB6 cannot easily manage JWT access tokens (30-min expiry requires refresh logic, cookies are tricky in `WinHttp`). Solution: static API key per school stored in `ascent_master.school_settings.integration_api_key` (VARCHAR 50, filtered unique index so NULLs don't collide). VB6 sends `X-Api-Key: <key>` header; `JwtAuthenticationFilter` checks this before Bearer JWT. On valid key: `LookupApiKeyAsync` queries master DB (joins `school_settings → schools → school_groups`) to resolve `SchoolId`, `GroupId`, `TenantDbName`, then sets `TenantContext.Current` with all 24 permissions and returns. Every existing school controller action then works identically — `SchoolAuthAttribute` just checks `TenantContext != null`. No changes to any controller, repository, or DTO needed.
73. **VB6 JSON without a library** — VB6 has no built-in JSON support and adding third-party ActiveX controls adds a deployment dependency. `AscentAPI.bas` implements JSON parsing with pure string manipulation: `JsonStr` uses `InStr`/`Mid` with escape-sequence handling; `JsonNum` reads contiguous numeric characters; `JsonBool` checks literal `"true"`; `JsonData` and `JsonSplit` use a depth counter (increment on `{`/`[`, decrement on `}`/`]`, tracking `bInStr` to ignore brackets inside strings) to extract nested objects and split arrays correctly. `BuildObj` uses `VarType()` to decide whether to quote a value — strings get `EscStr()` escaping; booleans emit `true`/`false`; null/empty emit `null`; everything else (Integer, Long, Double, Currency) emits as-is via `CStr()`.
74. **`students` is a system-versioned TEMPORAL table (Phase 79) — ALWAYS wrap ALTERs with SYSTEM_VERSIONING OFF/ON.** Because `SYSTEM_VERSIONING = ON (HISTORY_TABLE = dbo.students_history)`, any future DDL on `students` in a `.sql` script (add/drop/alter column, add index that requires it, rename, etc.) MUST be written as: `ALTER TABLE students SET (SYSTEM_VERSIONING = OFF);` → the `ALTER TABLE students …` change → **apply the same column change to `dbo.students_history`** (the history table must stay schema-identical) → `ALTER TABLE students SET (SYSTEM_VERSIONING = ON (HISTORY_TABLE = dbo.students_history));`. Simple add-column usually auto-propagates to history while ON, but drop/alter/rename do not — toggling OFF/ON and mirroring the change on `students_history` is the safe pattern. Never `TRUNCATE` or `DROP` `students` without turning versioning OFF first. This applies to `tenant_tables.sql`, migrations, and any ad-hoc SSMS script. (Period cols `valid_from`/`valid_to` are HIDDEN; don't list them in INSERTs.)
75. **Push notifications use FCM HTTP v1 with a hand-rolled OAuth token — no SDK (Phase 91).** The legacy server-key API is retired; v1 needs a short-lived OAuth2 token minted from the Firebase **service-account JSON**. Rather than add FirebaseAdmin/Google.Apis.Auth (heavy packages.config deps on the old-style .csproj), `FcmPushService` hand-rolls it (same house style as R2's manual SigV4): RS256-signed JWT → Google token endpoint → cached access token → `messages:send` per token. Signing needs a **PKCS#8** key parsed manually (`.NET Framework 4.8 lacks `ImportPkcs8PrivateKey`) into `RSAParameters`, then `RSACng.SignData(..., SHA256, Pkcs1)` (`RSACryptoServiceProvider` can't do SHA256 without the enhanced-CSP workaround; `RSACng` can). **The service-account JSON is a real secret** — it lives in `API/AscentSchools.API/App_Data/` (IIS denies HTTP access to App_Data, so it's never web-served), is added as csproj Content so it deploys, is **gitignored**, and its path is a Web.config appSetting (`Fcm:ServiceAccountPath`). Missing/unconfigured file → push silently no-ops. **All push fan-out is fire-and-forget on `Task.Run` and MUST capture dbName/groupId/schoolId on the request thread** (never read `TenantContext`/`HttpContext` inside the task — Phase 61 NRE rule). `device_push_tokens` is a **master** table (run its migration once on `ascent_master`, not per tenant). A new white-label app needs its own `google-services.json` entry but shares the one service account — see `FIREBASE_SETUP.md`. **DEPLOY GOTCHA (cost us a debugging session, verified 2026-07-18):** minting the OAuth token succeeds and the project id matches, but `messages:send` returns **403 `PERMISSION_DENIED` / `IAM_PERMISSION_DENIED` — permission `cloudmessaging.messages.create` denied** unless the service account (`firebase-adminsdk-…@<project>.iam.gserviceaccount.com`) has been granted the **"Firebase Cloud Messaging API Admin"** role (`roles/firebasecloudmessaging.admin`) in Google Cloud Console → IAM (the `firebase-adminsdk` account does NOT get it automatically). This is a **project-level setting**, so once granted it applies to both local and prod (same Firebase project). Symptom is invisible without instrumentation — `PushNotifier`/`FcmPushService` swallow all send failures (best-effort), so a content create succeeds with no push and no error. A Firebase-console "Send test message" to a device token does NOT exercise this (it bypasses our service account), so console-works-but-app-doesn't = check this IAM role. Fastest diagnosis: temporarily surface the raw FCM response — `FcmPushService.SendAsync` returns a per-token success count; the send body carries the exact 403 reason.

---

## Mobile App Architecture
- **Platform:** Android (Kotlin + Jetpack Compose, MVVM)
- **Auth flow (Parent):** SMS OTP — enter mobile → receive OTP → verify → child selector → HomeScreen
- **Auth flow (Teacher/Staff):** "Staff Login" bottom sheet on auth screen → username + password → TeacherHomeScreen
- **White-label:** Android product flavors — one APK per school; `applicationId`, `SCHOOL_CODE`, and `app_name` set per flavor in `build.gradle.kts`. `X-School-Code` header is baked in via `BuildConfig.SCHOOL_CODE` (no runtime school selection). Add a new school: copy the flavor template block, update 3 fields, drop icons in `src/{flavorName}/res/mipmap-*/`.
- **Build a school APK:** `./gradlew assemble{FlavorName}Release` (e.g. `assembleSrividyaRelease`). In Android Studio: select flavor from Build Variants panel.
- **Token storage:** `EncryptedSharedPreferences`; `deviceId` auto-generated UUID on first install, preserved across logout (`TokenStore.clear()` saves and restores it); `userType` field ("parent"/"teacher") determines which home screen to show
- **HTTP client:** Retrofit + OkHttp; `X-School-Code` from `BuildConfig.SCHOOL_CODE`, `Authorization` from `TokenStore.accessToken`
- **Android file structure:**
```
ui/auth/
├── SmsAuthScreen.kt      ← primary entry: school logo (`R.mipmap.ic_launcher_round`) + name on login; mobile entry card → OTP entry card; "Staff Login" bottom sheet for teachers
├── SmsAuthViewModel.kt   ← state machine: Idle/Loading/OtpSent/ParentSuccess/TeacherSuccess/Error
├── LoginScreen.kt        ← (legacy, superseded by SmsAuthScreen — may be removed)
└── LoginViewModel.kt     ← (legacy, superseded by SmsAuthViewModel — may be removed)
ui/home/HomeScreen.kt         ← parent shell; single `features` list drives BOTH bottom NavigationBar (Tabs) and the 3-col IconTile dashboard (Tiles); ⋮ toggle Tile/Tab view (persisted TokenStore.tilesView, default Tabs); tiles mode = branding-color logo band + News marquee ticker + IconTile grid w/ live badges + "Powered by Ascent Info Solutions" footer; shared FeatureContent(index) wiring; open feature full-screen + BackHandler (Phase 101)
ui/home/IconTile.kt           ← launcher-style tile (gradient square + glyph + label below + badge) used by parent + teacher tiles; parseBrandColor(hex, fallback) helper
ui/teacher/
├── TeacherHomeScreen.kt      ← class + section dropdowns; Attendance/Homework/Announcements/Messages as either action-card list OR IconTile grid (⋮ Tile/List toggle, Phase 101)
├── TeacherAttendanceScreen.kt← student list; tap=toggle P↔A; long-press=status sheet (P/A/Late); date picker; summary chips
├── TeacherHomeworkScreen.kt  ← homework list with due-date highlighting; FAB → CreateHomeworkSheet
└── TeacherViewModel.kt       ← loadClasses/Sections/Attendance/Homework; toggleStudentStatus; markAllPresent; saveAttendance; createHomework
data/local/TokenStore.kt      ← deviceId (auto-UUID, never cleared); userType; accessToken; studentName; brandingName/brandingLogoUrl/brandingPrimaryColor + tilesView (all survive logout via clear()) etc.
data/repository/AuthRepository.kt    ← requestOtp, verifyOtp, loginTeacher, logoutTeacher, logoutParent, getChildren, selectChild, silentRefresh (parent), silentRefreshTeacher
data/repository/StudentRepository.kt ← getProfile, getAttendance, getMarks, getHomework, getAnnouncements, getEvents
data/repository/FeeRepository.kt     ← getOutstanding(feeTypeCategory) → List<MobileFeeSummaryDto>; createOrder(MobileCreateOrderRequest); verifyPayment(gatewayOrderId, MobileVerifyRequest)
data/repository/TeacherRepository.kt ← getClasses, getSections, getAttendance, saveAttendance, getHomework, createHomework
ui/fee/FeeViewModel.kt        ← categories list (School/Transport/Hostel); selectedCategory StateFlow; FeeUiState.Success holds List<MobileFeeSummaryDto> (cross-year); initiatePayment(items, academicYearId, feeTypeCategory); verifyPayment passes gatewayOrderId as path param
ui/fee/FeeScreen.kt           ← TabRow (School/Transport/Hostel); LazyColumn grouped by academic year; per-year YearSummaryCard + pending/paid items; selection scoped to one year (switching year clears prior selection); concession shown in blue on item card; sticky pay bar
ui/events/EventsViewModel.kt         ← Loading/Success/Error state; load()
ui/events/EventsScreen.kt            ← card list with Coil thumbnail; YouTube overlay (dark scrim + SmartDisplay icon + red badge); attachment OutlinedButton opens Intent.ACTION_VIEW
MainActivity.kt               ← routes: not logged in → SmsAuthScreen; userType=teacher → TeacherHomeScreen; else → HomeScreen (parent); onInitiatePayment callback takes feeTypeCategory: String (not paymentModeId); NO blocking startup refresh (Phase 97 — it can't log the user out); LaunchedEffect(Unit) only registers push + fires a fire-and-forget refreshSessionQuietly(); a second LaunchedEffect collects RetrofitClient.sessionExpired (the sole automatic logout)
```
- **Screens (Parent):** SmsAuthScreen → Child Selector → HomeScreen → Attendance / Marks / Homework / Announcements / Profile / Fees / Events (Phase 11)
- **Screens (Teacher):** SmsAuthScreen (Staff Login sheet) → TeacherHomeScreen → TeacherAttendanceScreen / TeacherHomeworkScreen

---

## Pending / TBD
- **Staff UI** — `staff`, `staff_attendance`, `staff_advances`, `staff_salaries` tables exist in DB (via `staff_tables_migration.sql`) and the Staff section exists in the school app nav, but the full staff management UI (StaffPage, StaffAttendancePage, etc.) may need completion/verification
- **SMS DLT templates** — `SmsHelper.AbsentTemplateId`, `FeeDueTemplateId`, `CustomTemplateId` are placeholder constants; register actual templates with smslogin.mobi before SMS Center goes live
- **`ascent-control-app` env** — create `.env.local` with `VITE_API_URL=http://localhost:62845` for local dev
- **`ascent-school-app` env for local dev** — the school `.env.{school}` files point to `https://edu-care.in/api`; for local dev against a local API, temporarily change `VITE_API_BASE_URL` to `http://localhost:62845`

---

## Reference Files
| File | Purpose |
|---|---|
| `Database\master_tables.sql` | Run once to create `ascent_master` DB (15 tables, includes parent mobile tables) |
| `Database\master_db_migration.sql` | Add `db_name` column to existing `ascent_master.school_groups` |
| `Database\master_db_credentials_migration.sql` | Add `db_username` and `db_password` columns to `ascent_master.school_groups` |
| `Database\tenant_tables.sql` | Run once per school group for `ascent_group_{id}` DB (52 tables + permissions + roles; includes all migrations up to Phase 30) |
| `Database\seed_control_user.sql` | Seed a control app login user into `ascent_master.control_users` |
| `Database\seed_tenant_data.sql` | Seed role-permission mappings, payment modes, default school admin login into tenant DB |
| `Database\sections_migration.sql` | Add `sections` table + `section_id` column to `students` on existing tenant DBs |
| `Database\sample_sections.sql` | Insert Section A and B for every class in a school (run after sections_migration.sql) |
| `Database\update_student_section_id.sql` | Map existing students' free-text section → section_id FK (preview then uncomment UPDATE) |
| `Database\gateway_tables_migration.sql` | Add gateway tables + `is_online` to existing tenant DBs |
| `Database\fee_tables_migration.sql` | Add fee receipt tables to existing tenant DBs |
| `Database\mobile_master_migration.sql` | Add parent_accounts, parent_children, parent_refresh_tokens to `ascent_master` |
| `Database\seed_mobile_parent_data.sql` | Seed 3 demo parent accounts + child links into `ascent_master` (run after demo_data.sql) |
| `Database\mobile_tenant_migration.sql` | Add student_mobile_accounts, student_refresh_tokens, student_attendance, exam_types, student_marks, homework, homework_attachments, announcements to tenant DBs |
| `Database\student_promotion_migration.sql` | Add `UQ_students_admno_school_year` filtered unique index to enable multi-year student records on existing tenant DBs |
| `Database\sms_otp_migration.sql` | Add `device_id`/`device_bound_at` to `parent_accounts`; create `parent_otp` table in `ascent_master` |
| `Database\events_migration.sql` | Add `school_events` table (with `attachment_url`) to existing tenant DBs (Phase 11) |
| `Database\announcements_attachment_migration.sql` | Add `attachment_url` column to existing `announcements` table |
| `Database\block_student_migration.sql` | Add `blocked_reason` column to `students` on existing tenant DBs |
| `Database\detained_students_migration.sql` | Add `is_detained` + `detained_reason` columns to `students` on existing tenant DBs |
| `Database\staff_tables_migration.sql` | Add staff, staff_attendance, staff_advances, staff_advance_repayments, staff_salary_components, staff_salaries, staff_salary_items to existing tenant DBs |
| `Database\sms_migration.sql` | Add `sms_logs` table to existing tenant DBs |
| `Database\subjects_status_migration.sql` | Widen subjects.status VARCHAR(5→10); normalise 'Y'→'Active', 'N'→'Inactive' on existing tenant DBs |
| `Database\homework_attachment_url_migration.sql` | Add `attachment_url` column to `homework` table on existing tenant DBs |
| `Database\homework_section_migration.sql` | Add `section_id` nullable FK column to `homework` table on existing tenant DBs |
| `Database\fee_periods_migration.sql` | Add `fee_periods` table + `payment_type`/`fee_period_id` to `fee_structures` + `fee_period_id` to `fee_receipt_items` on existing tenant DBs |
| `Database\student_unique_id_migration.sql` | Add `student_unique_id` INT column to `students` and backfill (groups by admission_no+school_id) on existing tenant DBs |
| `Database\fee_collection_redesign_migration.sql` | Add `student_unique_id` column to `fee_receipts` on existing tenant DBs |
| `Database\fee_concessions_migration.sql` | Add `fee_concessions` table (with `term_id`/`fee_period_id` columns and two filtered unique indexes) on existing tenant DBs |
| `Database\transport_receipt_migration.sql` | Add `bus_route_id INT NULL` + FK to `fee_receipt_items` on existing tenant DBs |
| `Database\transport_concession_migration.sql` | Make `fee_concessions.fee_type_id` nullable; add `bus_route_id INT NULL` + FK; recreate term/period unique indexes with `fee_type_id IS NOT NULL` filter; add `UQ_fee_concessions_transport` index on existing tenant DBs |
| `Database\hostel_migration.sql` | Add `hostels` table, `hostel_fee_structures` table; add `hostel_id INT NULL FK` to `students`, `fee_receipt_items`, `fee_concessions`; add hostel concession filtered unique indexes on existing tenant DBs |
| `Database\vb_integration_migration.sql` | Add `integration_api_key VARCHAR(50) NULL` + filtered unique index to `ascent_master.school_settings`; includes commented SQL to generate/assign/view the key per school |
| `Database\school_settings_seed_migration.sql` | Insert a `school_settings` row for every school branch that doesn't have one yet (safe to re-run; IF NOT EXISTS guard) |
| `Database\sms_config_migration.sql` | Add `sms_configs` + `sms_templates` (per-school SMS gateway account + DLT templates) to existing tenant DBs; seeds existing schools with the old shared account + 3 default templates (template_id blank). Run once per tenant DB. OTP unaffected. |
| `Database\app_config_migration.sql` | Add `app_config` to `ascent_master` (mobile force/soft update gate); seeds a row per published applicationId + `'*'` fallback. Run once on master. |
| `Database\app_config_auto_update_migration.sql` | Add `auto_update_enabled BIT DEFAULT 0` to `app_config` (mobile auto-prompt opt-in, Phase 71); defaults OFF for all rows so auto-prompts stop on apply. Idempotent. Run once on master. |
| `Database\admin_permissions_migration.sql` | Insert the 15 Phase-50 permission codes + map them to the 4 default roles (full role-permission repair, idempotent) on existing tenant DBs (Phase 50/53). |
| `Database\grade_types_migration.sql` | Add `grade_types` table (marks-to-grade bands per subject; migrated from legacy `SAS_MarksGrade`) to existing tenant DBs. Idempotent (`IF NOT EXISTS`). Run once per tenant DB. |
| `Database\exam_master_migration.sql` | Add `exam_master` table (per-class/subject exam definitions; migrated from legacy `SAS_ExamNam`) to existing tenant DBs. Depends on exam_types/classes/academic_years/subjects/grade_types. Idempotent (`IF NOT EXISTS`). Run once per tenant DB. |
| `Database\users_staff_link_migration.sql` | Add `FK_users_staff (users.employee_id → staff.staff_id)` + filtered unique index `UQ_users_employee` (one login per staff) on existing tenant DBs (Phase 75). Column already exists — constraints only. Idempotent. Run once per tenant DB. |
| `Database\align_student_unique_id.sql` | One-time alignment of `students.student_unique_id` with legacy `StuUnqID` for sync-tool-created students (Phase 76). Cross-DB (tenant + legacy same instance); preview-first, transaction-wrapped (ROLLBACK default). Set `@SchoolId`, tenant DB, and legacy DB name. Not needed for AscentMigration-migrated schools. |
| `Database\students_updated_audit_migration.sql` | Add `updated_by` + `updated_at` columns to `students` on existing tenant DBs (Phase 78). Idempotent (`IF NOT EXISTS`). Run once per tenant DB. |
| `Database\students_temporal_migration.sql` | Turn `students` into a system-versioned temporal table (full-row audit history in `students_history`) on existing tenant DBs (Phase 79). Requires SQL Server 2016+. Idempotent. Run once per tenant DB (after `students_updated_audit_migration.sql`). |
| `Database\r2_config_migration.sql` | Add `r2_configs` table (per-school Cloudflare R2 storage credentials) to existing tenant DBs (Phase 84). Idempotent (`IF NOT EXISTS`). Run once per tenant DB. |
| `Database\media_uploads_migration.sql` | Add `media_uploads` table (R2 uploads for homework/announcements/events) + relax `school_events.media_url` to NULL, on existing tenant DBs (Phase 85). Idempotent. Run once per tenant DB. |
| `Database\announcements_section_migration.sql` | Add nullable `section_id` FK to `announcements` (teacher section-targeted announcements) on existing tenant DBs (Phase 90). Idempotent. Run once per tenant DB. |
| `Database\device_push_tokens_migration.sql` | Add `device_push_tokens` (FCM push tokens) to **`ascent_master`** (Phase 91). Idempotent. **Run ONCE on the master DB, NOT per tenant.** |
| `Database\class_teacher_assignments_migration.sql` | Add `class_teacher_assignments` (class → teacher mapping; prerequisite for messaging) to existing tenant DBs (Phase 92). Idempotent. Run once per tenant DB. |
| `Database\messaging_migration.sql` | Add `message_threads`, `messages`, `message_reports` (parent↔teacher messaging) to existing tenant DBs (Phase 92). Idempotent. Run per tenant DB **after** `class_teacher_assignments_migration.sql`. |
| `Database\homework_due_date_nullable_migration.sql` | Make `homework.due_date` NULLABLE (due date retired — Phase 93) on existing tenant DBs. Idempotent. Run once per tenant DB. |
| `Database\homework_index_migration.sql` | Drop obsolete `IX_homework_class_due`; add `IX_homework_school_assigned` + `IX_homework_class_assigned` matching the assigned_date sort (Phase 94). Idempotent. Run once per tenant DB. |
| `Database\fee_receipts_ist_default_migration.sql` | Replace `fee_receipts.payment_date` + `created_at` DEFAULTs with an IST expression (`SYSDATETIMEOFFSET() AT TIME ZONE 'India Standard Time'`) so server-stamped receipt dates aren't a day behind (server runs US Eastern) — Phase 98. Idempotent (drops the auto-named default constraints, re-adds IST ones). Run once per tenant DB. |
| `FIREBASE_SETUP.md` | Firebase/FCM setup guide (repo root) — console project + per-app registration, `google-services.json` + service-account JSON placement, secrets/gitignore, and how to add a new white-label app (Phase 91). |
| `Database\cleanup_stray_attendance.sql` | Remove stray `student_attendance` rows saved against non-current-year student rows by the old all-years grid (Phase 66/67); preview-first, transaction-wrapped, per tenant DB. |
| `deploy\deploy-api.ps1` / `deploy-school.ps1` / `deploy-parent.ps1` | One-click build + FTP deploy (PowerShell, native FTP). Config in gitignored `deploy/deploy.config.json` (copy from `deploy.config.example.json`). See `deploy/README.md`. Run from repo root. |
| `deploy\deploy-privacy.ps1` | Upload the static per-school privacy-policy pages to `https://edu-care.in/{folder}/privacy-policy.html` (Phase 95). Folders chakin/stannsasf/holyspiritjm/depaul → `/httpdocs/{folder}/`. `-IncludeAccountDeletion` also pushes account-deletion.html. |
| **Play Store release guide (artifact)** | https://claude.ai/code/artifact/6769f494-9ad8-42a3-be28-6c12066454a2 — step-by-step Play Console release: build/sign AABs, Data safety, content-rating question→answer key, App access reviewer login (9999999999 / 123456; CHAK IN code 1000), submit + rollout. **Reuse for every future Play release** (new schools, next CHAK IN version). |
| `Database\eod_sync_migration.sql` | Add `source VARCHAR(10) NULL` to `fee_receipts`; tags all existing receipts as `'webapp'`; run on each existing tenant DB for EOD legacy sync |
| `Database\cleanup_migration_data.sql` | Manual SSMS cleanup before re-migration — deletes all migrated tables for one `@SchoolId` in reverse FK order + clears `_migration_log`; transaction-wrapped (rolls back if live data blocks a delete); alternative to the migration tool's `Cleanup=true` flag |
| `Database\cleanup_stray_attendance.sql` | Removes "stray" `student_attendance` rows saved against non-current academic-year student rows by the old all-years grid bug (Phase 66/67). Preview-first (2 SELECTs); set `@SchoolId`, run per tenant DB. SAFE DELETE (recommended) removes only provable duplicates — a prior-year row whose physical student (`student_unique_id`) also has a current-year mark for the same date; AGGRESSIVE DELETE wipes all non-current-year attendance. Both transaction-wrapped, default `ROLLBACK` (dry run) until you switch to `COMMIT`. |
| `Database\legacy_classdata_migration.sql` | Seed class groups (Pre-Primary/Primary/High School/General, IDENTITY_INSERT for predictable IDs) and 26 classes for a specific school; one-time manual alternative to running `ClassesMigrator` via the migration tool |
| `AscentMigration\AscentMigration.sln` | VB6 legacy data migration tool — .NET 4.8 console app; configure `App.config` (Source/Dest connection strings, SchoolId, DefaultAcademicYearId, Modules, DryRun) then run |
| `VbIntegration\AscentAPI.bas` | VB6 integration module — API key auth, JSON helpers, master data, students, fees, attendance, bulk import functions |
| `VbIntegration\AscentAPI_Examples.bas` | VB6 usage examples — startup, master data print, student sync, fee display, cash/cheque collection, bulk import, end-of-day pull |
| `Database\create_tables.sql` | Legacy single-DB version — reference only, do not use |
| `Schema_Proposal.md` | Full old VB6 → new column mapping with FK summary |
| `Clarification Answers.txt` | 35 domain Q&A answered by client |
